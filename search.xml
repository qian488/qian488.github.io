<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>VSC与Git常用命令</title>
    <url>/2024/01/17/VSC%E4%B8%8EGit%E5%B8%B8%E7%94%A8%E5%91%BD%E4%BB%A4/</url>
    <content><![CDATA[<h2 id="VSC终端基础"><a href="#VSC终端基础" class="headerlink" title="VSC终端基础"></a>VSC终端基础</h2><ol>
<li><p>清除终端</p>
<p><code>clear</code></p>
</li>
<li><p>列出当前目录中的文件和文件夹</p>
<p><code>ls</code></p>
</li>
<li><p>切换指定目录</p>
<p><code>cd &lt;目录路径&gt;</code></p>
</li>
<li><p>返回上一级目录</p>
<p><code>cd ..</code></p>
</li>
<li><p>创建新文件夹</p>
<p><code>mkdir &lt;文件夹名&gt;</code></p>
</li>
<li><p>创建新文件</p>
<p><code>touch &lt;文件名&gt;</code></p>
</li>
<li><p>打开文件</p>
<p><code>code &lt;文件名&gt;</code></p>
</li>
<li><p>删除文件</p>
<p><code>rm &lt;文件名&gt;</code></p>
</li>
<li><p>删除文件夹</p>
<p><code>rm -r &lt;文件夹名&gt;</code></p>
</li>
<li><p>查找文件</p>
<p><code>find &lt;目录路径&gt; -name &lt;文件名&gt;</code></p>
</li>
<li><p>查找文件内容</p>
<p><code>grep &lt;搜索内容&gt; &lt;文件名&gt;</code></p>
</li>
</ol>
<h2 id="Git版本控制"><a href="#Git版本控制" class="headerlink" title="Git版本控制"></a>Git版本控制</h2><ol>
<li><p>初始化Git仓库</p>
<p><code>git init</code></p>
</li>
<li><p>克隆远程仓库到本地</p>
<p><code>git clone &lt;远程仓库地址&gt;</code></p>
</li>
<li><p>添加文件到暂存区</p>
<p><code>git add &lt;文件名&gt;</code></p>
</li>
<li><p>添加所有修改文件到暂存区</p>
<p><code>git add .</code></p>
</li>
<li><p>提交暂存区文件到版本库</p>
<p><code>git commit -m &quot;提交信息&quot;</code></p>
</li>
<li><p>查看当前仓库状态</p>
<p><code>git status</code></p>
</li>
<li><p>查看文件修改差异</p>
<p><code>git diff &lt;文件名&gt;</code></p>
</li>
<li><p>查看提交历史</p>
<p><code>git log</code></p>
</li>
<li><p>创建新分支</p>
<p><code>git branch &lt;分支名&gt;</code></p>
</li>
<li><p>切换到指定分支</p>
<p><code>git checkout &lt;分支名&gt;</code></p>
</li>
<li><p>合并指定分支到当前分支</p>
<p><code>git merge &lt;分支名&gt;</code></p>
</li>
<li><p>拉取远程仓库更新</p>
<p><code>git pull</code></p>
</li>
<li><p>推送本地提交到远程仓库</p>
<p><code>git push</code></p>
</li>
<li><p>查看远程仓库地址</p>
<p><code>git remote -v</code></p>
</li>
<li><p>删除分支</p>
<p><code>git branch -d &lt;分支名&gt;</code></p>
</li>
</ol>
<h2 id="Git介绍"><a href="#Git介绍" class="headerlink" title="Git介绍"></a>Git介绍</h2><p><em>Git</em>是一个分布式版本控制系统，用于跟踪文件和代码的变化，并支持多人协同开发。</p>
]]></content>
      <tags>
        <tag>test</tag>
      </tags>
  </entry>
  <entry>
    <title>基于hexo与git的个人博客快速部署</title>
    <url>/2024/01/17/%E5%9F%BA%E4%BA%8Ehexo%E4%B8%8Egit%E7%9A%84%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2%E5%BF%AB%E9%80%9F%E9%83%A8%E7%BD%B2/</url>
    <content><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>首先，这个方法仅仅适于新手尝鲜，有想法搭建一个个人博客玩玩的。<del>没错，我就是先尝试玩玩</del>。真要想制作个人网站，最好是去学习web开发的相关知识。</p>
<p>Hexo是一款基于Node.js的静态博客框架，可以方便的生成静态网页托管在GitHub上，是搭建该个人博客的框架。故而，首先得确保电脑有配置好相对应的环境。没有也别担心，本文会详细说明步骤。</p>
<h3 id="安装Git和Node-js"><a href="#安装Git和Node-js" class="headerlink" title="安装Git和Node.js"></a>安装Git和Node.js</h3><p>直接官网下载即可，安装部分，纯小白可以无脑下一步，有点基础的可以自行谷歌。</p>
<p><strong>注意！</strong>最好下载长期支持版，即LTS</p>
<p>其他版本可能不稳定。</p>
<h4 id="安装Git"><a href="#安装Git" class="headerlink" title="安装Git"></a>安装Git</h4><p>Git官网：<a href="https://git-scm.com/">https://git-scm.com/</a></p>
<p>安装完后，可以在电脑桌面，右键，有出现<strong>Open Git Bash here</strong>即为成功。</p>
<h4 id="安装Node-js"><a href="#安装Node-js" class="headerlink" title="安装Node.js"></a>安装Node.js</h4><p>Node.js官网：<a href="https://nodejs.org/">https://nodejs.org/</a></p>
<p>安装完成后，打开cmd窗口或者PowerShell窗口,输入</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">node -v<br></code></pre></td></tr></table></figure>

<p>返回版本号即为成功</p>
<h3 id="安装Hexo"><a href="#安装Hexo" class="headerlink" title="安装Hexo"></a>安装Hexo</h3><p>在电脑本地新建一个用于安装博客的文件夹，进入文件夹，右键，Open Git Bash here 就会打开一个git窗口，显示的地址就是文件夹的路径。</p>
<p>然后打开Hexo官网，将官网给出的几条命令逐条复制到git窗口，回车表示运行</p>
<p>Hexo官网：<a href="https://hexo.io/zh-cn/index.html">https://hexo.io/zh-cn/index.html</a></p>
<h4 id="安装Hexo框架"><a href="#安装Hexo框架" class="headerlink" title="安装Hexo框架"></a>安装Hexo框架</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">npm install hexo-cli -g<br></code></pre></td></tr></table></figure>

<h4 id="初始化博客"><a href="#初始化博客" class="headerlink" title="初始化博客"></a>初始化博客</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">hexo init blog<br></code></pre></td></tr></table></figure>

<h4 id="进入博客文件夹"><a href="#进入博客文件夹" class="headerlink" title="进入博客文件夹"></a>进入博客文件夹</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">cd blog<br></code></pre></td></tr></table></figure>

<h4 id="安装Hexo的依赖包文件"><a href="#安装Hexo的依赖包文件" class="headerlink" title="安装Hexo的依赖包文件"></a>安装Hexo的依赖包文件</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">npm install<br></code></pre></td></tr></table></figure>

<h4 id="启动Hexo服务"><a href="#启动Hexo服务" class="headerlink" title="启动Hexo服务"></a>启动Hexo服务</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">hexo server<br></code></pre></td></tr></table></figure>

<p>在浏览器输入<strong><a href="http://localhost:4000/">http://localhost:4000</a></strong>即可本地访问博客，推荐使用chrome浏览器。</p>
<p>按F12，进入开发者模式，可以查看网页的元素等。</p>
<h3 id="编辑博客"><a href="#编辑博客" class="headerlink" title="编辑博客"></a>编辑博客</h3><p>在前面打开的git窗口中输入以下命令</p>
<h4 id="创建文章"><a href="#创建文章" class="headerlink" title="创建文章"></a>创建文章</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">hexo new &quot;文章标题&quot;<br></code></pre></td></tr></table></figure>

<p>在<code>/blog/souce/posts</code>文件夹下可以看到新建的文章，以markdown文件形式，打开即可使用markdown编辑文章。</p>
<p>可以使用<strong>Visual Studio Code</strong>（轻量级文本编辑器)来进行编辑，不过更推荐使用<strong>Typora</strong>(轻量级Markdown编辑器)。</p>
<h4 id="生成网页"><a href="#生成网页" class="headerlink" title="生成网页"></a>生成网页</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">hexo g<br></code></pre></td></tr></table></figure>

<h4 id="运行博客"><a href="#运行博客" class="headerlink" title="运行博客"></a>运行博客</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">hexo s<br></code></pre></td></tr></table></figure>

<p>即回到浏览器刷新一下即可看到编辑的文件，如果不行，就重新进入 <strong><a href="http://localhost:4000/">http://localhost:4000</a></strong>即可。</p>
<h3 id="更改主题样式"><a href="#更改主题样式" class="headerlink" title="更改主题样式"></a>更改主题样式</h3><p>打开Hexo官网，寻找喜欢的主题，就可以进入对应主题的Github仓库，然后跟着主题的配置指南即可。这里我以本博客的主题样式为例。<a href="https://github.com/Fechin/hexo-theme-diaspora">Diaspora</a>，通过链接去到主题的Github仓库，可以跟着指南配置。</p>
<h4 id="下载主题"><a href="#下载主题" class="headerlink" title="下载主题"></a>下载主题</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git clone https://github.com/Fechin/hexo-theme-diaspora.git themes/diaspora<br></code></pre></td></tr></table></figure>

<p>在<strong>Themes</strong>文件夹下，可以看到下载好的主题</p>
<p>需要对主题进行个性化修改，就进入对应主题的文件夹下<code>_config.yml</code>可以进行对应的配置。如果懂相关知识就可以随便改了<del>（bushi）</del></p>
<p>一般来说，一般人主要更改的就是图像以及音频，然后其他功能需要就开，不需要就关掉。</p>
<p>图像部分，找到<strong>cover</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext"># 默认文章封面图（随机调用,支持外链）<br>cover: <br>  - /img/图片文件名<br></code></pre></td></tr></table></figure>

<p>记得将想要放置的图片放进<code>/Themes/img</code>文件夹下</p>
<p>音频部分，确保开启音频，</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">autoplay: true<br><br># 默认音乐（随机播放）<br>mp3: <br>    - 音频的外链地址，如(http://music.163.com/song/media/outer/url?id=869119.mp3)<br></code></pre></td></tr></table></figure>

<p>不知道外链怎么获取的可以去谷歌或者bing，当然也可以直接去这个网站找<a href="https://music.liuzhijin.cn/?name=%E4%B8%9C%E6%96%B9&type=netease">https://music.liuzhijin.cn/?name=%E4%B8%9C%E6%96%B9&amp;type=netease</a></p>
<h4 id="启用主题"><a href="#启用主题" class="headerlink" title="启用主题"></a>启用主题</h4><p>修改Hexo配置文件 <code>_config.yml</code> 找到主题项，设置为diaspora</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">...<br>theme: diaspora<br>...<br></code></pre></td></tr></table></figure>

<p>在这个配置文件中可以修改网站的title等信息，一并修改后</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext"># Site<br>title: 千幻笙的小窝<br>subtitle: &#x27;Awith的小站&#x27;<br>author: Awith<br></code></pre></td></tr></table></figure>

<p><strong>Ctrl+S</strong>保存退出</p>
<p>在git窗口生成网页，运行博客，在浏览器查看主题样式。如果git窗口之前不小心关了，可以去到自己建的用于安装hexo的文件夹，右键，Open Git Bash here 就会打开一个git窗口。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">hexo g<br>hexo s<br></code></pre></td></tr></table></figure>

<p>刷新一下浏览器即可查看更改后的网页。</p>
<h3 id="注册Github账号以及新建仓库"><a href="#注册Github账号以及新建仓库" class="headerlink" title="注册Github账号以及新建仓库"></a>注册Github账号以及新建仓库</h3><p>要将我们的个人博客网站部署到GitHub Pages上，拥有一个GitHub账户自然就是最基本的前提了。当然也可以部署到Gitee Pages上，或者是有自己的服务器就部署到自己的服务器上。一般主要尝试一下的话，直接在GitHub Pages上就好了。如果没有魔法，那就配置在Gitee Pages上吧。</p>
<p>Github官网：<a href="https://github.com/">https://github.com/</a></p>
<p>注册完账号后新建一个仓库用于个人博客</p>
<p><strong>注意！</strong>仓库名应该为：**<code>用户名.github.io </code><strong>这个</strong>用户名**使用你的GitHub帐号名称代替</p>
<p>创建好仓库后，在setting里找到pages，可以看到GitHub Pages给我们提供了一个格式为<code>https://用户名.github.io/ </code>的免费域名，并且相应的网站是从该仓库的 main 分支构建得到的。复制该网站到浏览器，即可查看形成的网页。</p>
<h3 id="上传博客到Github托管"><a href="#上传博客到Github托管" class="headerlink" title="上传博客到Github托管"></a>上传博客到Github托管</h3><p>在git窗口执行命令行</p>
<h4 id="部署Git全局配置"><a href="#部署Git全局配置" class="headerlink" title="部署Git全局配置"></a>部署Git全局配置</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git config --global user.name &quot;你的GitHub用户名&quot;<br>git config --global user.email &quot;你的GitHub注册邮箱&quot;<br></code></pre></td></tr></table></figure>

<p>没有报错，就是成功</p>
<h4 id="生成ssh密钥文件"><a href="#生成ssh密钥文件" class="headerlink" title="生成ssh密钥文件"></a>生成ssh密钥文件</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">ssh-keygen -t rsa -C &quot;你的GitHub注册邮箱&quot;<br></code></pre></td></tr></table></figure>

<p>然后直接三个回车即可，默认不需要设置密码。</p>
<p>输入以下命令获取密钥</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">cat ~/.ssh/id_rsa.pub<br></code></pre></td></tr></table></figure>

<p>将显示的密钥复制，在Github仓库setting下Deploy Keys，公钥随便输入，密钥就填刚才生成的。</p>
<h4 id="检测ssh是否成功"><a href="#检测ssh是否成功" class="headerlink" title="检测ssh是否成功"></a>检测ssh是否成功</h4><p>输入以下命令</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">ssh -T git@github.com<br></code></pre></td></tr></table></figure>

<p>若出现successfully即为成功，若出现</p>
<p>This key is not known by any other names.<br>Are you sure you want to continue connecting (yes&#x2F;no&#x2F;[fingerprint])?</p>
<p>一般情况下，不用担心，输入yes，回车就可以了。</p>
<p>这是一个安全特性，用于确保你连接到的主机是你预期的主机，而不是中间人攻击。这个提示的意思是，你的计算机之前没有连接过这个主机，因此无法验证其身份。你需要决定是否继续连接。</p>
<p>你有以下选项：</p>
<ol>
<li>输入”yes”，继续连接到主机。这将更新你的<code>~/.ssh/known_hosts</code>文件，将主机的密钥添加到已知主机列表中。</li>
<li>输入”no”，取消连接到主机。</li>
</ol>
<h4 id="设置部署仓库和分支"><a href="#设置部署仓库和分支" class="headerlink" title="设置部署仓库和分支"></a>设置部署仓库和分支</h4><p>回到我们前面创建的用于安装hexo的文件夹，打开目录下面的<code>_config.yml</code>文件，并滚动到文件最后，找到deploy，进行更改。</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">deploy:<br>  type: &#x27;git&#x27;<br>  repository: git@github.com:qian488/qian488.github.io.git<br>  branch: main<br></code></pre></td></tr></table></figure>

<p>关于repository的获取，就是去创建的GitHub的仓库下，点击code，点击ssh，看到SSH key，复制上面的即可。</p>
<p>关于branch的填写，需要跟之前GitHub形成网页的分支一样，即main</p>
<p><strong>注意！</strong>冒号后要空格。</p>
<h4 id="安装Git部署插件"><a href="#安装Git部署插件" class="headerlink" title="安装Git部署插件"></a>安装Git部署插件</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">npm install hexo-deployer-git --save<br></code></pre></td></tr></table></figure>

<p>等待安装完成后即可。</p>
<h4 id="部署网页"><a href="#部署网页" class="headerlink" title="部署网页"></a>部署网页</h4><p>清除缓存，生成网页，运行博客，检查没什么问题后，就实施部署</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">hexo clean <br>hexo g<br>hexo s<br>hexo d<br></code></pre></td></tr></table></figure>

<p>去到Github的仓库即可看到上传的内容，而进入GitHub Pages提供的网站，即可看到个人博客搭建成功，将该网站分享给好友，也可查看该个人博客。</p>
<h3 id="后续更新博客"><a href="#后续更新博客" class="headerlink" title="后续更新博客"></a>后续更新博客</h3><p>主要步骤就是</p>
<p>1.创建文章</p>
<p>2.编写博客</p>
<p>3.生成网页</p>
<p>4.部署网页</p>
<p>5.检查网页</p>
<p>希望对你有用！</p>
]]></content>
      <tags>
        <tag>hexo git</tag>
      </tags>
  </entry>
  <entry>
    <title>Learning Git Branching 通关</title>
    <url>/2024/01/19/Learning-Git-Branching-%E9%80%9A%E5%85%B3/</url>
    <content><![CDATA[<p>好耶、边玩边学！！！</p>
<p><strong>Learning Git Branching游玩地址：</strong><a href="https://learngitbranching.js.org/?locale=zh_CN">https://learngitbranching.js.org/?locale=zh_CN</a></p>
<p>我在思考需不需要将知识点总结写出来，但是我觉得这个网页的闯关式教学就已经挺好的。于是，我决定只保留每一关的问题与我的答案。<code>Q</code>表示问题，<code>A</code>表示答案。就当是一个小项目的分享。</p>
<p>强烈建议直接进入网站游玩闯关。</p>
<h3 id="Git-commit"><a href="#Git-commit" class="headerlink" title="Git commit"></a>Git commit</h3><h4 id="Q"><a href="#Q" class="headerlink" title="Q"></a>Q</h4><p>进行两次提交</p>
<h4 id="A"><a href="#A" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git commit<br>git commit<br></code></pre></td></tr></table></figure>

<h3 id="Git-branch"><a href="#Git-branch" class="headerlink" title="Git branch"></a>Git branch</h3><h4 id="Q-1"><a href="#Q-1" class="headerlink" title="Q"></a>Q</h4><p>创建一个名为 <code>bugFix</code> 的新分支，然后切换过去。</p>
<h4 id="A-1"><a href="#A-1" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git branch bugFix<br>git checkout bugFix<br></code></pre></td></tr></table></figure>

<p>or</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git checkout -b bugFix<br></code></pre></td></tr></table></figure>

<h3 id="Git-merge"><a href="#Git-merge" class="headerlink" title="Git merge"></a>Git merge</h3><h4 id="Q-2"><a href="#Q-2" class="headerlink" title="Q"></a>Q</h4><p>创建新分支 <code>bugFix</code>，切换到该分支，提交一次</p>
<p>切换回 <code>main</code>，再提交一次</p>
<p>把 <code>bugFix</code> 合并到 <code>main</code></p>
<h4 id="A-2"><a href="#A-2" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git checkout -b bugFix<br>git commit<br>git checkout main<br>git commit<br>git merge bugFix<br></code></pre></td></tr></table></figure>

<h3 id="Git-rebase"><a href="#Git-rebase" class="headerlink" title="Git rebase"></a>Git rebase</h3><h4 id="Q-3"><a href="#Q-3" class="headerlink" title="Q"></a>Q</h4><p>新建并切换到 <code>bugFix</code> 分支，提交一次</p>
<p>切换回 <code>main</code> 分支再提交一次</p>
<p>再次切换到 <code>bugFix</code> 分支，rebase 到 <code>main</code> 上</p>
<h4 id="A-3"><a href="#A-3" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git checkout -b bugFix<br>git commit<br>git checkout main<br>git commit<br>git checkout bugFix<br>git rebase main<br></code></pre></td></tr></table></figure>

<h3 id="分离HEAD"><a href="#分离HEAD" class="headerlink" title="分离HEAD"></a>分离HEAD</h3><h4 id="Q-4"><a href="#Q-4" class="headerlink" title="Q"></a>Q</h4><p>从 <code>bugFix</code> 分支中分离出 HEAD 并让其指向一个提交记录。</p>
<p>通过哈希值指定提交记录。每个提交记录的哈希值显示在代表提交记录的圆圈中。</p>
<h4 id="A-4"><a href="#A-4" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git checkout c4<br></code></pre></td></tr></table></figure>

<h3 id="相对引用"><a href="#相对引用" class="headerlink" title="相对引用(^)"></a>相对引用(^)</h3><h4 id="Q-5"><a href="#Q-5" class="headerlink" title="Q"></a>Q</h4><p>切换到 <code>bugFix</code> 的 parent 节点。这会进入分离 <code>HEAD</code> 状态。</p>
<h4 id="A-5"><a href="#A-5" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git checkout bugFix^<br></code></pre></td></tr></table></figure>

<h3 id="相对引用2"><a href="#相对引用2" class="headerlink" title="相对引用2(~)"></a>相对引用2(~)</h3><h4 id="Q-6"><a href="#Q-6" class="headerlink" title="Q"></a>Q</h4><p>移动 <code>HEAD</code>，<code>main</code> 和 <code>bugFix</code> 到目标所示的位置。</p>
<h4 id="A-6"><a href="#A-6" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git checkout HEAD^<br>git branch -f bugFix HEAD^<br>git branch -f main c6<br></code></pre></td></tr></table></figure>

<p>or</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git branch -f bugFix HEAD~2<br>git checkout HEAD^<br>git branch -f main c6<br></code></pre></td></tr></table></figure>

<h3 id="撤销变更"><a href="#撤销变更" class="headerlink" title="撤销变更"></a>撤销变更</h3><h4 id="Q-7"><a href="#Q-7" class="headerlink" title="Q"></a>Q</h4><p>分别撤销 <code>local</code> 分支和 <code>pushed</code> 分支上的最近一次提交。共需要撤销两个提交（每个分支一个）。</p>
<p>记住 <code>pushed</code> 是远程分支，<code>local</code> 是本地分支</p>
<h4 id="A-7"><a href="#A-7" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git reset local^<br>git checkout pushed<br>git revert pushed<br></code></pre></td></tr></table></figure>

<h3 id="Git-cherry-pick"><a href="#Git-cherry-pick" class="headerlink" title="Git cherry-pick"></a>Git cherry-pick</h3><h4 id="Q-8"><a href="#Q-8" class="headerlink" title="Q"></a>Q</h4><p>选取其他分支的指定提交记录，合并到<code>main</code>分支</p>
<h4 id="A-8"><a href="#A-8" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git cherry-pick c3 c4 c7<br></code></pre></td></tr></table></figure>

<h3 id="交互式-rebase"><a href="#交互式-rebase" class="headerlink" title="交互式 rebase"></a>交互式 rebase</h3><h4 id="Q-9"><a href="#Q-9" class="headerlink" title="Q"></a>Q</h4><p>当 rebase UI界面打开时, 你能做3件事:</p>
<ul>
<li>调整提交记录的顺序（通过鼠标拖放来完成）</li>
<li>删除你不想要的提交（通过切换 <code>pick</code> 的状态来完成，关闭就意味着你不想要这个提交记录）</li>
<li>合并提交。</li>
</ul>
<p>做一次交互式的 rebase，整理成目标窗口中的提交顺序。</p>
<h4 id="A-9"><a href="#A-9" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git rebase -i overHere<br>调整按题目顺序即可<br></code></pre></td></tr></table></figure>

<h3 id="只取一个提交记录"><a href="#只取一个提交记录" class="headerlink" title="只取一个提交记录"></a>只取一个提交记录</h3><h4 id="Q-10"><a href="#Q-10" class="headerlink" title="Q"></a>Q</h4><p>确保 <code>main</code> 分支能得到 <code>bugFix</code> 分支上的相关提交。</p>
<h4 id="A-10"><a href="#A-10" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git rebase -i main<br>git checkout main<br>git cherry-pick bugFix<br></code></pre></td></tr></table></figure>

<p>or</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git rebase -i main<br>git rebase bugFix main<br></code></pre></td></tr></table></figure>

<h3 id="提交的技巧1"><a href="#提交的技巧1" class="headerlink" title="提交的技巧1"></a>提交的技巧1</h3><h4 id="Q-11"><a href="#Q-11" class="headerlink" title="Q"></a>Q</h4><p>你之前在 <code>newImage</code> 分支上进行了一次提交，然后又基于它创建了 <code>caption</code> 分支，然后又提交了一次。</p>
<p>此时你想对某个以前的提交记录进行一些小小的调整。比如设计师想修改一下 <code>newImage</code> 中图片的分辨率，尽管那个提交记录并不是最新的了。</p>
<p>通过下面的方法来克服困难：</p>
<ul>
<li>先用 <code>git rebase -i</code> 将提交重新排序，然后把我们想要修改的提交记录挪到最前</li>
<li>然后用 <code>git commit --amend</code> 来进行一些小修改</li>
<li>接着再用 <code>git rebase -i</code> 来将他们调回原来的顺序</li>
<li>最后我们把 main 移到修改的最前端（用你自己喜欢的方法），就大功告成啦</li>
</ul>
<h4 id="A-11"><a href="#A-11" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git rebase -i HEAD~2<br>git commit --amend<br>git rebase -i main<br>git rebase caption main<br></code></pre></td></tr></table></figure>

<h3 id="提交的技巧-2"><a href="#提交的技巧-2" class="headerlink" title="提交的技巧 2"></a>提交的技巧 2</h3><h4 id="Q-12"><a href="#Q-12" class="headerlink" title="Q"></a>Q</h4><p>可以使用 <code>rebase -i</code> 对提交记录进行重新排序。只要把我们想要的提交记录挪到最前端，我们就可以很轻松的用 <code>--amend</code> 修改它，然后把它们重新排成我们想要的顺序。</p>
<p>但这样做就唯一的问题就是要进行两次排序，而这有可能造成由 rebase 而导致的冲突。下面还是看看 <code>git cherry-pick</code> 是怎么做的吧。</p>
<p>目标和上一关一样，通过 <code>--amend</code> 改变提交记录 <code>C2</code>，但你不能用 <code>rebase -i</code></p>
<h4 id="A-12"><a href="#A-12" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git checkout main<br>git cherry-pick c2<br>git commit --amend<br>git cherry-pick c3<br></code></pre></td></tr></table></figure>

<h3 id="Git-Tag"><a href="#Git-Tag" class="headerlink" title="Git Tag"></a>Git Tag</h3><h4 id="Q-13"><a href="#Q-13" class="headerlink" title="Q"></a>Q</h4><p>按照目标建立两个标签，然后切换到 <code>v1</code> 上面，要注意你会进到分离 <code>HEAD</code> 的状态 —— 这是因为不能直接在<code>v1</code> 上面做 commit。</p>
<h4 id="A-13"><a href="#A-13" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git checkout c2<br>git tag v0 HEAD^<br>git tag v1<br></code></pre></td></tr></table></figure>

<h3 id="Git-Describe"><a href="#Git-Describe" class="headerlink" title="Git Describe"></a>Git Describe</h3><h4 id="Q-14"><a href="#Q-14" class="headerlink" title="Q"></a>Q</h4><p><code>git describe</code> 的语法是：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git describe &lt;ref&gt;<br></code></pre></td></tr></table></figure>

<p><code>&lt;ref&gt;</code> 可以是任何能被 Git 识别成提交记录的引用，如果你没有指定的话，Git 会使用你目前所在的位置（<code>HEAD</code>）。</p>
<p>它输出的结果是这样的：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">&lt;tag&gt;_&lt;numCommits&gt;_g&lt;hash&gt;<br></code></pre></td></tr></table></figure>

<p><code>tag</code> 表示的是离 <code>ref</code> 最近的标签， <code>numCommits</code> 是表示这个 <code>ref</code> 与 <code>tag</code> 相差有多少个提交记录， <code>hash</code> 表示的是你所给定的 <code>ref</code> 所表示的提交记录哈希值的前几位。</p>
<p>当 <code>ref</code> 提交记录上有某个标签时，则只输出标签名称</p>
<p>试着在这个关卡指定几个位置来感受一下这个命令</p>
<h4 id="A-14"><a href="#A-14" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git describe main<br>git describe side<br>git describe bugFix<br>git commit<br></code></pre></td></tr></table></figure>

<h3 id="多次-Rebase"><a href="#多次-Rebase" class="headerlink" title="多次 Rebase"></a>多次 Rebase</h3><h4 id="Q-15"><a href="#Q-15" class="headerlink" title="Q"></a>Q</h4><p>把所有这些分支上所做的工作都通过 rebase 合并到 main 分支上吧。</p>
<p>但是你的领导给你提了点要求 —— 他们希望得到有序的提交历史，也就是我们最终的结果应该是 <code>C6&#39;</code> 在 <code>C7&#39;</code> 上面， <code>C5&#39;</code> 在 <code>C6&#39;</code> 上面，依此类推。</p>
<h4 id="A-15"><a href="#A-15" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git rebase main bugFix<br>git rebase bugFix side<br>git rebase side another<br>git rebase another main<br></code></pre></td></tr></table></figure>

<h3 id="两个-parent-节点"><a href="#两个-parent-节点" class="headerlink" title="两个 parent 节点"></a>两个 parent 节点</h3><h4 id="Q-16"><a href="#Q-16" class="headerlink" title="Q"></a>Q</h4><p>操作符 <code>^</code> 与 <code>~</code> 符一样，后面也可以跟一个数字。</p>
<p>但是该操作符后面的数字与 <code>~</code> 后面的不同，并不是用来指定向上返回几代，而是指定合并提交记录的某个 parent 提交。还记得前面提到过的一个合并提交有两个 parent 提交吧，所以遇到这样的节点时该选择哪条路径就不是很清晰了。</p>
<p>Git 默认选择合并提交的“第一个” parent 提交，在操作符 <code>^</code> 后跟一个数字可以改变这一默认行为。这些操作符还支持链式操作！</p>
<p>在指定的目标位置创建一个新的分支。</p>
<p>很明显可以简单地直接使用提交记录的哈希值（比如 <code>C6</code>），但我要求你使用刚刚讲到的相对引用修饰符</p>
<h4 id="A-16"><a href="#A-16" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git branch -f bugWork c2<br></code></pre></td></tr></table></figure>

<p>or</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git branch bugWork main^^2^<br></code></pre></td></tr></table></figure>

<h3 id="纠缠不清的分支"><a href="#纠缠不清的分支" class="headerlink" title="纠缠不清的分支"></a>纠缠不清的分支</h3><h4 id="Q-17"><a href="#Q-17" class="headerlink" title="Q"></a>Q</h4><p>现在我们的 <code>main</code> 分支是比 <code>one</code>、<code>two</code> 和 <code>three</code> 要多几个提交。出于某种原因，我们需要把 <code>main</code> 分支上最近的几次提交做不同的调整后，分别添加到各个的分支上。</p>
<p><code>one</code> 需要重新排序并删除 <code>C5</code>，<code>two</code> 仅需要重排排序，而 <code>three</code> 只需要提交一次。</p>
<h4 id="A-17"><a href="#A-17" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git checkout one<br>git cherry-pick c4 c3 c2<br>git checkout two<br>git cherry-pick c5 c4 c3 c2<br>git checkout three<br>git rebase c2 three<br></code></pre></td></tr></table></figure>

<p>or</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git checkout one<br>git cherry-pick c4 c3 c2<br>git checkout two<br>git cherry-pick c5 c4 c3 c2<br>git branch -f three c2<br></code></pre></td></tr></table></figure>

<h3 id="Git-clone"><a href="#Git-clone" class="headerlink" title="Git clone"></a>Git clone</h3><p>从技术上来讲，<code>git clone</code> 命令在真实的环境下的作用是在<strong>本地</strong>创建一个远程仓库的拷贝（比如从 github.com）。 但在该教程中使用这个命令会有一些不同 —— 它会在远程创建一个你本地仓库的副本。显然这和真实命令的意思刚好相反，但是它帮咱们把本地仓库和远程仓库关联到了一起，在教程中就凑合着用吧。</p>
<h4 id="Q-18"><a href="#Q-18" class="headerlink" title="Q"></a>Q</h4><p>克隆仓库</p>
<h4 id="A-18"><a href="#A-18" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git clone<br></code></pre></td></tr></table></figure>

<h3 id="远程分支"><a href="#远程分支" class="headerlink" title="远程分支"></a>远程分支</h3><h4 id="Q-19"><a href="#Q-19" class="headerlink" title="Q"></a>Q</h4><p>在 <code>main</code> 分支上做一次提交；然后切换到 <code>o/main</code>，再做一提交。这有助于你理解远程分支的不同，他们的更新只是反映了远程的状态。</p>
<h4 id="A-19"><a href="#A-19" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git commit<br>git checkout o/main<br>git commit<br></code></pre></td></tr></table></figure>

<h3 id="Git-Fetch"><a href="#Git-Fetch" class="headerlink" title="Git Fetch"></a>Git Fetch</h3><h4 id="Q-20"><a href="#Q-20" class="headerlink" title="Q"></a>Q</h4><p><code>git fetch</code> 完成了仅有的但是很重要的两步:</p>
<ul>
<li>从远程仓库下载本地仓库中缺失的提交记录</li>
<li>更新远程分支指针(如 <code>o/main</code>)</li>
</ul>
<p><code>git fetch</code> 实际上将本地仓库中的远程分支更新成了远程仓库相应分支最新的状态。</p>
<p><code>git fetch</code> 并不会改变你本地仓库的状态。它不会更新你的 <code>main</code> 分支，也不会修改你磁盘上的文件。</p>
<p>本关只需用 <code>git fetch</code> 下载所有的提交！</p>
<h4 id="A-20"><a href="#A-20" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git fetch<br></code></pre></td></tr></table></figure>

<h3 id="Git-Pull"><a href="#Git-Pull" class="headerlink" title="Git Pull"></a>Git Pull</h3><h4 id="Q-21"><a href="#Q-21" class="headerlink" title="Q"></a>Q</h4><p><code>git pull</code> 就是 git fetch 和 git merge 的缩写！接下来会探索一下 <code>git pull</code> 的细节(包括选项和参数)，现在咱们先解决这个关卡。</p>
<p>实际上你完全可以用 <code>fetch</code> 和 <code>merge</code> 通过本关，但是这会增加你的命令数。</p>
<h4 id="A-21"><a href="#A-21" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git pull<br></code></pre></td></tr></table></figure>

<h3 id="模拟团队合作"><a href="#模拟团队合作" class="headerlink" title="模拟团队合作"></a>模拟团队合作</h3><h4 id="Q-22"><a href="#Q-22" class="headerlink" title="Q"></a>Q</h4><p>为了接下来的课程, 我们需要先教你如何制造远程仓库的变更。</p>
<p>这意味着，我们需要“假装”你的同事、朋友、合作伙伴更新了远程仓库，有可能是某个特定的分支，或是几个提交记录。</p>
<p>注意：该网页为了做到这点，引入一个自造命令 <code>git fakeTeamwork</code></p>
<p><code>fakeTeamwork</code> 默认操作就是在远程仓库的 main 分支上做一次提交。可以指定提交的分支或是数量，只需要在命令后加上它们就可以了。</p>
<p>克隆一个远程仓库（用 <code>git clone</code>），再在刚创建的远程仓库中模拟一些修改，然后在你自己的本地分支上做一些提交，再拉取远程仓库的变更。</p>
<h4 id="A-22"><a href="#A-22" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git clone<br>git fakeTeamwork 2<br>git commit<br>git pull<br></code></pre></td></tr></table></figure>

<h3 id="Git-Push"><a href="#Git-Push" class="headerlink" title="Git Push"></a>Git Push</h3><h4 id="Q-23"><a href="#Q-23" class="headerlink" title="Q"></a>Q</h4><p>与 <code>git pull</code> 相反的命令是什么呢？<code>git push</code>！</p>
<p><code>git push</code> 负责将<strong>你的</strong>变更上传到指定的远程仓库，并在远程仓库上合并你的新提交记录。一旦 <code>git push</code> 完成, 你的朋友们就可以从这个远程仓库下载。</p>
<p>要完成本关，需要向远程仓库分享两个提交记录。</p>
<h4 id="A-23"><a href="#A-23" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git commit<br>git commit<br>git push<br></code></pre></td></tr></table></figure>

<h3 id="偏离的提交历史"><a href="#偏离的提交历史" class="headerlink" title="偏离的提交历史"></a>偏离的提交历史</h3><h4 id="Q-24"><a href="#Q-24" class="headerlink" title="Q"></a>Q</h4><p>我们用 <code>git fetch</code> 更新了本地仓库中的远程分支，然后用 rebase 将我们的工作移动到最新的提交记录下，最后再用 <code>git push</code> 推送到远程仓库。还可以使用 <code>merge</code>，尽管 <code>git merge</code> 不会移动你的工作（它会创建新的合并提交），但是它会告诉 Git 你已经合并了远程仓库的所有变更。这是因为远程分支现在是你本地分支的祖先，也就是说你的提交已经包含了远程分支的所有变化。前面已经介绍过 <code>git pull</code> 就是 fetch 和 merge 的简写，类似的 <code>git pull --rebase</code> 就是 fetch 和 rebase 的简写！</p>
<p>要完成本关，你需要完成以下几步：</p>
<ul>
<li>克隆你的仓库</li>
<li>模拟一次远程提交（fakeTeamwork）</li>
<li>完成一次本地提交</li>
<li>用 <em>rebase</em> 发布你的工作</li>
</ul>
<h4 id="A-24"><a href="#A-24" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git clone<br>git fakeTeamwork<br>git commit<br>git pull --rebase<br>git push<br></code></pre></td></tr></table></figure>

<h3 id="锁定的Main-Locked-Main"><a href="#锁定的Main-Locked-Main" class="headerlink" title="锁定的Main(Locked Main)"></a>锁定的Main(Locked Main)</h3><h4 id="Q-25"><a href="#Q-25" class="headerlink" title="Q"></a>Q</h4><p>如果你是在一个大的合作团队中工作, 很可能是main被锁定了, 需要一些Pull Request流程来合并修改。如果你直接提交(commit)到本地main, 然后试图推送(push)修改, 你将会收到这样类似的信息:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">! [远程服务器拒绝] main -&gt; main (TF402455: 不允许推送(push)这个分支; 你必须使用pull request来更新这个分支.)<br></code></pre></td></tr></table></figure>

<p>远程服务器拒绝直接推送(push)提交到main, 因为策略配置要求 pull requests 来提交更新.</p>
<p>你应该按照流程,新建一个分支, 推送(push)这个分支并申请pull request,但是你忘记并直接提交给了main.现在你卡住并且无法推送你的更新.</p>
<p>解决办法：新建一个分支feature, 推送到远程服务器. 然后reset你的main分支和远程服务器保持一致, 否则下次你pull并且他人的提交和你冲突的时候就会有问题.</p>
<h4 id="A-25"><a href="#A-25" class="headerlink" title="A"></a>A</h4><p>注意：该网页中默认的行为是<code>--hard</code>硬重置，但是要记录Git中默认的是<code>--mixed</code></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git reset --hard o/main<br>git checkout -b feature c2<br>git push<br></code></pre></td></tr></table></figure>

<h3 id="推送主分支"><a href="#推送主分支" class="headerlink" title="推送主分支"></a>推送主分支</h3><h4 id="Q-26"><a href="#Q-26" class="headerlink" title="Q"></a>Q</h4><p>在大型项目中开发人员通常会在（从 <code>main</code> 上分出来的）特性分支上工作，工作完成后只做一次集成。这跟前面课程的描述很相像（把 side 分支推送到远程仓库），不过本节我们会深入一些.</p>
<p>但是有些开发人员只在 main 上做 push、pull —— 这样的话 main 总是最新的，始终与远程分支 (o&#x2F;main) 保持一致。</p>
<p>对于接下来这个工作流，我们集成了两个步骤：</p>
<ul>
<li>将特性分支集成到 <code>main</code> 上</li>
<li>推送并更新远程分支</li>
</ul>
<p>快速的更新 <code>main</code> 分支并推送到远程:</p>
<ul>
<li>将我们的工作 rebase 到远程分支的最新提交记录</li>
<li>向远程仓库推送我们的工作</li>
</ul>
<p>这个关卡的 Boss 很厉害 —— 以下是通关提示：</p>
<ul>
<li>这里共有三个特性分支 —— <code>side1</code> <code>side2</code> 和 <code>side3</code></li>
<li>我需要将这三分支按顺序推送到远程仓库</li>
<li>因为远程仓库已经被更新过了，所以我们还要把那些工作合并过来</li>
</ul>
<h4 id="A-26"><a href="#A-26" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git checkout main<br>git pull --rebase<br>git rebase main side1<br>git rebase side1 side2<br>git rebase side2 side3<br>git rebase side3 main<br>git push<br></code></pre></td></tr></table></figure>

<p>or</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git fetch<br>git rebase o/main side1<br>git rebase side1 side2<br>git rebase side2 side3<br>git rebase side3 main<br>git push<br></code></pre></td></tr></table></figure>

<h3 id="合并远程仓库"><a href="#合并远程仓库" class="headerlink" title="合并远程仓库"></a>合并远程仓库</h3><h4 id="Q-27"><a href="#Q-27" class="headerlink" title="Q"></a>Q</h4><p>在开发社区里，有许多关于 merge 与 rebase 的讨论。以下是关于 rebase 的优缺点：</p>
<p>优点:</p>
<ul>
<li>Rebase 使你的提交树变得很干净, 所有的提交都在一条线上</li>
</ul>
<p>缺点:</p>
<ul>
<li>Rebase 修改了提交树的历史</li>
</ul>
<p>比如, 提交 C1 可以被 rebase 到 C3 之后。这看起来 C1 中的工作是在 C3 之后进行的，但实际上是在 C3 之前。</p>
<p>一些开发人员喜欢保留提交历史，因此更偏爱 merge。而其他人可能更喜欢干净的提交树，于是偏爱 rebase。</p>
<p>本关，我们还是解决上一关卡中的问题，但是要用 merge 替换 rebase。这显然有点画蛇添足，但这只是为了更好的说明上面的观点。</p>
<h4 id="A-27"><a href="#A-27" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git checkout main<br>git pull<br>git merge side1<br>git merge side2<br>git merge side3<br>git push<br></code></pre></td></tr></table></figure>

<h3 id="远程跟踪分支"><a href="#远程跟踪分支" class="headerlink" title="远程跟踪分支"></a>远程跟踪分支</h3><h4 id="Q-28"><a href="#Q-28" class="headerlink" title="Q"></a>Q</h4><p>Git 好像知道 <code>main</code> 与 <code>o/main</code> 是相关的。当然这些分支的名字是相似的，可能会让你觉得是依此将远程分支 main 和本地的 main 分支进行了关联。这种关联在以下两种情况下可以清楚地得到展示：</p>
<ul>
<li>pull 操作时, 提交记录会被先下载到 o&#x2F;main 上，之后再合并到本地的 main 分支。隐含的合并目标由这个关联确定的。</li>
<li>push 操作时, 我们把工作从 <code>main</code> 推到远程仓库中的 <code>main</code> 分支(同时会更新远程分支 <code>o/main</code>) 。这个推送的目的地也是由这种关联确定的！</li>
</ul>
<p>直接了当地讲，<code>main</code> 和 <code>o/main</code> 的关联关系就是由分支的“remote tracking”属性决定的。<code>main</code> 被设定为跟踪 <code>o/main</code> —— 这意味着为 <code>main</code> 分支指定了推送的目的地以及拉取后合并的目标。</p>
<p>你可能想知道 <code>main</code> 分支上这个属性是怎么被设定的，你并没有用任何命令指定过这个属性呀！好吧, 当你克隆仓库的时候, Git 就自动帮你把这个属性设置好了。</p>
<p>当你克隆时, Git 会为远程仓库中的每个分支在本地仓库中创建一个远程分支（比如 <code>o/main</code>）。然后再创建一个跟踪远程仓库中活动分支的本地分支，默认情况下这个本地分支会被命名为 <code>main</code>。</p>
<p>克隆完成后，你会得到一个本地分支（如果没有这个本地分支的话，你的目录就是“空白”的），但是可以查看远程仓库中所有的分支（如果你好奇心很强的话）。这样做对于本地仓库和远程仓库来说，都是最佳选择。</p>
<p>这也解释了为什么会在克隆的时候会看到下面的输出：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">local branch &quot;main&quot; set to track remote branch &quot;o/main&quot;<br></code></pre></td></tr></table></figure>

<p>你可以让任意分支跟踪 <code>o/main</code>, 然后该分支会像 <code>main</code> 分支一样得到隐含的 push 目的地以及 merge 的目标。 这意味着你可以在分支 <code>totallyNotMain</code> 上执行 <code>git push</code>，将工作推送到远程仓库的 <code>main</code> 分支上。</p>
<p>有两种方法设置这个属性</p>
<p><strong>第一种方法</strong></p>
<p>第一种就是通过远程分支切换到一个新的分支，执行:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git checkout -b totallyNotMain o/main<br></code></pre></td></tr></table></figure>

<p>就可以创建一个名为 <code>totallyNotMain</code> 的分支，它跟踪远程分支 <code>o/main</code>。</p>
<p><strong>第二种方法</strong></p>
<p>另一种设置远程追踪分支的方法就是使用：<code>git branch -u</code> 命令，执行：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git branch -u o/main foo<br></code></pre></td></tr></table></figure>

<p>这样 <code>foo</code> 就会跟踪 <code>o/main</code> 了。如果当前就在 foo 分支上, 还可以省略 foo：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git branch -u o/main<br></code></pre></td></tr></table></figure>

<p>本节我们在<strong>不</strong>切换到 <code>main</code> 分支的情况下将工作推送到的远程仓库中的 <code>main</code> 分支上。</p>
<h4 id="A-28"><a href="#A-28" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git checkout -b side o/main<br>git commit<br>git pull --rebase<br>git push<br></code></pre></td></tr></table></figure>

<h3 id="Git-push-的参数1"><a href="#Git-push-的参数1" class="headerlink" title="Git push 的参数1"></a>Git push 的参数1</h3><h4 id="Q-29"><a href="#Q-29" class="headerlink" title="Q"></a>Q</h4><p>既然你知道了远程跟踪分支，我们可以开始揭开 git push、fetch 和 pull 的神秘面纱了。我们会逐个介绍这几个命令，它们在理念上是非常相似的。</p>
<p>首先来看 <code>git push</code>。在远程跟踪课程中，你已经学到了 Git 是通过当前所在分支的属性来确定远程仓库以及要 push 的目的地的。这是未指定参数时的行为，我们可以为 push 指定参数，语法是：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git push &lt;remote&gt; &lt;place&gt;<br></code></pre></td></tr></table></figure>

<p><code>&lt;place&gt;</code> 参数是什么意思呢？我们稍后会深入其中的细节, 先看看例子, 这个命令是:</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git push origin main<br></code></pre></td></tr></table></figure>

<p>把这个命令翻译过来就是：</p>
<p><em>切到本地仓库中的“main”分支，获取所有的提交，再到远程仓库“origin”中找到“main”分支，将远程仓库中没有的提交记录都添加上去，搞定之后告诉我。</em></p>
<p>我们通过“place”参数来告诉 Git 提交记录来自于 main, 要推送到远程仓库中的 main。它实际就是要同步的两个仓库的位置。</p>
<p>需要注意的是，因为我们通过指定参数告诉了 Git 所有它需要的信息, 所以它就忽略了我们所切换分支的属性！</p>
<p>本关我们要更新远程仓库中的 <code>foo</code> 和 <code>main</code>, 但是 <code>git checkout</code> 被禁用了！</p>
<p><em>注意：远程分支使用 <code>o/</code> 开头是因为 <code>origin/</code> 对于 UI 来说太长了。不用太在意这个，直接用 <code>origin</code> 作为远程仓库的名称就可以了。</em></p>
<h4 id="A-29"><a href="#A-29" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git push origin main<br>git push origin foo<br></code></pre></td></tr></table></figure>

<h3 id="Git-push-参数-2"><a href="#Git-push-参数-2" class="headerlink" title="Git push 参数 2"></a>Git push 参数 2</h3><h4 id="Q-30"><a href="#Q-30" class="headerlink" title="Q"></a>Q</h4><p>当为 git push 指定 place 参数为 <code>main</code> 时，我们同时指定了提交记录的来源和去向。</p>
<p>你可能想问 —— 如果来源和去向分支的名称不同呢？比如你想把本地的 <code>foo</code> 分支推送到远程仓库中的 <code>bar</code> 分支。</p>
<p>哎，很遗憾 Git 做不到…… 开个玩笑，别当真！当然是可以的啦 :) Git 拥有超强的灵活性（有点过于灵活了）</p>
<p>要同时为源和目的地指定 <code>&lt;place&gt;</code> 的话，只需要用冒号 <code>:</code> 将二者连起来就可以了：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git push origin &lt;source&gt;:&lt;destination&gt;<br></code></pre></td></tr></table></figure>

<p>这个参数实际的值是个 refspec，“refspec” 是一个自造的词，意思是 Git 能识别的位置（比如分支 <code>foo</code> 或者 <code>HEAD~1</code>）</p>
<p>一旦你指定了独立的来源和目的地，就可以组织出言简意赅的远程操作命令.</p>
<p>在这个关卡中，试着完成目标窗口展示的提交树，记住参数格式哟：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">&lt;source&gt;:&lt;destination&gt;<br></code></pre></td></tr></table></figure>

<h4 id="A-30"><a href="#A-30" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git push origin foo:main<br>git push origin main^:foo<br></code></pre></td></tr></table></figure>

<h3 id="Git-fetch-的参数"><a href="#Git-fetch-的参数" class="headerlink" title="Git fetch 的参数"></a>Git fetch 的参数</h3><h4 id="Q-31"><a href="#Q-31" class="headerlink" title="Q"></a>Q</h4><p><code>git fetch</code> 的参数和 <code>git push</code> 极其相似。他们的概念是相同的，只是方向相反罢了（因为现在你是下载，而非上传）</p>
<p>如果你像如下命令这样为 git fetch 设置 的话：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git fetch origin foo<br></code></pre></td></tr></table></figure>

<p>Git 会到远程仓库的 <code>foo</code> 分支上，然后获取所有本地不存在的提交，放到本地的 <code>o/foo</code> 上。</p>
<p>为何 Git 会将新提交放到 <code>o/foo</code> 而不是放到我本地的 foo 分支呢？之前不是说这样的 参数就是同时应用于本地和远程的位置吗？</p>
<p>好吧, 本例中 Git 做了一些特殊处理，因为你可能在 foo 分支上的工作还未完成，你也不想弄乱它。还记得在 <code>git fetch</code> 课程里我们讲到的吗 —— 它不会更新你的本地的非远程分支, 只是下载提交记录（这样, 你就可以对远程分支进行检查或者合并了）。</p>
<p>如果你觉得直接更新本地分支很爽，那你就用冒号分隔的 refspec 吧。不过，你不能在当前切换的分支上干这个事，但是其它分支是可以的。</p>
<p>这里有一点是需要注意的 —— <code>source</code> 现在指的是远程仓库中的位置，而 <code>&lt;destination&gt;</code> 才是要放置提交的本地仓库的位置。它与 git push 刚好相反，这是可以讲的通的，因为我们在往相反的方向传送数据。</p>
<p>理论上虽然行的通，但开发人员很少这么做。我在这里介绍它主要是为了从概念上说明 <code>fetch</code> 和 <code>push</code> 的相似性，只是方向相反罢了。</p>
<p>要完成本关，抓取目标窗口中指定的提交记录，使用这些魔幻的命令吧！</p>
<p>使用 fetch 时, 你必须指定 source 和 destination。 注意一下目标窗口, 因为提交对象的 ID 可能会变哦!</p>
<h4 id="A-31"><a href="#A-31" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git fetch origin foo:main<br>git fetch origin main^:foo<br>git checkout foo <br>git merge main<br></code></pre></td></tr></table></figure>

<h3 id="没有-source-的-source"><a href="#没有-source-的-source" class="headerlink" title="没有 source 的 source"></a>没有 source 的 source</h3><h4 id="Q-32"><a href="#Q-32" class="headerlink" title="Q"></a>Q</h4><p>Git 有两种关于 <code>&lt;source&gt;</code> 的用法是比较诡异的，即你可以在 git push 或 git fetch 时不指定任何 <code>source</code>，方法就是仅保留冒号和 destination 部分，source 部分留空。</p>
<ul>
<li><code>git push origin :side</code></li>
<li><code>git fetch origin :bugFix</code></li>
</ul>
<p>我们分别来看一下这两条命令的作用</p>
<p>通过给 push 传空值 source，成功删除了远程仓库中的 <code>foo</code> 分支</p>
<p> fetch 空 到本地，会在本地创建一个新分支。</p>
<p>这个关卡很容易 —— 只要删除一个远程的分支, 再用 <code>git fetch</code> 在本地创建一个新分支就可以了！</p>
<h4 id="A-32"><a href="#A-32" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git push origin :foo<br>git fetch origin :bar<br></code></pre></td></tr></table></figure>

<h3 id="Git-pull-的参数"><a href="#Git-pull-的参数" class="headerlink" title="Git pull 的参数"></a>Git pull 的参数</h3><h4 id="Q-33"><a href="#Q-33" class="headerlink" title="Q"></a>Q</h4><p> git pull 到头来就是 fetch 后跟 merge 的缩写。你可以理解为用同样的参数执行 git fetch，然后再 merge 你所抓取到的提交记录。</p>
<p>以下命令在 Git 中是等效的:</p>
<p><code>git pull origin foo</code> 相当于：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git fetch origin foo; git merge o/foo<br></code></pre></td></tr></table></figure>

<p>还有…</p>
<p><code>git pull origin bar~1:bugFix</code> 相当于：</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git fetch origin bar~1:bugFix; git merge bugFix<br></code></pre></td></tr></table></figure>

<p>看到了? git pull 实际上就是 fetch + merge 的缩写, git pull 唯一关注的是提交最终合并到哪里（也就是为 git fetch 所提供的 destination 参数）</p>
<p>请按照目标窗口中的状态进行操作。你需要下载一些提交，然后创建一些新分支，再合并这些分支到其它分支, 但这用不了几个命令</p>
<h4 id="A-33"><a href="#A-33" class="headerlink" title="A"></a>A</h4><figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git fetch origin bar:foo<br>git fetch origin main:side<br>git merge foo<br>git merge side<br></code></pre></td></tr></table></figure>

<p>or</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">git pull origin bar:foo<br>git pull origin main:side<br></code></pre></td></tr></table></figure>

<h3 id="Wow！你通关了！-ﾉ-ﾉ-ﾉ-ﾉ-ﾉ-ﾉ"><a href="#Wow！你通关了！-ﾉ-ﾉ-ﾉ-ﾉ-ﾉ-ﾉ" class="headerlink" title="Wow！你通关了！ (ﾉ^_^)ﾉ (ﾉ^_^)ﾉ (ﾉ^_^)ﾉ"></a><strong>Wow！你通关了！ (ﾉ^_^)ﾉ (ﾉ^_^)ﾉ (ﾉ^_^)ﾉ</strong></h3><p>本来以为两个小时左右应该就可以搞定，但没想到不止。不过这么一玩，也知道了Git大致的用法啦！</p>
]]></content>
      <tags>
        <tag>git</tag>
      </tags>
  </entry>
  <entry>
    <title>算法竞赛知识总结</title>
    <url>/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/</url>
    <content><![CDATA[<h1 id="算法基础"><a href="#算法基础" class="headerlink" title="算法基础"></a>算法基础</h1><p>算法学习的记录，以c++为示例</p>
<p>模板也是一步一步迭代更新</p>
<p>板子也不是我自己原创的，应该站在巨人的肩膀上，因此，网络上我习惯用的板子也收集</p>
<p>我觉得比较好的内容都收集起来</p>
<h2 id="时空复杂度分析"><a href="#时空复杂度分析" class="headerlink" title="时空复杂度分析"></a>时空复杂度分析</h2><p>理论基础，做题的时候首先观察数据范围，确定解决方案的大致时间复杂度，再思考用什么数据结构，能用什么算法。</p>
<h3 id="时间复杂度"><a href="#时间复杂度" class="headerlink" title="时间复杂度"></a><strong>时间复杂度</strong></h3><p>大O,算法导论给出的解释：<strong>大O用来表示上界的</strong>，当用它作为算法的最坏情况运行时间的上界，就是对任意数据输入的运行时间的上界。</p>
<p><strong>整体测试数据整理如下：</strong></p>
<p><img src="https://code-thinking-1253855093.file.myqcloud.com/pics/20201208231559175-20230310124325152.png" alt="程序超时1"></p>
<p>其中，递归算法的时间复杂度本质上是要看: <strong>递归的次数 * 每次递归中的操作次数</strong>。</p>
<h3 id="空间复杂度"><a href="#空间复杂度" class="headerlink" title="空间复杂度"></a>空间复杂度</h3><p><strong>空间复杂度是考虑程序运行时占用内存的大小，而不是可执行文件的大小。</strong></p>
<p>其中，<strong>递归算法的空间复杂度 &#x3D; 每次递归的空间复杂度 * 递归深度</strong></p>
<h2 id="二分"><a href="#二分" class="headerlink" title="二分"></a>二分</h2><p>二分法经常写乱，主要是因为<strong>对区间的定义没有想清楚，区间的定义就是不变量</strong>。要在二分查找的过程中，保持不变量，就是在while寻找中每一次边界的处理都要坚持根据区间的定义来操作，这就是<strong>循环不变量</strong>规则。</p>
<p>写二分法，区间的定义一般为两种，左闭右闭即[left, right]，或者左闭右开即[left, right)。</p>
<p><strong>二分法第一种写法</strong></p>
<p>第一种写法，我们定义 target 是在一个在左闭右闭的区间里，<strong>也就是[left, right] （这个很重要非常重要）</strong>。</p>
<p>区间的定义这就决定了二分法的代码应该如何写，<strong>因为定义target在[left, right]区间，所以有如下两点：</strong></p>
<ul>
<li>while (left &lt;&#x3D; right) 要使用 &lt;&#x3D; ，因为left &#x3D;&#x3D; right是有意义的，所以使用 &lt;&#x3D;</li>
<li>if (nums[middle] &gt; target) right 要赋值为 middle - 1，因为当前这个nums[middle]一定不是target，那么接下来要查找的左区间结束下标位置就是 middle - 1</li>
</ul>
<p>例如在数组：1,2,3,4,7,9,10中查找元素2，如图所示：</p>
<p><img src="https://code-thinking-1253855093.file.myqcloud.com/pics/20210311153055723.jpg" alt="704.二分查找"></p>
<p>代码如下：（详细注释）</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">// 版本一</span><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">search</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, <span class="hljs-type">int</span> target)</span> </span>&#123;<br>        <span class="hljs-type">int</span> left = <span class="hljs-number">0</span>;<br>        <span class="hljs-type">int</span> right = nums.<span class="hljs-built_in">size</span>() - <span class="hljs-number">1</span>; <span class="hljs-comment">// 定义target在左闭右闭的区间里，[left, right]</span><br>        <span class="hljs-keyword">while</span> (left &lt;= right) &#123; <span class="hljs-comment">// 当left==right，区间[left, right]依然有效，所以用 &lt;=</span><br>            <span class="hljs-type">int</span> middle = left + ((right - left) / <span class="hljs-number">2</span>);<span class="hljs-comment">// 防止溢出 等同于(left + right)/2</span><br>            <span class="hljs-keyword">if</span> (nums[middle] &gt; target) &#123;<br>                right = middle - <span class="hljs-number">1</span>; <span class="hljs-comment">// target 在左区间，所以[left, middle - 1]</span><br>            &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nums[middle] &lt; target) &#123;<br>                left = middle + <span class="hljs-number">1</span>; <span class="hljs-comment">// target 在右区间，所以[middle + 1, right]</span><br>            &#125; <span class="hljs-keyword">else</span> &#123; <span class="hljs-comment">// nums[middle] == target</span><br>                <span class="hljs-keyword">return</span> middle; <span class="hljs-comment">// 数组中找到目标值，直接返回下标</span><br>            &#125;<br>        &#125;<br>        <span class="hljs-comment">// 未找到目标值</span><br>        <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>

<ul>
<li>时间复杂度：O(log n)</li>
<li>空间复杂度：O(1)</li>
</ul>
<p><strong>二分法第二种写法</strong></p>
<p>如果说定义 target 是在一个在左闭右开的区间里，也就是[left, right) ，那么二分法的边界处理方式则截然不同。</p>
<p>有如下两点：</p>
<ul>
<li>while (left &lt; right)，这里使用 &lt; ,因为left &#x3D;&#x3D; right在区间[left, right)是没有意义的</li>
<li>if (nums[middle] &gt; target) right 更新为 middle，因为当前nums[middle]不等于target，去左区间继续寻找，而寻找区间是左闭右开区间，所以right更新为middle，即：下一个查询区间不会去比较nums[middle]</li>
</ul>
<p>在数组：1,2,3,4,7,9,10中查找元素2，如图所示：（<strong>注意和方法一的区别</strong>）</p>
<p><img src="https://code-thinking-1253855093.file.myqcloud.com/pics/20210311153123632.jpg" alt="704.二分查找1"></p>
<p>代码如下：（详细注释）</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">// 版本二</span><br><span class="hljs-keyword">class</span> <span class="hljs-title class_">Solution</span> &#123;<br><span class="hljs-keyword">public</span>:<br>    <span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">search</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt;&amp; nums, <span class="hljs-type">int</span> target)</span> </span>&#123;<br>        <span class="hljs-type">int</span> left = <span class="hljs-number">0</span>;<br>        <span class="hljs-type">int</span> right = nums.<span class="hljs-built_in">size</span>(); <span class="hljs-comment">// 定义target在左闭右开的区间里，即：[left, right)</span><br>        <span class="hljs-keyword">while</span> (left &lt; right) &#123; <span class="hljs-comment">// 因为left == right的时候，在[left, right)是无效的空间，所以使用 &lt;</span><br>            <span class="hljs-type">int</span> middle = left + ((right - left) &gt;&gt; <span class="hljs-number">1</span>);<br>            <span class="hljs-keyword">if</span> (nums[middle] &gt; target) &#123;<br>                right = middle; <span class="hljs-comment">// target 在左区间，在[left, middle)中</span><br>            &#125; <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span> (nums[middle] &lt; target) &#123;<br>                left = middle + <span class="hljs-number">1</span>; <span class="hljs-comment">// target 在右区间，在[middle + 1, right)中</span><br>            &#125; <span class="hljs-keyword">else</span> &#123; <span class="hljs-comment">// nums[middle] == target</span><br>                <span class="hljs-keyword">return</span> middle; <span class="hljs-comment">// 数组中找到目标值，直接返回下标</span><br>            &#125;<br>        &#125;<br>        <span class="hljs-comment">// 未找到目标值</span><br>        <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<br>    &#125;<br>&#125;;<br></code></pre></td></tr></table></figure>

<ul>
<li>时间复杂度：O(log n)</li>
<li>空间复杂度：O(1)</li>
</ul>
<h3 id="二分查找"><a href="#二分查找" class="headerlink" title="二分查找"></a>二分查找</h3><p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\a060be287b82a5db42c67884310c20c.png"></p>
<h4 id="整数二分"><a href="#整数二分" class="headerlink" title="整数二分"></a>整数二分</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">check</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<span class="hljs-comment">//检查x是否满足某种性质</span><br>    <span class="hljs-comment">/*...*/</span><br>&#125;<br><br><span class="hljs-comment">//区间[l,r]划分成[l,mid]和[mid+1,r]时</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">bsearch_1</span><span class="hljs-params">(<span class="hljs-type">int</span> l,<span class="hljs-type">int</span> r)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">while</span>(l&lt;r)<br>    &#123;<br>        <span class="hljs-type">int</span> mid=l+r&gt;&gt;<span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">if</span>(<span class="hljs-built_in">check</span>(mid)) r=mid;<br>        <span class="hljs-keyword">else</span> l=mid+<span class="hljs-number">1</span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> l;<br>&#125;<br><span class="hljs-comment">//区间[l,r]划分为[l,mid-1]和[mid,r]时</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">bsearch_2</span><span class="hljs-params">(<span class="hljs-type">int</span> l,<span class="hljs-type">int</span> r)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">while</span>(l&lt;r)<br>    &#123;<br>        <span class="hljs-type">int</span> mid=l+r&gt;&gt;<span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">if</span>(<span class="hljs-built_in">check</span>(mid)) l=mid;<br>        <span class="hljs-keyword">else</span> r=mid<span class="hljs-number">-1</span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> l;<br>&#125;<br></code></pre></td></tr></table></figure>

<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//相较于上一个板子，我更喜欢这个板子</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">find_max</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt; num,<span class="hljs-type">int</span> q)</span></span><br><span class="hljs-function"></span>&#123;<span class="hljs-comment">//最大化查找</span><br>    <span class="hljs-type">int</span> l=<span class="hljs-number">0</span>,r=num.<span class="hljs-built_in">size</span>();<br>    <span class="hljs-keyword">while</span>(l+<span class="hljs-number">1</span>&lt;r)<br>    &#123;<br>        <span class="hljs-type">int</span> mid=l+r&gt;&gt;<span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">if</span>(num[mid]&lt;=q)<br>        &#123;l=mid;&#125;<br>        <span class="hljs-keyword">else</span><br>        &#123;r=mid;&#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> l;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">find_min</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt; num,<span class="hljs-type">int</span> q)</span></span><br><span class="hljs-function"></span>&#123;<span class="hljs-comment">//最小化查找</span><br>    <span class="hljs-type">int</span> l=<span class="hljs-number">0</span>,r=num.<span class="hljs-built_in">size</span>();<br>    <span class="hljs-keyword">while</span>(l+<span class="hljs-number">1</span>&lt;r)<br>    &#123;<br>        <span class="hljs-type">int</span> mid=l+r&gt;&gt;<span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">if</span>(num[mid]&gt;=q)<br>        &#123;r=mid;&#125;<br>        <span class="hljs-keyword">else</span><br>        &#123;l=mid;&#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> r;<br>&#125;<br></code></pre></td></tr></table></figure>



<h4 id="浮点数二分"><a href="#浮点数二分" class="headerlink" title="浮点数二分"></a>浮点数二分</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">check</span><span class="hljs-params">(<span class="hljs-type">double</span> x)</span></span><br><span class="hljs-function"></span>&#123;<span class="hljs-comment">//检查x是否满足某种性质</span><br>    <span class="hljs-comment">/*...*/</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">double</span> <span class="hljs-title">bsearch_3</span><span class="hljs-params">(<span class="hljs-type">double</span> l,<span class="hljs-type">double</span> r)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">const</span> <span class="hljs-type">double</span> eps=<span class="hljs-number">1e-6</span>;<span class="hljs-comment">//eps表示精度，取决于题目对精度的要求</span><br>    <span class="hljs-keyword">while</span>(r-l&gt;eps)<br>    &#123;<br>        <span class="hljs-type">double</span> mid =(l+r)/<span class="hljs-number">2</span>;<br>        <span class="hljs-keyword">if</span>(<span class="hljs-built_in">check</span>(mid)) r=mid;<br>        <span class="hljs-keyword">else</span> l=mid;<br>    &#125;<br>    <span class="hljs-keyword">return</span> l;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="二分答案"><a href="#二分答案" class="headerlink" title="二分答案"></a>二分答案</h3><p>其实，二分答案就是二分查找加上check,关键在于check函数的题目要求，来找到答案</p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\a5d90511246deea32f98148b9f0562e.png"></p>
<h2 id="STL"><a href="#STL" class="headerlink" title="STL"></a>STL</h2><p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330111437526.png"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330111728952.png"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330111818379.png"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330111852559.png"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330111944213.png"></p>
<h2 id="常用库函数"><a href="#常用库函数" class="headerlink" title="常用库函数"></a>常用库函数</h2><h2 id="排序"><a href="#排序" class="headerlink" title="排序"></a>排序</h2><h3 id="常用排序"><a href="#常用排序" class="headerlink" title="常用排序"></a>常用排序</h3><p><strong>利用stl的sort()函数</strong></p>
<p>sort() 函数有 2 种用法，其语法格式分别为：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//对 [first, last) 区域内的元素做默认的升序排序</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">sort</span> <span class="hljs-params">(RandomAccessIterator first, RandomAccessIterator last)</span></span>;<br><span class="hljs-comment">//按照指定的 comp 排序规则，对 [first, last) 区域内的元素进行排序</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">sort</span> <span class="hljs-params">(RandomAccessIterator first, RandomAccessIterator last, Compare comp)</span></span>;<br></code></pre></td></tr></table></figure>

<p>其中，first 和 last 都为随机访问迭代器，它们的组合 [first, last) 用来指定要排序的目标区域；另外在第 2 种格式中，comp 可以是 C++ STL 标准库提供的排序规则（比如 std::greater），也可以是自定义的排序规则。</p>
<h4 id="使用sort对数组排序"><a href="#使用sort对数组排序" class="headerlink" title="使用sort对数组排序"></a>使用sort对数组排序</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-keyword">include</span><span class="hljs-string">&lt;algorithm&gt;</span></span><br><br>vector&lt;<span class="hljs-type">int</span>&gt; v;<br><span class="hljs-built_in">sort</span>(v.<span class="hljs-built_in">begin</span>(),v.<span class="hljs-built_in">end</span>());<br><br><span class="hljs-type">int</span> a[N];<br><span class="hljs-built_in">sort</span>(a,a+n);<br><br></code></pre></td></tr></table></figure>

<p><strong>例子</strong></p>
<p>使用 STL 标准库提供的排序规则</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span>&#123;<br>    <span class="hljs-type">int</span> arr[] = &#123;<span class="hljs-number">2</span>,<span class="hljs-number">6</span>,<span class="hljs-number">3</span>,<span class="hljs-number">5</span>,<span class="hljs-number">4</span>,<span class="hljs-number">8</span>,<span class="hljs-number">1</span>,<span class="hljs-number">0</span>,<span class="hljs-number">9</span>,<span class="hljs-number">10</span>&#125;;<br>    <span class="hljs-built_in">sort</span>(arr, arr+<span class="hljs-number">10</span>, std::<span class="hljs-built_in">greater</span>&lt;<span class="hljs-type">int</span>&gt;());<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>;i &lt; <span class="hljs-number">10</span>;i++)<br>        cout &lt;&lt; arr[i] &lt;&lt; <span class="hljs-string">&quot; &quot;</span>;<br>    cout &lt;&lt; endl;<br>    <span class="hljs-built_in">sort</span>(arr, arr+<span class="hljs-number">10</span>, std::<span class="hljs-built_in">less</span>&lt;<span class="hljs-type">int</span>&gt;());<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>;i &lt; <span class="hljs-number">10</span>;i++)<br>        cout &lt;&lt; arr[i] &lt;&lt; <span class="hljs-string">&quot; &quot;</span>;<br>&#125;<br><span class="hljs-comment">// out</span><br><span class="hljs-comment">/*</span><br><span class="hljs-comment">10 9 8 6 5 4 3 2 1 0</span><br><span class="hljs-comment">0 1 2 3 4 5 6 8 9 10</span><br><span class="hljs-comment">*/</span><br></code></pre></td></tr></table></figure>

<p>使用自定义比较器</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">cmp</span><span class="hljs-params">(<span class="hljs-type">const</span> <span class="hljs-type">int</span> a, <span class="hljs-type">const</span> <span class="hljs-type">int</span> b)</span></span>&#123;<br>    <span class="hljs-keyword">return</span> a &lt; b;<br>&#125;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span>&#123;<br>    <span class="hljs-type">int</span> arr[] = &#123;<span class="hljs-number">2</span>,<span class="hljs-number">6</span>,<span class="hljs-number">3</span>,<span class="hljs-number">5</span>,<span class="hljs-number">4</span>,<span class="hljs-number">8</span>,<span class="hljs-number">1</span>,<span class="hljs-number">0</span>,<span class="hljs-number">9</span>,<span class="hljs-number">10</span>&#125;;<br>    <span class="hljs-built_in">sort</span>(arr, arr+<span class="hljs-number">10</span>, cmp);<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>;i &lt; <span class="hljs-number">10</span>;i++)<br>        cout &lt;&lt; arr[i] &lt;&lt; <span class="hljs-string">&quot; &quot;</span>;<br>&#125;<br><span class="hljs-comment">// out</span><br><span class="hljs-comment">/*</span><br><span class="hljs-comment">0 1 2 3 4 5 6 8 9 10</span><br><span class="hljs-comment">*/</span><br></code></pre></td></tr></table></figure>

<p>使用 lambda 表达式自定义比较器</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span>&#123;<br>    <span class="hljs-type">int</span> arr[] = &#123;<span class="hljs-number">2</span>,<span class="hljs-number">6</span>,<span class="hljs-number">3</span>,<span class="hljs-number">5</span>,<span class="hljs-number">4</span>,<span class="hljs-number">8</span>,<span class="hljs-number">1</span>,<span class="hljs-number">0</span>,<span class="hljs-number">9</span>,<span class="hljs-number">10</span>&#125;;<br>    <span class="hljs-built_in">sort</span>(arr, arr+<span class="hljs-number">10</span>, [](<span class="hljs-type">const</span> <span class="hljs-type">int</span> a, <span class="hljs-type">const</span> <span class="hljs-type">int</span> b)&#123;<br>         <span class="hljs-keyword">return</span> a &lt; b;<br>         &#125;);<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>;i &lt; <span class="hljs-number">10</span>;i++)<br>        cout &lt;&lt; arr[i] &lt;&lt; <span class="hljs-string">&quot; &quot;</span>;<br>&#125;<br><span class="hljs-comment">// out</span><br><span class="hljs-comment">/*</span><br><span class="hljs-comment">0 1 2 3 4 5 6 8 9 10</span><br><span class="hljs-comment">*/</span><br></code></pre></td></tr></table></figure>

<h4 id="使用sort对map排序"><a href="#使用sort对map排序" class="headerlink" title="使用sort对map排序"></a>使用sort对map排序</h4><p>map是用来存放&lt;key, value&gt;键值对的数据结构，可以很方便快速的根据key查到相应的value，map本身的实现方式内含了比较器的设置，只要我们在map初始化的时候传入比较器，即可完成对应的排序。</p>
<p>定义包含水果及其个数的map，按照水果名称字典序进行排序 （按key排序）</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-meta">#<span class="hljs-keyword">include</span><span class="hljs-string">&lt;map&gt;</span></span><br><br><span class="hljs-keyword">using</span> <span class="hljs-keyword">namespace</span> std;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span>&#123;<br>    map&lt;string, <span class="hljs-type">int</span>, less&lt;string&gt;&gt; msi;<br>    msi[<span class="hljs-string">&quot;apple&quot;</span>] = <span class="hljs-number">5</span>;<br>    msi[<span class="hljs-string">&quot;watermelon&quot;</span>] = <span class="hljs-number">2</span>;<br>    msi[<span class="hljs-string">&quot;pear&quot;</span>] = <span class="hljs-number">3</span>;<br>    msi[<span class="hljs-string">&quot;peach&quot;</span>] = <span class="hljs-number">6</span>;<br>    msi[<span class="hljs-string">&quot;cherry&quot;</span>] = <span class="hljs-number">10</span>;<br><br>    <span class="hljs-keyword">for</span>(<span class="hljs-keyword">auto</span> item: msi)<br>        cout &lt;&lt; item.first &lt;&lt; <span class="hljs-string">&quot; &quot;</span> &lt;&lt; item.second &lt;&lt; endl;<br><br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br><span class="hljs-comment">// out</span><br><span class="hljs-comment">/*</span><br><span class="hljs-comment">apple 5</span><br><span class="hljs-comment">cherry 10</span><br><span class="hljs-comment">peach 6</span><br><span class="hljs-comment">pear 3</span><br><span class="hljs-comment">watermelon 2</span><br><span class="hljs-comment">*/</span><br></code></pre></td></tr></table></figure>

<p>定义包含水果及其个数的map，按照水果个数进行排序，当水果个数相同时，按照水果名称字典序排序 （将map转为vector进行排序）</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">cmp</span><span class="hljs-params">(<span class="hljs-type">const</span> pair&lt;string, <span class="hljs-type">int</span>&gt;&amp; a, <span class="hljs-type">const</span> pair&lt;string, <span class="hljs-type">int</span>&gt;&amp; b)</span></span>&#123;<br>    <span class="hljs-keyword">if</span>(a.second &lt; b.second) <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(a.second == b.second) <span class="hljs-keyword">return</span> a.first &lt; b.first;<br>    <span class="hljs-keyword">else</span> <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>&#125;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span>&#123;<br>    map&lt;string, <span class="hljs-type">int</span>&gt; msi;<br>    msi[<span class="hljs-string">&quot;apple&quot;</span>] = <span class="hljs-number">5</span>;<br>    msi[<span class="hljs-string">&quot;watermelon&quot;</span>] = <span class="hljs-number">2</span>;<br>    msi[<span class="hljs-string">&quot;pear&quot;</span>] = <span class="hljs-number">3</span>;<br>    msi[<span class="hljs-string">&quot;peach&quot;</span>] = <span class="hljs-number">5</span>;<br>    msi[<span class="hljs-string">&quot;cherry&quot;</span>] = <span class="hljs-number">10</span>;<br><br>    vector&lt;pair&lt;string, <span class="hljs-type">int</span>&gt;&gt; <span class="hljs-built_in">vpi</span>(msi.<span class="hljs-built_in">begin</span>(), msi.<span class="hljs-built_in">end</span>());<br>    <span class="hljs-built_in">sort</span>(vpi.<span class="hljs-built_in">begin</span>(), vpi.<span class="hljs-built_in">end</span>(), cmp);<br><br>    <span class="hljs-keyword">for</span>(<span class="hljs-keyword">auto</span> item: vpi)&#123;<br>        cout &lt;&lt; item.first &lt;&lt; <span class="hljs-string">&quot; &quot;</span> &lt;&lt; item.second &lt;&lt; endl;<br>    &#125;<br><br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br><span class="hljs-comment">// out</span><br><span class="hljs-comment">/*</span><br><span class="hljs-comment">watermelon 2</span><br><span class="hljs-comment">pear 3</span><br><span class="hljs-comment">apple 5</span><br><span class="hljs-comment">peach 5</span><br><span class="hljs-comment">cherry 10</span><br><span class="hljs-comment">*/</span><br></code></pre></td></tr></table></figure>

<h4 id="O-nlogn"><a href="#O-nlogn" class="headerlink" title="O(nlogn)"></a>O(nlogn)</h4><h5 id="快速排序"><a href="#快速排序" class="headerlink" title="快速排序"></a>快速排序</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">quick_sort</span><span class="hljs-params">(<span class="hljs-type">int</span> q[],<span class="hljs-type">int</span> l,<span class="hljs-type">int</span> r)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span>(l&gt;=r) <span class="hljs-keyword">return</span>;<br>    <br>    <span class="hljs-type">int</span> i=l<span class="hljs-number">-1</span>,j=r+<span class="hljs-number">1</span>,x=q[l+r&gt;&gt;<span class="hljs-number">1</span>];<br>    <span class="hljs-keyword">while</span>(i&lt;j)<br>    &#123;<br>        <span class="hljs-keyword">do</span> i++;<span class="hljs-keyword">while</span>(q[i]&lt;x);<br>        <span class="hljs-keyword">do</span> j--;<span class="hljs-keyword">while</span>(q[j]&gt;x);<br>        <span class="hljs-keyword">if</span>(i&lt;j) <span class="hljs-built_in">swap</span>(q[i],q[j]);<br>    &#125;<br>    <span class="hljs-built_in">quick_sort</span>(q,l,j),<span class="hljs-built_in">quick_sort</span>(q,j+<span class="hljs-number">1</span>,r);<br>&#125;<br></code></pre></td></tr></table></figure>

<h5 id="归并排序"><a href="#归并排序" class="headerlink" title="归并排序"></a>归并排序</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">merge_sort</span><span class="hljs-params">(<span class="hljs-type">int</span> q[],<span class="hljs-type">int</span> l,<span class="hljs-type">int</span> r)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span>(l&gt;=r) <span class="hljs-keyword">return</span>;<br>    <br>    <span class="hljs-type">int</span> mid = l+r&gt;&gt;<span class="hljs-number">1</span>;<br>    <span class="hljs-built_in">merge_sort</span>(q,l,mid);<br>    <span class="hljs-built_in">merge_sort</span>(q,mid+<span class="hljs-number">1</span>,r);<br>    <br>    <span class="hljs-type">int</span> k=<span class="hljs-number">0</span>,i=l,j=mid+<span class="hljs-number">1</span>;<br>    <span class="hljs-keyword">while</span>(i&lt;=mid&amp;&amp;j&lt;=r)<br>    &#123;<br>        <span class="hljs-keyword">if</span>(q[i]&lt;q[j]) <br>        &#123;<br>            tmp[k++]=q[i++];<br>        &#125;<br>        <span class="hljs-keyword">else</span><br>        &#123;<br>            tmp[k++]=q[j++];<br>        &#125;<br>        <br>    &#125;<br>    <br>    <span class="hljs-keyword">while</span>(i&lt;=mid) tmp[k++]=q[i++];<br>    <span class="hljs-keyword">while</span>(j&lt;=r) tmp[k++]=q[j++];<br>    <br>    <span class="hljs-keyword">for</span>(i=l,j=<span class="hljs-number">0</span>;i&lt;=r;i++,j++) q[i]=tmp[j];<br>&#125;<br></code></pre></td></tr></table></figure>



<h3 id="基于比较的排序-8个"><a href="#基于比较的排序-8个" class="headerlink" title="基于比较的排序(8个)"></a>基于比较的排序(8个)</h3><p>所有的不稳定都可以转化为稳定(按照数值顺序为首位,下标顺序为次位)</p>
<h4 id="1-直接插入排序"><a href="#1-直接插入排序" class="headerlink" title="1.直接插入排序"></a>1.直接插入排序</h4><p><strong>做法:相当于每次把元素插入有序位置</strong><br><strong>时间复杂度</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1.最好情况:整个数组都有序，只执行外层循环，O(n)<br>2.平均情况:O(n^2)<br>3.最坏情况:O(n^2)<br></code></pre></td></tr></table></figure>

<p><strong>辅助空间复杂度:O(1)</strong></p>
<p><strong>稳定</strong></p>
<p><strong>代码:</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">insert_sort</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>        <span class="hljs-comment">//由于是从头往后做，前面做过的都是有序的，只需要找到合适位置，将后面整体往后移一位，在有序位置插入t</span><br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;n;i++) <span class="hljs-comment">//q[0]只有一个数，有序</span><br>        &#123;<br>                <span class="hljs-type">int</span> t=q[i],j=i;<br><br>                <span class="hljs-keyword">while</span>(j&amp;&amp;q[j<span class="hljs-number">-1</span>]&gt;t)<br>                &#123;<br>                        q[j]=q[j<span class="hljs-number">-1</span>];<br>                        j--;<br>                &#125;<br>                q[j]=t;<br>        &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="2-折半插入排序"><a href="#2-折半插入排序" class="headerlink" title="2.折半插入排序"></a>2.折半插入排序</h4><p><strong>直接插入排序的不明显优化：用二分找到第一个&gt;t的位置,优化了比较次数(O(n)-&gt;O(logn))，但是没有优化移动次数</strong><br><strong>时间复杂度</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1.最好情况:整个数组都有序，只执行外层循环，里面都continue，O(n)<br>2.平均情况:O(n^2)<br>3.最坏情况:O(n^2)<br></code></pre></td></tr></table></figure>

<p><strong>辅助空间复杂度:O(1)</strong></p>
<p><strong>稳定</strong></p>
<p><strong>代码:</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">binary_insert_sort</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;n;i++)<br>        &#123;<br>                <span class="hljs-type">int</span> t=q[i];<br>                <span class="hljs-keyword">if</span>(q[i]&gt;=q[i<span class="hljs-number">-1</span>]) <span class="hljs-keyword">continue</span>;<br>                <span class="hljs-comment">//二分出第一个&gt;t的元素，将此之后的所有元素往后移动一位，再将t插到当前有序位置</span><br>                <span class="hljs-type">int</span> l=<span class="hljs-number">0</span>,r=i<span class="hljs-number">-1</span>;<br>                <span class="hljs-keyword">while</span>(l&lt;r)<br>                &#123;<br>                        <span class="hljs-type">int</span> mid=l+r&gt;&gt;<span class="hljs-number">1</span>;<br>                        <span class="hljs-keyword">if</span>(q[mid]&gt;t) r=mid;<br>                        <span class="hljs-keyword">else</span> l=mid+<span class="hljs-number">1</span>;<br>                &#125;<br>                <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=i<span class="hljs-number">-1</span>;j&gt;=r;j--)<br>                        q[j+<span class="hljs-number">1</span>]=q[j];<br>                q[r]=t;<br>        &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="3-冒泡排序"><a href="#3-冒泡排序" class="headerlink" title="3.冒泡排序"></a>3.冒泡排序</h4><p><strong>做法:每次从后往前处理逆序对，把第i小的元素放i位，最多n-1次之后就有序，因为n-1次后，剩下的最后一个数一定是最大的</strong><br><strong>优化:可以通过交换次数进行优化，如果某一次没有交换一次，说明已经有序，直接break出去</strong><br><strong>时间复杂度</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1.最好情况:整个数组都有序，第一次就break,O(n)<br>2.平均情况:O(n^2)<br>3.最坏情况:每次使逆序对-1，最坏有n^2个逆序对，O(n^2)<br></code></pre></td></tr></table></figure>

<p><strong>辅助空间复杂度:O(1)</strong></p>
<p><strong>稳定</strong></p>
<p><strong>代码:</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">bubble_sort</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;n<span class="hljs-number">-1</span>;i++)<span class="hljs-comment">//最多执行n-1次，n-1次后剩下的一个数一定是最大的</span><br>        &#123;<br>                <span class="hljs-type">bool</span> has_swap=<span class="hljs-literal">false</span>;<span class="hljs-comment">//做优化，如果某次没有交换过一次，说明有序，直接break</span><br>                <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=n<span class="hljs-number">-1</span>;j&gt;i;j--)<span class="hljs-comment">//前i个元素有序(0~i-1),只需要比较后面的，又因为每次比较j与j-1，所以j&gt;i</span><br>                &#123;<br>                        <span class="hljs-keyword">if</span>(q[j]&lt;q[j<span class="hljs-number">-1</span>])<br>                        &#123;<br>                                <span class="hljs-built_in">swap</span>(q[j],q[j<span class="hljs-number">-1</span>]);<br>                                has_swap=<span class="hljs-literal">true</span>;<br>                        &#125;<br>                &#125;<br>                <span class="hljs-keyword">if</span>(!has_swap) <span class="hljs-keyword">break</span>;<br>        &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="4-简单-选择排序——最朴素的做法，所以也最慢"><a href="#4-简单-选择排序——最朴素的做法，所以也最慢" class="headerlink" title="4.(简单)选择排序——最朴素的做法，所以也最慢^+^"></a>4.(简单)选择排序——最朴素的做法，所以也最慢^+^</h4><p><strong>区别:冒泡排序可以看成是选择排序的优化</strong><br><strong>做法:选择排序不是处理逆序对，而是最朴素德每次找到第i小的元素，与第i位上的元素互换</strong><br><strong>时间复杂度</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1.最好情况:O(n^2)<br>2.平均情况:O(n^2)<br>3.最坏情况:O(n^2)<br></code></pre></td></tr></table></figure>

<p><strong>辅助空间复杂度:O(1)</strong></p>
<p><strong>不稳定，如2(1) 2(2) 1,排序后1 2（2） 2（1)，相对位置发生了变化</strong></p>
<p><strong>代码:</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">select_sort</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;n<span class="hljs-number">-1</span>;i++)<span class="hljs-comment">//也是最多只需要n-1次，n-1次后剩下的一个元素就是最大的</span><br>        &#123;<br>                <span class="hljs-type">int</span> k=i;<br>                <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=i+<span class="hljs-number">1</span>;j&lt;n;j++)<span class="hljs-comment">//前面i个有序(0~i-1),所以从i开始往后面找最小,进行交换</span><br>                        <span class="hljs-keyword">if</span>(q[j]&lt;q[k])<br>                                k=j;<br>                <span class="hljs-built_in">swap</span>(q[i],q[k]);<br>        &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="5-希尔排序——第一个把排序最坏时间复杂度降到O-n-2-之下的"><a href="#5-希尔排序——第一个把排序最坏时间复杂度降到O-n-2-之下的" class="headerlink" title="5.希尔排序——第一个把排序最坏时间复杂度降到O(n^2)之下的"></a>5.希尔排序——第一个把排序最坏时间复杂度降到O(n^2)之下的</h4><p><strong>做法:</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1.每次把所有元素分组，每一组下标都是等差数列，公差d称之为增量，每组的公差d相同<br><br>2.每组内部做插入排序<br><br>3.以此类推，组内插入排序，不断细化公差d，直到为d为1<br><br>当d为n/2,n/4,n/8...,1时，时间复杂度最坏O(n^2)<br>当d为n/3,n/9,n/27,...,1时，时间复杂度O(n^(3/2))<br></code></pre></td></tr></table></figure>

<p><strong>原因:借助插入排序对于部分有序的序列排序效果好的特性进行优化</strong></p>
<p><strong>时间复杂度</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">根据d的划分来看的，种类多<br>大体上O(n(3/2))<br></code></pre></td></tr></table></figure>

<p><strong>辅助空间复杂度:O(1)</strong></p>
<p><strong>不稳定，因为会进行分组</strong></p>
<p><strong>代码:</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">shell_sort</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> d=n/<span class="hljs-number">2</span>;d;d/=<span class="hljs-number">2</span>)<span class="hljs-comment">//枚举公差, for(int d=n/3;d;d==2?d=1:d/=3),并不是每一个都会除到1，所以要特判</span><br>        &#123;<br>                <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> start=<span class="hljs-number">0</span>;start&lt;d;start++)<span class="hljs-comment">//枚举起点，起点只会在1-d之间，对应下表就是0-d-1</span><br>                &#123;<br>                        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=start+d;i&lt;n;i+=d)<span class="hljs-comment">//枚举组内做插入排序，第一个元素有序,所以起点start+d</span><br>                        &#123;<br>                                <span class="hljs-type">int</span> t=q[i],j=i;<br>                                <span class="hljs-keyword">while</span>(j&gt;start&amp;&amp;q[j-d]&gt;t)<span class="hljs-comment">//起点位start</span><br>                                &#123;<br>                                        q[j]=q[j-d];<br>                                        j-=d;<span class="hljs-comment">//公差为d</span><br>                                &#125;<br>                                q[j]=t;<br>                        &#125;<br>                &#125;<br>        &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="6-快速排序-和归并排序一样，基于分治-且快排和哈希一般不考虑最坏情况时间复杂度"><a href="#6-快速排序-和归并排序一样，基于分治-且快排和哈希一般不考虑最坏情况时间复杂度" class="headerlink" title="6.快速排序(和归并排序一样，基于分治),且快排和哈希一般不考虑最坏情况时间复杂度"></a>6.快速排序(和归并排序一样，基于分治),且快排和哈希一般不考虑最坏情况时间复杂度</h4><p><strong>做法:1.找分界值通常x&#x3D;q[l+r&gt;&gt;1] 2.用分界值把区间分为左右两端，使左&lt;&#x3D;x,x,x&lt;&#x3D;右 3.递归排序左右两段</strong><br><strong>这里与一些老教材不同，AcWing的模板不能保证排完序后中间那个数就是x，而一些老版的教材可以保证这一点</strong></p>
<p><strong>时间复杂度</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1.最好情况:O(nlogn)<br>2.平均情况:O(nlogn)<br>3.最坏情况:O(n^2)<br></code></pre></td></tr></table></figure>

<p><strong>空间复杂度:由于需要调用系统栈，所以O(logn)</strong></p>
<p><strong>不稳定,因为在划分时，等于x的元素即可能在前，也可能在后</strong><br><strong>代码</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">quick_sort</span><span class="hljs-params">(<span class="hljs-type">int</span> q[],<span class="hljs-type">int</span> l,<span class="hljs-type">int</span> r)</span></span><br><span class="hljs-function"></span>&#123;<br>        <span class="hljs-keyword">if</span>(l&gt;=r) <span class="hljs-keyword">return</span> ;<br>        <span class="hljs-type">int</span> i=l<span class="hljs-number">-1</span>,j=r+<span class="hljs-number">1</span>,x=q[l+r&gt;&gt;<span class="hljs-number">1</span>];<br><br>        <span class="hljs-keyword">while</span>(i&lt;j)<span class="hljs-comment">//双指针算法</span><br>        &#123;<br>                <span class="hljs-keyword">do</span> i++;<span class="hljs-keyword">while</span>(q[i]&lt;x);<br>                <span class="hljs-keyword">do</span> j--;<span class="hljs-keyword">while</span>(q[j]&gt;x);<br>                <span class="hljs-keyword">if</span>(i&lt;j) <span class="hljs-built_in">swap</span>(q[i],q[j]);<br>        &#125;<br>        <span class="hljs-built_in">quick_sort</span>(q,l,j),<span class="hljs-built_in">quick_sort</span>(q,j+<span class="hljs-number">1</span>,r);<br>        <span class="hljs-comment">//递归的分界点，取j时,x不能为q[r],取q[l+r&gt;&gt;1];取i时，x不能取q[l],也不能取q[l+r&gt;&gt;1]，要取q[l+r+1&gt;&gt;1]</span><br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="7-堆排序"><a href="#7-堆排序" class="headerlink" title="7.堆排序"></a>7.堆排序</h4><p><strong>down(i)</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">递归处理使以i为父节点的树满足大根堆/小根堆的定义方式，不满足就往下;满足就不变<br></code></pre></td></tr></table></figure>

<p><strong>删除堆顶操作</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">并不是真的delete，而是放到heap的size之外去<br>先于最后一个数heap[size]互换，再删除heap[size](就是size--，让我们索引不到，但实际还在数组中)<br>最后由于刚刚上来的数不一定是最值，所以需要down(1)使满足堆的递归定义<br></code></pre></td></tr></table></figure>

<p><strong>做法:</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">建立小根堆或大根堆<br><br>大根堆:每次删除堆顶（将堆顶删除,就是最后一个数替换堆顶），然后heap的size-1，表示不再管q[]中的最后一个数,最后记得down(1)，因为最后一个数不能保证是此时heap中最大的数<br><br>小根堆:可以每次先输出/用辅助函数存下来，再删除堆顶；或者和大根堆一样操作，然后逆序输出<br></code></pre></td></tr></table></figure>

<p><strong>时间复杂度</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1.最好情况:O(nlogn)<br>2.平均情况:O(nlogn)<br>3.最坏情况:O(nlogn)<br></code></pre></td></tr></table></figure>

<p><strong>辅助空间复杂度:O(logn),因为要用递归</strong></p>
<p><strong>不稳定,第一个会与最后一个交换，相等就不稳定了</strong></p>
<p><strong>代码:</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//可用大根堆，可用小根堆</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">down</span><span class="hljs-params">(<span class="hljs-type">int</span> u)</span></span><br><span class="hljs-function"></span>&#123;<br>        <span class="hljs-type">int</span> t=u;<br>        <span class="hljs-keyword">if</span>(<span class="hljs-number">2</span>*u&lt;=sz &amp;&amp; q[<span class="hljs-number">2</span>*u]&gt;q[t]) t=<span class="hljs-number">2</span>*u;<span class="hljs-comment">//用小根堆就改成&lt;</span><br>        <span class="hljs-keyword">if</span>(<span class="hljs-number">2</span>*u+<span class="hljs-number">1</span>&lt;=sz &amp;&amp; q[<span class="hljs-number">2</span>*u+<span class="hljs-number">1</span>]&gt;q[t]) t=<span class="hljs-number">2</span>*u+<span class="hljs-number">1</span>;<span class="hljs-comment">//用小根堆就改成&lt;</span><br>        <span class="hljs-keyword">if</span>(u!=t)<br>        &#123;<br>                <span class="hljs-built_in">swap</span>(q[u],q[t]);<br>                <span class="hljs-built_in">down</span>(t);<br>        &#125;<br>&#125;<br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">heap_sort</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>        sz=n;<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=n/<span class="hljs-number">2</span>;i;i--)<br>                <span class="hljs-built_in">down</span>(i);<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;n<span class="hljs-number">-1</span>;i++)<br>        &#123;<br>        <span class="hljs-comment">//可以选择倒着输出q[]</span><br>        <span class="hljs-comment">//也可以在删的时候输出小根堆的堆顶q[1]，由于只输出了n-1次,出循环后再输出q[1]就行</span><br>                <span class="hljs-built_in">swap</span>(q[<span class="hljs-number">1</span>],q[sz]);<br>                sz--;<br>                <span class="hljs-built_in">down</span>(<span class="hljs-number">1</span>);<br>        &#125;<br><br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="8-归并排序-基于分治"><a href="#8-归并排序-基于分治" class="headerlink" title="8.归并排序(基于分治)"></a>8.归并排序(基于分治)</h4><p><strong>做法:1.找分界点mid&#x3D;(l+r)&#x2F;2 2.用分界点把区间分为左右两端，递归处理这两段 3.二路归并</strong></p>
<p><strong>时间复杂度</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1.最好情况:O(nlogn)<br>2.平均情况:O(nlogn)<br>3.最坏情况:O(nlogn)<br></code></pre></td></tr></table></figure>

<p><strong>空间复杂度:由于需要调用系统栈和辅助数组，所以O(logn+n) -&gt; O(n)</strong></p>
<p><strong>稳定</strong><br><strong>代码</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">merge_sort</span><span class="hljs-params">(<span class="hljs-type">int</span> q[],<span class="hljs-type">int</span> l,<span class="hljs-type">int</span> r)</span></span><br><span class="hljs-function"></span>&#123;<br>        <span class="hljs-keyword">if</span>(l&gt;=r) <span class="hljs-keyword">return</span> ;<br><br>        <span class="hljs-type">int</span> mid=l+r&gt;&gt;<span class="hljs-number">1</span>;<br>        <span class="hljs-built_in">merge_sort</span>(q,l,mid),<span class="hljs-built_in">merge_sort</span>(q,mid+<span class="hljs-number">1</span>,r);<br><br>        <span class="hljs-type">int</span> k=<span class="hljs-number">0</span>,i=l,j=mid+<span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">while</span>(i&lt;=mid&amp;&amp;j&lt;=r)<br>        &#123;<br>                <span class="hljs-keyword">if</span>(q[i]&lt;=q[j]) tmp[k++]=q[i++];<br>                <span class="hljs-keyword">else</span> tmp[k++]=q[j++];<br>        &#125;<br><br>        <span class="hljs-keyword">while</span>(i&lt;=mid) tmp[k++]=q[i++];<br>        <span class="hljs-keyword">while</span>(j&lt;=r) tmp[k++]=q[j++];<br><br>        <span class="hljs-keyword">for</span>(i=l,j=<span class="hljs-number">0</span>;i&lt;=r;i++,j++)<br>                q[i]=tmp[j];<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="非比较排序"><a href="#非比较排序" class="headerlink" title="非比较排序"></a>非比较排序</h3><h4 id="1-桶排序-又叫计数排序"><a href="#1-桶排序-又叫计数排序" class="headerlink" title="1.桶排序(又叫计数排序)"></a>1.桶排序(又叫计数排序)</h4><p><strong>桶:实际上就是开数组，存储每个数值出现的次数，所以数组的大小取决于数值的大小</strong><br><strong>a[i]的位置取决于小于a[i]的数量+i左边等于a[i]的数量,所以可以想到用前缀和的思想来求位置</strong></p>
<p><strong>此时存在一个区别:有没有相等的数</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1.如果所有数都不相等，可以直接从前往后做,位置为s[a[i]]-1<br>2.如果存在相等的数x，则需要从后往前摆放，s[x]-1,s[x]--,组合起来:--s[x]<br><br>这里s[x]-1是这么想的:假设第一个大于x的数为y,y的位置为s[x]<br>则最后一个x为s[x]-1,让后往前x的数量-1，所以s[x]--<br></code></pre></td></tr></table></figure>

<p><strong>思想:用桶+辅助数组直接把每个数摆放的应该放的位置，并保证相对位置不变</strong><br><strong>做法:为了保证相对顺序，倒着摆放</strong></p>
<p><strong>时间复杂度</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1.最好情况:O(n+m)<br>2.平均情况:O(n+m)<br>3.最坏情况:O(n+m)<br></code></pre></td></tr></table></figure>

<p><strong>空间复杂度:由于要求统计数值的个数和求数值数量的前缀和，以及辅助数组，所以O(n+m)</strong></p>
<p><strong>稳定:倒着排保证的相对位置</strong><br><strong>代码</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">bucket_sort</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;n;i++)    s[q[i]]++;<span class="hljs-comment">//记录数组中每个数值的数量</span><br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;N;i++)    s[i]+=s[i<span class="hljs-number">-1</span>];<span class="hljs-comment">//对每个数的数量求前缀和</span><br>        <span class="hljs-comment">//因为s[]的大小取决去桶的数量，也就是数组中数的大小</span><br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=n<span class="hljs-number">-1</span>;i&gt;=<span class="hljs-number">0</span>;i--) w[--s[q[i]]]=q[i];<span class="hljs-comment">//避免相同的x相对位置乱序，倒着摆放，最后一个x的位置为s[x]-1，完事s[x]--</span><br>        <span class="hljs-comment">//相当于位置为--s[x],所以就是w[--s[q[i]]]=q[i]</span><br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;n;i++) q[i]=w[i];<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="2-基数排序"><a href="#2-基数排序" class="headerlink" title="2.基数排序"></a>2.基数排序</h4><p><strong>实际上就是一个多关键字排序，这个多关键字是把a[i]化为r进制的每一位(估计数值范围，范围内用什么进制都可以)</strong></p>
<p><strong>做法：</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1.可以从高位递归到低位，需要用到系统栈;<br>2.可以从低位到高位枚举，由于桶排序是稳定的，所以基数排序也是稳定的<br></code></pre></td></tr></table></figure>

<p><strong>思想:用桶+辅助数组直接把每个数摆放的应该放的位置，并保证相对位置不变</strong><br><strong>做法:为了保证相对顺序，倒着摆放</strong></p>
<p><strong>时间复杂度</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1.最好情况:O(k(n+r)),k为a[i]分为r进制的位数<br>2.平均情况:O(k(n+r)),k为a[i]分为r进制的位数<br>3.最坏情况:O(k(n+r)),k为a[i]分为r进制的位数<br></code></pre></td></tr></table></figure>

<p><strong>空间复杂度:由于要求统计数值的个数和求数值数量的前缀和，以及辅助数组，所以O(n+m)</strong></p>
<p><strong>稳定:从低到高做桶排序，每次高位都是以低位为基础，如果高位相同，则按照低位的相对位置排，所以稳定</strong><br><strong>代码</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">radix_sort</span><span class="hljs-params">(<span class="hljs-type">int</span> d,<span class="hljs-type">int</span> r)</span><span class="hljs-comment">//d表示关键字的位数,r表示r进制</span></span><br><span class="hljs-function"></span>&#123;<br>        <span class="hljs-type">int</span> radix=<span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=d;i++)<span class="hljs-comment">//枚举每一位,从低位到高位,由于每次都是在上次的基础上再桶排序，所以稳定</span><br>        &#123;<br>                <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">0</span>;j&lt;r;j++) s[j]=<span class="hljs-number">0</span>;<span class="hljs-comment">//可以直接用memset</span><br><br>                <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">0</span>;j&lt;n;j++) s[q[j]/radix%r]++;<br><br>                <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">1</span>;j&lt;r;j++) s[j]+=s[j<span class="hljs-number">-1</span>];<br><br>                <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=n<span class="hljs-number">-1</span>;j&gt;=<span class="hljs-number">0</span>;j--) w[--s[q[j]/radix%r]]=q[j];<br><br>                <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">0</span>;j&lt;n;j++) q[j]=w[j];<br>                radix*=r;<br><br>        &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="外部排序-外存里的排序，主要取决于读取次数"><a href="#外部排序-外存里的排序，主要取决于读取次数" class="headerlink" title="外部排序(外存里的排序，主要取决于读取次数)"></a>外部排序(外存里的排序，主要取决于读取次数)</h3><p><strong>做法:</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1. 置换选择排序(在内存中预处理出m个有序序列,m可能很大，大到内存存不下)<br>2. 再利用Huffman思想(每次去k个最短的序列)，使用败者树将k个序列合并成一个序列,k个序列归并<br>   所以读写时间复杂度为O(logk m)<br>3. 以此类推直到m个序列合并成1个序列<br></code></pre></td></tr></table></figure>

<p>为了减少O(logk m)的读取时间复杂度，有两种方法</p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1. k增加，当时要权衡归并是内存对k的限制<br><br>2. m减小，用置换选择排序来生成更长的序列，从而使m减小<br></code></pre></td></tr></table></figure>

<p><strong>置换选择排序(内排序)主要有两种实现方法</strong></p>
<figure class="highlight plaintext"><table><tr><td class="code"><pre><code class="hljs plaintext">1. 堆排序<br>拿小根堆为例<br>如当前序列读到x，此时堆顶为a<br>1.1 先输出a<br>1.2 if(x&gt;a)<br>        插到堆顶，down(1)<br>    if(x&lt;a) <br>        不能放到堆顶,因为输出的序列要保持有序，直接插x到堆顶，下一次输出x会有a、x的序列，但是这是逆序的<br>        正确做法:把最后一个元素heap[size]放到堆顶，dowm(1),再将x放到最后元素的位置,up(size)<br>1.3 当堆为空时，就不能再操作，形成了一个升序序列，长度大概为2m<br>    再重新开文件做上述步骤，得到m个升序序列<br><br>2. 败者树(对胜者树排序的优化，优化IO次数) <br></code></pre></td></tr></table></figure>

<p><strong>每次从m段有序序列中选k段，合并成一段，可以用Huffman树的思想来优化，相当于k进制的Huffman树<br>每合并k段，形成1段，所以每次减少k-1段，如果(m-1)%(k-1)!&#x3D;0,则加0(也叫虚段)</strong></p>
<p><strong>k路归并时每次需要找到k个序列的最小值经行比较<br>可以用对来维护，而每次堆删除元素和插入比a更小的数，比较慢，删除——down(1),插入x(x&lt;a)——down(1)+up(size)<br>所以可以将堆优化为胜者树</strong></p>
<p><strong>胜者树:结构是堆,但是从下往上建立</strong><br><img src="https://cdn.acwing.com/media/article/image/2022/08/11/140994_9368d2ea19-1.jpg" alt="1.jpg"></p>
<p><strong>败者树:结构是堆，但是从下往上建立</strong><br><img src="https://cdn.acwing.com/media/article/image/2022/08/11/140994_e551870119-2.jpg" alt="2.jpg"></p>
<h2 id="数据结构"><a href="#数据结构" class="headerlink" title="数据结构"></a>数据结构</h2><h3 id="数组（array）"><a href="#数组（array）" class="headerlink" title="数组（array）"></a>数组（array）</h3><p><strong>数组是存放在连续内存空间上的相同类型数据的集合。</strong></p>
<p>两点注意的是</p>
<ul>
<li><strong>数组下标都是从0开始的。</strong></li>
<li><strong>数组内存空间的地址是连续的</strong></li>
</ul>
<p>正是<strong>因为数组的在内存空间的地址是连续的，所以我们在删除或者增添元素的时候，就难免要移动其他元素的地址。</strong></p>
<p><strong>数组的元素是不能删的，只能覆盖。</strong></p>
<h3 id="链表"><a href="#链表" class="headerlink" title="链表"></a>链表</h3><p>链表是一种通过指针串联在一起的线性结构。算法竞赛中主要使用数组模拟链表。</p>
<h4 id="单链表"><a href="#单链表" class="headerlink" title="单链表"></a><strong>单链表</strong></h4><p>每一个节点由两部分组成，一个是数据域一个是指针域（存放指向下一个节点的指针），最后一个节点的指针域指向null（空指针的意思）。</p>
<p>链表的入口节点称为链表的头结点也就是head。指针域只能指向节点的下一个节点。</p>
<p><img src="https://code-thinking-1253855093.file.myqcloud.com/pics/20200806194529815.png" alt="链表1"></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">// 单链表</span><br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">ListNode</span> &#123;<br>    <span class="hljs-type">int</span> val;  <span class="hljs-comment">// 节点上存储的元素</span><br>    ListNode *next;  <span class="hljs-comment">// 指向下一个节点的指针</span><br>    <span class="hljs-built_in">ListNode</span>(<span class="hljs-type">int</span> x) : <span class="hljs-built_in">val</span>(x), <span class="hljs-built_in">next</span>(<span class="hljs-literal">NULL</span>) &#123;&#125;  <span class="hljs-comment">// 节点的构造函数</span><br>&#125;;<br></code></pre></td></tr></table></figure>

<p>不定义构造函数行不行，答案是可以的，C++默认生成一个构造函数。</p>
<h4 id="双链表"><a href="#双链表" class="headerlink" title="双链表"></a><strong>双链表</strong></h4><p> 每一个节点有两个指针域，一个指向下一个节点，一个指向上一个节点。</p>
<p>双链表 既可以向前查询也可以向后查询。<img src="https://code-thinking-1253855093.file.myqcloud.com/pics/20200806194559317.png" alt="链表2"></p>
<h4 id="循环链表"><a href="#循环链表" class="headerlink" title="循环链表"></a><strong>循环链表</strong></h4><p>循环链表，顾名思义，就是链表首尾相连。</p>
<p><img src="https://code-thinking-1253855093.file.myqcloud.com/pics/20200806194629603.png" alt="链表4"></p>
<h4 id="数组模拟链表"><a href="#数组模拟链表" class="headerlink" title="数组模拟链表"></a>数组模拟链表</h4><p><strong>单链表</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//head存储链表头，e[]存储节点值，ne[]存储节点的next指针，idx表示当前索引用到了哪个节点</span><br><span class="hljs-type">int</span> head,e[N],ne[N],idx;<br><br><span class="hljs-comment">//初始化</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">init</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    head=<span class="hljs-number">-1</span>;<br>    idx=<span class="hljs-number">0</span>;<br>&#125;<br><span class="hljs-comment">//头插法</span><br><span class="hljs-comment">//在链表头插入一个数a</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">insert</span><span class="hljs-params">(<span class="hljs-type">int</span> a)</span></span><br><span class="hljs-function"></span>&#123;<br>    e[idx]=a,ne[idx]=head,head=idx++;<br>&#125;<br><br><span class="hljs-comment">//将x插到下标是k的点后面</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">add</span><span class="hljs-params">(<span class="hljs-type">int</span> k,<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    e[idx]=x;<br>    ne[idx]=ne[k];<br>    ne[k]=idx++;<br>&#125;<br><br><span class="hljs-comment">//将下标是k的点后面的点删掉</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">remove</span><span class="hljs-params">(<span class="hljs-type">int</span> k)</span></span><br><span class="hljs-function"></span>&#123;<br>    ne[k]=ne[ne[k]];<br>&#125;<br><br><span class="hljs-comment">//将头节点删除，需要保证头节点存在</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">remove</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    head=ne[head];<br>&#125;<br></code></pre></td></tr></table></figure>

<p><strong>双链表</strong></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//e[]表示节点的值，l[]表示节点的左指针，r[]表示节点的右指针，idx表示当前索引用到了哪个节点</span><br><span class="hljs-type">int</span> e[N],l[N],r[N],idx;<br><br><span class="hljs-comment">//初始化</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">init</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-comment">//0是左端点，1是右端点</span><br>    r[<span class="hljs-number">0</span>]=<span class="hljs-number">1</span>,l[<span class="hljs-number">1</span>]=<span class="hljs-number">0</span>;<br>    idx=<span class="hljs-number">2</span>;<br>&#125;<br><br><span class="hljs-comment">//在节点a的右边插入一个数x</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">insert</span><span class="hljs-params">(<span class="hljs-type">int</span> a,<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    e[idx]=x;<br>    l[idx]=a,r[idx]=r[a];<br>    l[r[a]]=idx,r[a]=idx++;<br>&#125;<br><span class="hljs-comment">//在节点a的左边插入一个数x可以调用右边插入的方法</span><br><span class="hljs-built_in">insert</span>(l[k],x);<br><br><span class="hljs-comment">//删除节点a</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">remove</span><span class="hljs-params">(<span class="hljs-type">int</span> a)</span></span><br><span class="hljs-function"></span>&#123;<br>    l[r[a]]=l[a];<br>    r[l[a]]=r[a];<br>&#125;<br></code></pre></td></tr></table></figure>



<h3 id="栈-stack"><a href="#栈-stack" class="headerlink" title="栈(stack)"></a>栈(stack)</h3><h4 id="STL中的栈"><a href="#STL中的栈" class="headerlink" title="STL中的栈"></a>STL中的栈</h4><h5 id="stack"><a href="#stack" class="headerlink" title="stack"></a>stack</h5><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-built_in">size</span>()<span class="hljs-comment">//栈的大小</span><br><span class="hljs-built_in">empty</span>()<span class="hljs-comment">//判断栈是否为空</span><br><span class="hljs-built_in">push</span>()<span class="hljs-comment">//向栈顶插入一个元素</span><br><span class="hljs-built_in">top</span>()<span class="hljs-comment">//返回栈顶元素</span><br><span class="hljs-built_in">pop</span>()<span class="hljs-comment">//弹出栈顶元素</span><br></code></pre></td></tr></table></figure>



<h4 id="数组模拟栈"><a href="#数组模拟栈" class="headerlink" title="数组模拟栈"></a>数组模拟栈</h4><h5 id="普通栈"><a href="#普通栈" class="headerlink" title="普通栈"></a>普通栈</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//tt表示栈顶</span><br><span class="hljs-type">int</span> stk[N],tt=<span class="hljs-number">0</span>;<br><br><span class="hljs-comment">//向栈顶插入一个数</span><br>stk[++tt]=x;<br><br><span class="hljs-comment">//从栈顶弹出一个数</span><br>tt--;<br><br><span class="hljs-comment">//栈顶的值</span><br>stk[tt];<br><br><span class="hljs-comment">//判断栈是否为空</span><br><span class="hljs-keyword">if</span>(tt&gt;<span class="hljs-number">0</span>)<br>&#123;<br>    <span class="hljs-keyword">not</span> empty<br>&#125;<br></code></pre></td></tr></table></figure>

<h5 id="单调栈"><a href="#单调栈" class="headerlink" title="单调栈"></a>单调栈</h5><p>常见模型：找出每个数左边离它最近的比它大&#x2F;小的数</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> tt=<span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=n;i++)<br>&#123;<br>    <span class="hljs-keyword">while</span>(tt&amp;&amp;<span class="hljs-built_in">check</span>(stk[tt],i)) tt--;<br>    stk[++tt]=i;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330092717494.png"></p>
<h3 id="队列-queue"><a href="#队列-queue" class="headerlink" title="队列(queue)"></a>队列(queue)</h3><h4 id="STL中的队列"><a href="#STL中的队列" class="headerlink" title="STL中的队列"></a>STL中的队列</h4><h5 id="queue"><a href="#queue" class="headerlink" title="queue"></a>queue</h5><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-built_in">size</span>()<br><span class="hljs-built_in">empty</span>()<br><span class="hljs-built_in">push</span>()<span class="hljs-comment">//向队尾插入一个元素</span><br><span class="hljs-built_in">front</span>()<span class="hljs-comment">//返回队头元素</span><br><span class="hljs-built_in">back</span>()<span class="hljs-comment">//返回队尾元素</span><br><span class="hljs-built_in">pop</span>()<span class="hljs-comment">//弹出队头元素</span><br></code></pre></td></tr></table></figure>

<h5 id="priority-queue"><a href="#priority-queue" class="headerlink" title="priority_queue"></a>priority_queue</h5><p>优先队列，默认是大根堆</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-built_in">push</span>()<span class="hljs-comment">//插入一个元素</span><br><span class="hljs-built_in">top</span>()<span class="hljs-comment">//返回堆顶元素</span><br><span class="hljs-built_in">pop</span>()<span class="hljs-comment">//弹出堆顶元素    </span><br></code></pre></td></tr></table></figure>

<p>定义成小根堆 </p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp">priority_queue&lt;<span class="hljs-type">int</span>,vector&lt;<span class="hljs-type">int</span>&gt;,greater&lt;<span class="hljs-type">int</span>&gt;&gt; q;<br></code></pre></td></tr></table></figure>

<h5 id="deque"><a href="#deque" class="headerlink" title="deque"></a>deque</h5><p>双端队列</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-built_in">size</span>()<br><span class="hljs-built_in">empty</span>()<br><span class="hljs-built_in">clear</span>()<br><span class="hljs-built_in">front</span>()/<span class="hljs-built_in">back</span>()<br><span class="hljs-built_in">push_back</span>()/<span class="hljs-built_in">pop_back</span>()<br><span class="hljs-built_in">push_front</span>()/<span class="hljs-built_in">pop_front</span>()<br><span class="hljs-built_in">begin</span>()/<span class="hljs-built_in">end</span>()<br>[]<br></code></pre></td></tr></table></figure>



<h4 id="数组模拟队列"><a href="#数组模拟队列" class="headerlink" title="数组模拟队列"></a>数组模拟队列</h4><h5 id="普通队列"><a href="#普通队列" class="headerlink" title="普通队列"></a>普通队列</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//hh表示队头，tt表示队尾</span><br><span class="hljs-type">int</span> q[N],hh=<span class="hljs-number">0</span>,tt=<span class="hljs-number">-1</span>;<br><br><span class="hljs-comment">//向队尾插入一个数</span><br>q[++tt]=x;<br><br><span class="hljs-comment">//从队头弹出一个数</span><br>hh++;<br><br><span class="hljs-comment">//队头的值</span><br>q[hh];<br><br><span class="hljs-comment">//队尾的值</span><br>q[tt];<br><br><span class="hljs-comment">//判断队列是否为空</span><br><span class="hljs-keyword">if</span>(hh&lt;=tt)<br>&#123;<br>    <span class="hljs-keyword">not</span> empty<br>&#125;<br></code></pre></td></tr></table></figure>

<h5 id="循环队列"><a href="#循环队列" class="headerlink" title="循环队列"></a>循环队列</h5><figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//hh表示队头，tt表示队尾的后一个位置</span><br><span class="hljs-type">int</span> q[N],hh=<span class="hljs-number">0</span>,tt=<span class="hljs-number">0</span>;<br><br><span class="hljs-comment">//向队尾插入一个数</span><br>q[tt++]=x;<br><span class="hljs-keyword">if</span>(tt==N) tt=<span class="hljs-number">0</span>;<br><br><span class="hljs-comment">//从队头弹出一个数</span><br>hh++;<br><span class="hljs-keyword">if</span>(hh==N) hh=<span class="hljs-number">0</span>;<br><br><span class="hljs-comment">//队头的值</span><br>q[hh];<br><br><span class="hljs-comment">//判断队列是否为空</span><br><span class="hljs-keyword">if</span>(hh!=tt)<br>&#123;<br>    <br>&#125;<br></code></pre></td></tr></table></figure>

<h5 id="单调队列"><a href="#单调队列" class="headerlink" title="单调队列"></a>单调队列</h5><p>常见模型：找出滑动窗口中的最大值&#x2F;最小值</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> hh=<span class="hljs-number">0</span>,tt=<span class="hljs-number">-1</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;n;i++)<br>&#123;<br>    <span class="hljs-keyword">while</span>(hh&lt;=tt&amp;&amp;<span class="hljs-built_in">check_out</span>(q[hh])) hh++;<span class="hljs-comment">//判断队头是否滑出窗口</span><br>    <span class="hljs-keyword">while</span>(hh&lt;=tt&amp;&amp;<span class="hljs-built_in">check</span>(q[tt],i)) tt--;<br>    q[++tt]=i;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330092619779.png"></p>
<h3 id="堆-heap"><a href="#堆-heap" class="headerlink" title="堆(heap)"></a>堆(heap)</h3><p>手写堆的基本操作</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-number">1.</span>插入一个数<br>    heap[++size]=x;<span class="hljs-built_in">up</span>(size);<br><span class="hljs-number">2.</span>求集合中最小值<br>    heap[<span class="hljs-number">1</span>]<br><span class="hljs-number">3.</span>删除最小值<br>    heap[<span class="hljs-number">1</span>]=heap[size];size--;<span class="hljs-built_in">down</span>(<span class="hljs-number">1</span>);<br><span class="hljs-number">4.</span>删除任意元素<br>    heap[k]=heap[size];size--;<span class="hljs-built_in">down</span>(k);<span class="hljs-built_in">up</span>(k);<br><span class="hljs-number">5.</span>修改任意元素<br>    heap[k]=x;<span class="hljs-built_in">down</span>(k);<span class="hljs-built_in">up</span>(k);<br></code></pre></td></tr></table></figure>

<p>手写堆的实现</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//h[N]存储堆中的值，h[1]是堆顶，x的左儿子是2x,右儿子是2x+1</span><br><span class="hljs-comment">//ph[k]存储第k个的点是 第几个插入的</span><br><span class="hljs-comment">//hp[k]存储堆中下标是k的点是第几个插入的</span><br><span class="hljs-type">int</span> h[N],ph[N],hp[N],size;<br><br><span class="hljs-comment">//交换两个点及其映射关系</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">heap_swap</span><span class="hljs-params">(<span class="hljs-type">int</span> a,<span class="hljs-type">int</span> b)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-built_in">swap</span>(ph[hp[a]],ph[hp[b]]);<br>    <span class="hljs-built_in">swap</span>(hp[a],hp[b]);<br>    <span class="hljs-built_in">swap</span>(h[a],h[b]);<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">down</span><span class="hljs-params">(<span class="hljs-type">int</span> u)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> t=u;<br>    <span class="hljs-keyword">if</span>(u*<span class="hljs-number">2</span>&lt;=size&amp;&amp;h[u*<span class="hljs-number">2</span>]&lt;h[t]) t=u*<span class="hljs-number">2</span>;<br>    <span class="hljs-keyword">if</span>(u*<span class="hljs-number">2</span>+<span class="hljs-number">1</span>&lt;=size&amp;&amp;h[u*<span class="hljs-number">2</span>+<span class="hljs-number">1</span>]&lt;h[t]) t=u*<span class="hljs-number">2</span>+<span class="hljs-number">1</span>;<br>    <span class="hljs-keyword">if</span>(u!=t)<br>    &#123;<br>        <span class="hljs-built_in">heap_swap</span>(u,t);<br>        <span class="hljs-built_in">down</span>(t);<br>    &#125;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">up</span><span class="hljs-params">(<span class="hljs-type">int</span> u)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">while</span>(u/<span class="hljs-number">2</span>&amp;&amp;h[u]&lt;h[u/<span class="hljs-number">2</span>])<br>    &#123;<br>        <span class="hljs-built_in">heap_swap</span>(u,u/<span class="hljs-number">2</span>);<br>        u&gt;&gt;<span class="hljs-number">1</span>;<br>    &#125;<br>&#125;<br><br><span class="hljs-comment">//O(n)建堆</span><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=n/<span class="hljs-number">2</span>;i;i--) <span class="hljs-built_in">down</span>(i);<br></code></pre></td></tr></table></figure>

<p>用stl库的priority_queue</p>
<h3 id="字符串（string）"><a href="#字符串（string）" class="headerlink" title="字符串（string）"></a>字符串（string）</h3><h4 id="KMP"><a href="#KMP" class="headerlink" title="KMP"></a>KMP</h4><p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330094444867.png"></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//求next数组</span><br><span class="hljs-comment">//s[]是模式串，p[]是模板串，n是s的长度，m是p的长度</span><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">2</span>,j=<span class="hljs-number">0</span>;i&lt;=m;i++)<br>&#123;<br>    <span class="hljs-keyword">while</span>(j&amp;&amp;p[i]!=p[j+<span class="hljs-number">1</span>]) j=ne[j];<br>    <span class="hljs-keyword">if</span>(p[i]==p[j+<span class="hljs-number">1</span>]) j++;<br>    ne[i]=j;<br>&#125;<br><br><span class="hljs-comment">//匹配</span><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>,j=<span class="hljs-number">0</span>;i&lt;=n;i++)<br>&#123;<br>    <span class="hljs-keyword">while</span>(j&amp;&amp;s[i]!=p[j+<span class="hljs-number">1</span>]) j=ne[j];<br>    <span class="hljs-keyword">if</span>(s[i]==p[j+<span class="hljs-number">1</span>]) j++;<br>    <span class="hljs-keyword">if</span>(j==m)<br>    &#123;<br>        j=ne[j];<br>        <span class="hljs-comment">//匹配成功后的逻辑</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330095633341.png"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330095416938.png"></p>
<h4 id="tire树"><a href="#tire树" class="headerlink" title="tire树"></a>tire树</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> son[N][<span class="hljs-number">26</span>],cnt[N],idx;<br><span class="hljs-comment">//son[][]存储树中每个节点的子节点</span><br><span class="hljs-comment">//cnt[]存储以每个节点结尾的单词数量</span><br><span class="hljs-comment">//0号点是根节点也是空节点</span><br><br><span class="hljs-comment">//插入一个字符串</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">insert</span><span class="hljs-params">(<span class="hljs-type">char</span> *str)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> p=<span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;str[i];i++)<br>    &#123;<br>        <span class="hljs-type">int</span> u=str[i]-<span class="hljs-string">&#x27;a&#x27;</span>;<br>        <span class="hljs-keyword">if</span>(!son[p][u]) son[p][u]=++idx;<br>        p=son[p][u];<br>    &#125;<br>    cnt[p++];<br>&#125;<br><br><span class="hljs-comment">//查询字符串出现的次数</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">query</span><span class="hljs-params">(<span class="hljs-type">char</span> *str)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> p=<span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;str[i];i++)<br>    &#123;<br>        <span class="hljs-type">int</span> u=str[i]-<span class="hljs-string">&#x27;a&#x27;</span>;<br>        <span class="hljs-keyword">if</span>(!son[p][u]) <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>        p=son[p][u];<br>    &#125;<br>    <span class="hljs-keyword">return</span> cnt[p];<br>&#125;<br></code></pre></td></tr></table></figure>





<h3 id="并查集"><a href="#并查集" class="headerlink" title="并查集"></a>并查集</h3><h4 id="朴素并查集"><a href="#朴素并查集" class="headerlink" title="朴素并查集"></a>朴素并查集</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> p[N];<span class="hljs-comment">//存储每个点的祖宗节点</span><br><br><span class="hljs-comment">//返回x的祖宗节点</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">find</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span>(p[x]!=x) p[x]=<span class="hljs-built_in">find</span>(p[x]);<br>    <span class="hljs-keyword">return</span> p[x];<br>&#125;<br><br><span class="hljs-comment">//初始化，假定节点编号是1~n</span><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=n;i++) p[i]=i;<br><br><span class="hljs-comment">//合并a和b所在的两个集合</span><br>p[<span class="hljs-built_in">find</span>(a)]=<span class="hljs-built_in">find</span>(b);<br></code></pre></td></tr></table></figure>

<h4 id="维护size的并查集"><a href="#维护size的并查集" class="headerlink" title="维护size的并查集"></a>维护size的并查集</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> p[N],size[N];<br><span class="hljs-comment">//p[]存储每个点的祖宗节点，size[]只有祖宗节点的有意义，表示祖宗节点所在集合中的点的数量</span><br><br><span class="hljs-comment">//返回祖宗节点</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">find</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span>(p[x]!=x) p[x]=<span class="hljs-built_in">find</span>(p[x]);<br>    <span class="hljs-keyword">return</span> p[x];<br>&#125;<br><br><span class="hljs-comment">//初始化，假定节点编号为1~n</span><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=n;i++)<br>&#123;<br>    p[i]=i;<br>    size[i]=<span class="hljs-number">1</span>;<br>&#125;<br><br><span class="hljs-comment">//合并a和b所在的集合</span><br>p[<span class="hljs-built_in">find</span>(a)]=<span class="hljs-built_in">find</span>(b);<br>size[b]+=size[a];<br></code></pre></td></tr></table></figure>

<h4 id="维护到祖宗节点距离的并查集"><a href="#维护到祖宗节点距离的并查集" class="headerlink" title="维护到祖宗节点距离的并查集"></a>维护到祖宗节点距离的并查集</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> p[N],d[N];<br><span class="hljs-comment">//p[]存储每个点的祖宗节点，d[x]存储x到p[x]的距离</span><br><br><span class="hljs-comment">//返回x的祖宗节点</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">find</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span>(p[x]!=x)<br>    &#123;<br>        <span class="hljs-type">int</span> u=<span class="hljs-built_in">find</span>(p[x]);<br>        d[x]+=d[p[x]];<br>        p[x]=u;<br>    &#125;<br>    <span class="hljs-keyword">return</span> p[x];<br>&#125;<br><br><span class="hljs-comment">//初始化，假定节点是1~n</span><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=n;i++)<br>&#123;<br>    p[i]=i;<br>    d[i]=<span class="hljs-number">0</span>;<br>&#125;<br><br><span class="hljs-comment">//合并a和b所在的集合</span><br>p[<span class="hljs-built_in">find</span>(a)]=<span class="hljs-built_in">find</span>(b);<br>d[<span class="hljs-built_in">find</span>(a)]=distance;<span class="hljs-comment">//根据具体问题，初始化find(a)的偏移量</span><br></code></pre></td></tr></table></figure>



<h3 id="Hash表"><a href="#Hash表" class="headerlink" title="Hash表"></a>Hash表</h3><p>哈希表是根据关键码的值而直接进行访问的数据结构。</p>
<p><strong>一般哈希表都是用来快速判断一个元素是否出现集合里。</strong></p>
<p>当我们想使用哈希法来解决问题的时候，我们一般会选择如下三种数据结构。</p>
<ul>
<li>数组</li>
<li>set （集合）</li>
<li>map(映射)</li>
</ul>
<h4 id="一般哈希"><a href="#一般哈希" class="headerlink" title="一般哈希"></a>一般哈希</h4><h5 id="拉链法"><a href="#拉链法" class="headerlink" title="拉链法"></a>拉链法</h5><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> h[N],e[N],ne[N],idx;<br><br><span class="hljs-comment">//向哈希表中插入一个数</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">insert</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> k=(x%N+N)%N;<br>    e[idx]=x;<br>    ne[idx]=h[k];<br>    h[k]=idx++;<br>&#125;<br><br><span class="hljs-comment">//在哈希表中查询某个数是否存在</span><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">find</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> k=(x%N+N)%N;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=h[k];i!=<span class="hljs-number">-1</span>;i=ne[i])<br>    &#123;<br>        <span class="hljs-keyword">if</span>(e[i]==x) <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<h5 id="开放寻址法"><a href="#开放寻址法" class="headerlink" title="开放寻址法"></a>开放寻址法</h5><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> h[N];<br><br><span class="hljs-comment">//如果x在哈希表中，返回x的下标；如果不在，返回x应该插入的位置</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">find</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> t=(x%N+N)%N;<br>    <span class="hljs-keyword">while</span>(h[t]！=null&amp;&amp;h[t]!=x)<br>    &#123;<br>        t++;<br>        <span class="hljs-keyword">if</span>(t==N) t=<span class="hljs-number">0</span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> t;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="字符串哈希"><a href="#字符串哈希" class="headerlink" title="字符串哈希"></a>字符串哈希</h4><p>核心思想：将字符串看成P进制数，P的经验值是131或者13331，取这两个值的冲突概率低</p>
<p>小技巧：取模的数用2^64，这样直接用unsigned long long存储，溢出的结果就是取模的结果</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">typedef</span> <span class="hljs-type">unsigned</span> <span class="hljs-type">long</span> <span class="hljs-type">long</span> ULL;<br>ULL h[N],p[N];<span class="hljs-comment">//h[k]存储字符串前k个字母的哈希值，p[k]存储 P^k mod 2^64</span><br><br><span class="hljs-comment">//初始化</span><br>p[<span class="hljs-number">0</span>]=<span class="hljs-number">1</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=n;i++)<br>&#123;<br>    h[i]=h[i<span class="hljs-number">-1</span>]*P+str[i];<br>    p[i]=p[i<span class="hljs-number">-1</span>]*P;<br>&#125;<br><br><span class="hljs-comment">//计算子串str[l,r]的哈希值</span><br><span class="hljs-function">ULL <span class="hljs-title">get</span><span class="hljs-params">(<span class="hljs-type">int</span> l,<span class="hljs-type">int</span> r)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">return</span> h[r]-h[l<span class="hljs-number">-1</span>]*p[r-l+<span class="hljs-number">1</span>];<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="二叉树"><a href="#二叉树" class="headerlink" title="二叉树"></a>二叉树</h3><h4 id="满二叉树"><a href="#满二叉树" class="headerlink" title="满二叉树"></a><strong>满二叉树</strong></h4><p>满二叉树：如果一棵二叉树只有度为0的结点和度为2的结点，并且度为0的结点在同一层上，则这棵二叉树为满二叉树。</p>
<p><img src="https://code-thinking-1253855093.file.myqcloud.com/pics/20200806185805576.png" alt="img"></p>
<p>这棵二叉树为满二叉树，也可以说深度为k，有2^k-1个节点的二叉树。</p>
<h4 id="完全二叉树"><a href="#完全二叉树" class="headerlink" title="完全二叉树"></a>完全二叉树</h4><p>什么是完全二叉树？</p>
<p>完全二叉树的定义如下：在完全二叉树中，除了最底层节点可能没填满外，其余每层节点数都达到最大值，并且最下面一层的节点都集中在该层最左边的若干位置。若最底层为第 h 层（h从1开始），则该层包含 1~ 2^(h-1) 个节点。</p>
<p><img src="https://code-thinking-1253855093.file.myqcloud.com/pics/20200920221638903.png" alt="img"></p>
<p><strong>优先级队列其实是一个堆，堆就是一棵完全二叉树，同时保证父子节点的顺序关系。</strong></p>
<h4 id="二叉搜索树"><a href="#二叉搜索树" class="headerlink" title="二叉搜索树"></a>二叉搜索树</h4><p>二叉搜索树是有数值的，<strong>二叉搜索树是一个有序树</strong>。</p>
<ul>
<li>若它的左子树不空，则左子树上所有结点的值均小于它的根结点的值；</li>
<li>若它的右子树不空，则右子树上所有结点的值均大于它的根结点的值；</li>
<li>它的左、右子树也分别为二叉排序树</li>
</ul>
<p><img src="https://code-thinking-1253855093.file.myqcloud.com/pics/20200806190304693.png" alt="img"></p>
<h4 id="平衡二叉搜索树"><a href="#平衡二叉搜索树" class="headerlink" title="平衡二叉搜索树"></a>平衡二叉搜索树</h4><p>平衡二叉搜索树：又被称为AVL（Adelson-Velsky and Landis）树，且具有以下性质：它是一棵空树或它的左右两个子树的高度差的绝对值不超过1，并且左右两个子树都是一棵平衡二叉树。</p>
<p><img src="https://code-thinking-1253855093.file.myqcloud.com/pics/20200806190511967.png" alt="img"></p>
<p>最后一棵 不是平衡二叉树，因为它的左右两个子树的高度差的绝对值超过了1。</p>
<p><strong>C++中map、set、multimap，multiset的底层实现都是平衡二叉搜索树</strong>，所以map、set的增删操作时间时间复杂度是logn，注意我这里没有说unordered_map、unordered_set，unordered_map、unordered_set底层实现是哈希表。</p>
<h4 id="二叉树的存储"><a href="#二叉树的存储" class="headerlink" title="二叉树的存储"></a>二叉树的存储</h4><p><strong>二叉树可以链式存储，也可以顺序存储。</strong></p>
<p>那么链式存储方式就用指针， 顺序存储的方式就是用数组。</p>
<p>顾名思义就是顺序存储的元素在内存是连续分布的，而链式存储则是通过指针把分布在各个地址的节点串联一起。</p>
<p>链式存储如图：</p>
<p><img src="https://code-thinking-1253855093.file.myqcloud.com/pics/2020092019554618.png" alt="img"></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">struct</span> <span class="hljs-title class_">TreeNode</span> &#123;<br>    <span class="hljs-type">int</span> val;<br>    TreeNode *left;<br>    TreeNode *right;<br>    <span class="hljs-built_in">TreeNode</span>(<span class="hljs-type">int</span> x) : <span class="hljs-built_in">val</span>(x), <span class="hljs-built_in">left</span>(<span class="hljs-literal">NULL</span>), <span class="hljs-built_in">right</span>(<span class="hljs-literal">NULL</span>) &#123;&#125;<br>&#125;;<br></code></pre></td></tr></table></figure>

<p>链式存储是大家很熟悉的一种方式，那么我们来看看如何顺序存储呢？</p>
<p>其实就是用数组来存储二叉树，顺序存储的方式如图：</p>
<p><img src="https://code-thinking-1253855093.file.myqcloud.com/pics/20200920200429452.png" alt="img"></p>
<p>用数组来存储二叉树如何遍历的呢？</p>
<p><strong>如果父节点的数组下标是 i，那么它的左孩子就是 i * 2 + 1，右孩子就是 i * 2 + 2。</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-comment">//tree[N]存储堆中的值，tree[0]是根</span><br>tree[N];<br></code></pre></td></tr></table></figure>



<h3 id="线段树"><a href="#线段树" class="headerlink" title="线段树"></a>线段树</h3><h3 id="树状数组-Binary-Indexed-Tree"><a href="#树状数组-Binary-Indexed-Tree" class="headerlink" title="树状数组(Binary Indexed Tree)"></a>树状数组(Binary Indexed Tree)</h3><p>利用数的二进制特征进行检索的树状结构</p>
<p>主要用于对一个数组进行<strong>区间求和+点修</strong>或者<strong>区间修改+点查</strong></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240226122648324.png"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240226122731502.png"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240226122829557.png"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240226122911204.png"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240226122932765.png"></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//董晓算法的板子</span><br><span class="hljs-type">int</span> tree[N]<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">lowbit</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span>&#123;<span class="hljs-keyword">return</span> x&amp;-x;&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">change</span><span class="hljs-params">(<span class="hljs-type">int</span> x,<span class="hljs-type">int</span> k)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">while</span>(x&lt;=N) tree[x]+=k,x+=<span class="hljs-built_in">lowbit</span>(x);<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">query</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> t=<span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">while</span>(x) t+=tree[x],x-=<span class="hljs-built_in">lowbit</span>(x);<br>    <span class="hljs-keyword">return</span> t;<br>&#125;<br><br><span class="hljs-comment">//ACWing板子</span><br><span class="hljs-type">int</span> tr[M]<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">lowbit</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">return</span> x&amp;-x;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">query</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> res = <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=x;i;i-=<span class="hljs-built_in">lowbit</span>(i))<br>        res+=tr[i];<br>    <span class="hljs-keyword">return</span> res;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">add</span><span class="hljs-params">(<span class="hljs-type">int</span> x,<span class="hljs-type">int</span> v)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=x;i&lt;M;i+=<span class="hljs-built_in">lowbit</span>(i))<br>        tr[i]+=v;<br>&#125;<br></code></pre></td></tr></table></figure>



<h2 id="递归与回溯"><a href="#递归与回溯" class="headerlink" title="递归与回溯"></a>递归与回溯</h2><h3 id="递归"><a href="#递归" class="headerlink" title="递归"></a>递归</h3><h3 id="回溯"><a href="#回溯" class="headerlink" title="回溯"></a>回溯</h3><p>回溯是递归的副产品，只要有递归就会有回溯。</p>
<p><strong>回溯的本质是穷举，穷举所有可能，然后选出我们想要的答案</strong>，如果想让回溯法高效一些，可以加一些剪枝的操作，但也改不了回溯法就是穷举的本质。</p>
<p>回溯法，一般可以解决如下几种问题：</p>
<ul>
<li>组合问题：N个数里面按一定规则找出k个数的集合</li>
<li>切割问题：一个字符串按一定规则有几种切割方式</li>
<li>子集问题：一个N个数的集合里有多少符合条件的子集</li>
<li>排列问题：N个数按一定规则全排列，有几种排列方式</li>
<li>棋盘问题：N皇后，解数独等等</li>
</ul>
<p><strong>回溯法解决的问题都可以抽象为树形结构</strong>，回溯法解决的都是在集合中递归查找子集，<strong>集合的大小就构成了树的宽度，递归的深度，都构成的树的深度</strong>。</p>
<p>递归就要有终止条件，所以必然是一棵高度有限的树（N叉树）。</p>
<p>如图：</p>
<p><img src="https://code-thinking-1253855093.file.myqcloud.com/pics/20210130173631174.png" alt="回溯算法理论基础"></p>
<p>注意图中，我特意举例集合大小和孩子的数量是相等的！</p>
<p>回溯函数遍历过程伪代码如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">for (选择：本层集合中元素（树中节点孩子的数量就是集合的大小）) &#123;<br>    处理节点;<br>    backtracking(路径，选择列表); // 递归<br>    回溯，撤销处理结果<br>&#125;<br></code></pre></td></tr></table></figure>

<p>for循环就是遍历集合区间，可以理解一个节点有多少个孩子，这个for循环就执行多少次。</p>
<p>backtracking这里自己调用自己，实现递归。</p>
<p>大家可以从图中看出<strong>for循环可以理解是横向遍历，backtracking（递归）就是纵向遍历</strong>，这样就把这棵树全遍历完了，一般来说，搜索叶子节点就是找的其中一个结果了。</p>
<p>分析完过程，回溯算法模板框架如下：</p>
<figure class="highlight text"><table><tr><td class="code"><pre><code class="hljs text">void backtracking(参数) &#123;<br>    if (终止条件) &#123;<br>        存放结果;<br>        return;<br>    &#125;<br><br>    for (选择：本层集合中元素（树中节点孩子的数量就是集合的大小）) &#123;<br>        处理节点;<br>        backtracking(路径，选择列表); // 递归<br>        回溯，撤销处理结果<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>



<h2 id="数论"><a href="#数论" class="headerlink" title="数论"></a>数论</h2><p>数论的题很多时候是需要数学定理，结论以及证明。</p>
<h3 id="质数与约数"><a href="#质数与约数" class="headerlink" title="质数与约数"></a>质数与约数</h3><h4 id="试除法判定质数"><a href="#试除法判定质数" class="headerlink" title="试除法判定质数"></a>试除法判定质数</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">is_prime</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span>(x&lt;<span class="hljs-number">2</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">2</span>;i&lt;=x/i;i++)<br>    &#123;<br>        <span class="hljs-keyword">if</span>(x%i==<span class="hljs-number">0</span>) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="试除法分解质因数"><a href="#试除法分解质因数" class="headerlink" title="试除法分解质因数"></a>试除法分解质因数</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">divide</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=x/i;i++)<br>    &#123;<br>        <span class="hljs-keyword">if</span>(x%i==<span class="hljs-number">0</span>)<br>        &#123;<br>            <span class="hljs-type">int</span> s=<span class="hljs-number">0</span>;<br>            <span class="hljs-keyword">while</span>(x%i==<span class="hljs-number">0</span>) x/=i,s++;<br>            cout&lt;&lt;i&lt;&lt;<span class="hljs-string">&quot; &quot;</span>&lt;&lt;s&lt;&lt;endl;<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">if</span>(x&gt;<span class="hljs-number">1</span>) cout&lt;&lt;x&lt;&lt;<span class="hljs-string">&quot; &quot;</span>&lt;&lt;<span class="hljs-number">1</span>&lt;&lt;endl;<br>    cout&lt;&lt;endl;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="朴素筛法求素数"><a href="#朴素筛法求素数" class="headerlink" title="朴素筛法求素数"></a>朴素筛法求素数</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> primes[N],cnt;<span class="hljs-comment">//primes[]存储所有素数</span><br><span class="hljs-type">bool</span> vis[N];<span class="hljs-comment">//vis[x]存储x是否被筛掉</span><br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">get_primes</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">2</span>;i&lt;=n;i++)<br>    &#123;<br>        <span class="hljs-keyword">if</span>(vis[i]) <span class="hljs-keyword">continue</span>;<br>        primes[cnt++]=i;<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=i;j&lt;=n;j+=i) vis[j]=<span class="hljs-literal">true</span>;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="线性筛法求素数"><a href="#线性筛法求素数" class="headerlink" title="线性筛法求素数"></a>线性筛法求素数</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> primes[N],cnt;<span class="hljs-comment">//primes[]存储所有素数</span><br><span class="hljs-type">bool</span> vis[N];<span class="hljs-comment">//vis[x]存储x是否被筛掉</span><br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">get_primes</span><span class="hljs-params">(<span class="hljs-type">int</span> n)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">2</span>;i&lt;=n;i++)<br>    &#123;<br>        <span class="hljs-keyword">if</span>(!vis[i]) primes[cnt++]=i;<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">0</span>;primes[j]&lt;=n/i;j++)<br>        &#123;<br>            vis[primes[j]*i]=<span class="hljs-literal">true</span>;<br>            <span class="hljs-keyword">if</span>(i%primes[j]==<span class="hljs-number">0</span>) <span class="hljs-keyword">break</span>;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="试除法求所有约数"><a href="#试除法求所有约数" class="headerlink" title="试除法求所有约数"></a>试除法求所有约数</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-function">vector&lt;<span class="hljs-type">int</span>&gt; <span class="hljs-title">get_divisors</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    vector&lt;<span class="hljs-type">int</span>&gt; res;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=x/i;i++)<br>    &#123;<br>        <span class="hljs-keyword">if</span>(x%i==<span class="hljs-number">0</span>)<br>        &#123;<br>            res.<span class="hljs-built_in">push_back</span>(i);<br>            <span class="hljs-keyword">if</span>(i!=x/i) res.<span class="hljs-built_in">push_back</span>(x/i);<br>        &#125;<br>    &#125;<br>    <span class="hljs-built_in">sort</span>(res.<span class="hljs-built_in">begin</span>(),res.<span class="hljs-built_in">end</span>());<br>    <span class="hljs-keyword">return</span> res;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="约数个数和约数之和"><a href="#约数个数和约数之和" class="headerlink" title="约数个数和约数之和"></a>约数个数和约数之和</h4><p>如果有N&#x3D;${p_1}^{c_1}<em>{p_2}^{c_2}</em>…*{p_k}^{c_k}$</p>
<p>约数个数：$(c_1+1)<em>(c_2+1)</em>…*(c_k+1)$</p>
<p>约数之和：$({p_1}^0+{p_1}^1+…+{p_1}^{c_1})*…({p_k}^0+{p_k}^1+…+{p_k}^{c_k})$</p>
<h4 id="欧几里得算法"><a href="#欧几里得算法" class="headerlink" title="欧几里得算法"></a>欧几里得算法</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">gcd</span><span class="hljs-params">(<span class="hljs-type">int</span> a,<span class="hljs-type">int</span> b)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">return</span> b?<span class="hljs-built_in">gcd</span>(b,a%b):a;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="扩展欧几里得算法"><a href="#扩展欧几里得算法" class="headerlink" title="扩展欧几里得算法"></a>扩展欧几里得算法</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//求x,y，使得ax+by=gcd(a,b)</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">exgcd</span><span class="hljs-params">(<span class="hljs-type">int</span> a,<span class="hljs-type">int</span> b,<span class="hljs-type">int</span> &amp;x,<span class="hljs-type">int</span> &amp;y)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span>(!b)<br>    &#123;<br>        x=<span class="hljs-number">1</span>;y=<span class="hljs-number">0</span>;<br>        <span class="hljs-keyword">return</span> a;<br>    &#125;<br>    <span class="hljs-type">int</span> d=<span class="hljs-built_in">exgcd</span>(b,a%b,y,x);<br>    y-=(a/b)*x;<br>    <span class="hljs-keyword">return</span> d;<br>&#125;<br></code></pre></td></tr></table></figure>



<h3 id="欧拉函数"><a href="#欧拉函数" class="headerlink" title="欧拉函数"></a>欧拉函数</h3><h4 id="朴素求欧拉函数"><a href="#朴素求欧拉函数" class="headerlink" title="朴素求欧拉函数"></a>朴素求欧拉函数</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">phi</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> res=x;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">2</span>;i&lt;=x/i;i++)<br>    &#123;<br>        <span class="hljs-keyword">if</span>(x%i==<span class="hljs-number">0</span>)<br>        &#123;<br>            res=res/i*(i<span class="hljs-number">-1</span>);<br>            <span class="hljs-keyword">while</span>(x%i==<span class="hljs-number">0</span>) x/=i;<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">if</span>(x&gt;<span class="hljs-number">1</span>) res=res/x*(x<span class="hljs-number">-1</span>);<br>    <span class="hljs-keyword">return</span> res;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="筛法求欧拉函数"><a href="#筛法求欧拉函数" class="headerlink" title="筛法求欧拉函数"></a>筛法求欧拉函数</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> primes[N],cnt;<span class="hljs-comment">//primes存储所有素数</span><br><span class="hljs-type">int</span> euler[N];<span class="hljs-comment">//存储每一个数的欧拉函数</span><br><span class="hljs-type">bool</span> vis[N];<span class="hljs-comment">//vis[x]存储x是否被筛掉</span><br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">get_eulers</span><span class="hljs-params">(<span class="hljs-type">int</span> n)</span></span><br><span class="hljs-function"></span>&#123;<br>    euler[<span class="hljs-number">1</span>]=<span class="hljs-number">1</span>;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">2</span>;i&lt;=n;i++)<br>    &#123;<br>        <span class="hljs-keyword">if</span>(!vis[i])<br>        &#123;<br>            primes[cnt++]=i;<br>            euler[i]=i<span class="hljs-number">-1</span>;<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">0</span>;primes[j]&lt;=n/i;j++)<br>    &#123;<br>        <span class="hljs-type">int</span> t=primes[j]*i;<br>        vis[t]=<span class="hljs-literal">true</span>;<br>        <span class="hljs-keyword">if</span>(i%primes[j]==<span class="hljs-number">0</span>)<br>        &#123;<br>            euler[t]=euler[i]*primes[j];<br>            <span class="hljs-keyword">break</span>;<br>        &#125;<br>        euler[t]=euler[i]*(primes[j]<span class="hljs-number">-1</span>);<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>



<h3 id="费马小定理"><a href="#费马小定理" class="headerlink" title="费马小定理"></a>费马小定理</h3><h3 id="中国剩余定理"><a href="#中国剩余定理" class="headerlink" title="中国剩余定理"></a>中国剩余定理</h3><h3 id="容斥原理"><a href="#容斥原理" class="headerlink" title="容斥原理"></a>容斥原理</h3><h3 id="高斯消元"><a href="#高斯消元" class="headerlink" title="高斯消元"></a>高斯消元</h3><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-comment">// a[N][N]是增广矩阵</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">gauss</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> c, r;<br>    <span class="hljs-keyword">for</span> (c = <span class="hljs-number">0</span>, r = <span class="hljs-number">0</span>; c &lt; n; c ++ )<br>    &#123;<br>        <span class="hljs-type">int</span> t = r;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = r; i &lt; n; i ++ )   <span class="hljs-comment">// 找到绝对值最大的行</span><br>            <span class="hljs-keyword">if</span> (<span class="hljs-built_in">fabs</span>(a[i][c]) &gt; <span class="hljs-built_in">fabs</span>(a[t][c]))<br>                t = i;<br><br>        <span class="hljs-keyword">if</span> (<span class="hljs-built_in">fabs</span>(a[t][c]) &lt; eps) <span class="hljs-keyword">continue</span>;<br><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = c; i &lt;= n; i ++ ) <span class="hljs-built_in">swap</span>(a[t][i], a[r][i]);      <span class="hljs-comment">// 将绝对值最大的行换到最顶端</span><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = n; i &gt;= c; i -- ) a[r][i] /= a[r][c];      <span class="hljs-comment">// 将当前上的首位变成1</span><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = r + <span class="hljs-number">1</span>; i &lt; n; i ++ )       <span class="hljs-comment">// 用当前行将下面所有的列消成0</span><br>            <span class="hljs-keyword">if</span> (<span class="hljs-built_in">fabs</span>(a[i][c]) &gt; eps)<br>                <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = n; j &gt;= c; j -- )<br>                    a[i][j] -= a[r][j] * a[i][c];<br><br>        r ++ ;<br>    &#125;<br><br>    <span class="hljs-keyword">if</span> (r &lt; n)<br>    &#123;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = r; i &lt; n; i ++ )<br>            <span class="hljs-keyword">if</span> (<span class="hljs-built_in">fabs</span>(a[i][n]) &gt; eps)<br>                <span class="hljs-keyword">return</span> <span class="hljs-number">2</span>; <span class="hljs-comment">// 无解</span><br>        <span class="hljs-keyword">return</span> <span class="hljs-number">1</span>; <span class="hljs-comment">// 有无穷多组解</span><br>    &#125;<br><br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = n - <span class="hljs-number">1</span>; i &gt;= <span class="hljs-number">0</span>; i -- )<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = i + <span class="hljs-number">1</span>; j &lt; n; j ++ )<br>            a[i][n] -= a[i][j] * a[j][n];<br><br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>; <span class="hljs-comment">// 有唯一解</span><br>&#125;<br></code></pre></td></tr></table></figure>



<h3 id="组合计数"><a href="#组合计数" class="headerlink" title="组合计数"></a>组合计数</h3><p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\40572741-3470-412c-b208-039a8df38911-3350490.jpg"></p>
<h4 id="递归法求组合数"><a href="#递归法求组合数" class="headerlink" title="递归法求组合数"></a>递归法求组合数</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-comment">// c[a][b] 表示从a个苹果中选b个的方案数</span><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; N; i ++ )<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt;= i; j ++ )<br>        <span class="hljs-keyword">if</span> (!j) c[i][j] = <span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">else</span> c[i][j] = (c[i - <span class="hljs-number">1</span>][j] + c[i - <span class="hljs-number">1</span>][j - <span class="hljs-number">1</span>]) % mod;<br></code></pre></td></tr></table></figure>


<h4 id="通过预处理逆元的方式求组合数"><a href="#通过预处理逆元的方式求组合数" class="headerlink" title="通过预处理逆元的方式求组合数"></a>通过预处理逆元的方式求组合数</h4><p>首先预处理出所有阶乘取模的余数<code>fact[N]</code>，以及所有阶乘取模的逆元<code>infact[N]</code><br>如果取模的数是质数，可以用费马小定理求逆元</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">qmi</span><span class="hljs-params">(<span class="hljs-type">int</span> a, <span class="hljs-type">int</span> k, <span class="hljs-type">int</span> p)</span>    <span class="hljs-comment">// 快速幂模板</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> res = <span class="hljs-number">1</span>;<br>    <span class="hljs-keyword">while</span> (k)<br>    &#123;<br>        <span class="hljs-keyword">if</span> (k &amp; <span class="hljs-number">1</span>) res = (LL)res * a % p;<br>        a = (LL)a * a % p;<br>        k &gt;&gt;= <span class="hljs-number">1</span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> res;<br>&#125;<br><br><span class="hljs-comment">// 预处理阶乘的余数和阶乘逆元的余数</span><br>fact[<span class="hljs-number">0</span>] = infact[<span class="hljs-number">0</span>] = <span class="hljs-number">1</span>;<br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt; N; i ++ )<br>&#123;<br>    fact[i] = (LL)fact[i - <span class="hljs-number">1</span>] * i % mod;<br>    infact[i] = (LL)infact[i - <span class="hljs-number">1</span>] * <span class="hljs-built_in">qmi</span>(i, mod - <span class="hljs-number">2</span>, mod) % mod;<br>&#125;<br></code></pre></td></tr></table></figure>


<h4 id="Lucas定理"><a href="#Lucas定理" class="headerlink" title="Lucas定理"></a>Lucas定理</h4><p>若p是质数，则对于任意整数 $1 &lt;&#x3D; m &lt;&#x3D; n$，有：<br>    $C_n^m &#x3D; C_{n%p}^{m%p} * C_{n &#x2F; p}^{m &#x2F; p} (%p)$</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">qmi</span><span class="hljs-params">(<span class="hljs-type">int</span> a, <span class="hljs-type">int</span> k)</span>       <span class="hljs-comment">// 快速幂模板</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> res = <span class="hljs-number">1</span>;<br>    <span class="hljs-keyword">while</span> (k)<br>    &#123;<br>        <span class="hljs-keyword">if</span> (k &amp; <span class="hljs-number">1</span>) res = (LL)res * a % p;<br>        a = (LL)a * a % p;<br>        k &gt;&gt;= <span class="hljs-number">1</span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> res;<br>&#125;<br><br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">C</span><span class="hljs-params">(<span class="hljs-type">int</span> a, <span class="hljs-type">int</span> b)</span>     <span class="hljs-comment">// 通过定理求组合数C(a, b)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> res = <span class="hljs-number">1</span>;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>, j = a; i &lt;= b; i ++, j -- )<br>    &#123;<br>        res = (LL)res * j % p;<br>        res = (LL)res * <span class="hljs-built_in">qmi</span>(i, p - <span class="hljs-number">2</span>) % p;<br>    &#125;<br>    <span class="hljs-keyword">return</span> res;<br>&#125;<br><br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">lucas</span><span class="hljs-params">(LL a, LL b)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span> (a &lt; p &amp;&amp; b &lt; p) <span class="hljs-keyword">return</span> <span class="hljs-built_in">C</span>(a, b);<br>    <span class="hljs-keyword">return</span> (LL)<span class="hljs-built_in">C</span>(a % p, b % p) * <span class="hljs-built_in">lucas</span>(a / p, b / p) % p;<br>&#125;<br></code></pre></td></tr></table></figure>



<h4 id="分解质因数法求组合数"><a href="#分解质因数法求组合数" class="headerlink" title="分解质因数法求组合数"></a>分解质因数法求组合数</h4><p>当我们需要求出组合数的真实值，而非对某个数的余数时，分解质因数的方式比较好用：</p>
<ol>
<li>筛法求出范围内的所有质数</li>
<li>通过$$ C_a^b &#x3D; \frac{a!(a - b)!}{b!}$$ 这个公式求出每个质因子的次数。 $n! $中$p$的次数是$$ \frac{n}{p} + \frac{n}{p^2}+ \frac{n}{p^3} + …$$</li>
<li>用高精度乘法将所有质因子相乘</li>
</ol>
<figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> primes[N], cnt;     <span class="hljs-comment">// 存储所有质数</span><br><span class="hljs-type">int</span> sum[N];     <span class="hljs-comment">// 存储每个质数的次数</span><br><span class="hljs-type">bool</span> st[N];     <span class="hljs-comment">// 存储每个数是否已被筛掉</span><br><br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">get_primes</span><span class="hljs-params">(<span class="hljs-type">int</span> n)</span>      <span class="hljs-comment">// 线性筛法求素数</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">2</span>; i &lt;= n; i ++ )<br>    &#123;<br>        <span class="hljs-keyword">if</span> (!st[i]) primes[cnt ++ ] = i;<br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; primes[j] &lt;= n / i; j ++ )<br>        &#123;<br>            st[primes[j] * i] = <span class="hljs-literal">true</span>;<br>            <span class="hljs-keyword">if</span> (i % primes[j] == <span class="hljs-number">0</span>) <span class="hljs-keyword">break</span>;<br>        &#125;<br>    &#125;<br>&#125;<br><br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">get</span><span class="hljs-params">(<span class="hljs-type">int</span> n, <span class="hljs-type">int</span> p)</span>       <span class="hljs-comment">// 求n！中的次数</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> res = <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">while</span> (n)<br>    &#123;<br>        res += n / p;<br>        n /= p;<br>    &#125;<br>    <span class="hljs-keyword">return</span> res;<br>&#125;<br><br><br><span class="hljs-function">vector&lt;<span class="hljs-type">int</span>&gt; <span class="hljs-title">mul</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt; a, <span class="hljs-type">int</span> b)</span>       <span class="hljs-comment">// 高精度乘低精度模板</span></span><br><span class="hljs-function"></span>&#123;<br>    vector&lt;<span class="hljs-type">int</span>&gt; c;<br>    <span class="hljs-type">int</span> t = <span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; a.<span class="hljs-built_in">size</span>(); i ++ )<br>    &#123;<br>        t += a[i] * b;<br>        c.<span class="hljs-built_in">push_back</span>(t % <span class="hljs-number">10</span>);<br>        t /= <span class="hljs-number">10</span>;<br>    &#125;<br><br>    <span class="hljs-keyword">while</span> (t)<br>    &#123;<br>        c.<span class="hljs-built_in">push_back</span>(t % <span class="hljs-number">10</span>);<br>        t /= <span class="hljs-number">10</span>;<br>    &#125;<br>    <br>    <span class="hljs-keyword">return</span> c;<br><br>&#125;<br><br><span class="hljs-built_in">get_primes</span>(a);  <span class="hljs-comment">// 预处理范围内的所有质数</span><br><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; cnt; i ++ )     <span class="hljs-comment">// 求每个质因数的次数</span><br>&#123;<br>    <span class="hljs-type">int</span> p = primes[i];<br>    sum[i] = <span class="hljs-built_in">get</span>(a, p) - <span class="hljs-built_in">get</span>(b, p) - <span class="hljs-built_in">get</span>(a - b, p);<br>&#125;<br><br>vector&lt;<span class="hljs-type">int</span>&gt; res;<br>res.<span class="hljs-built_in">push_back</span>(<span class="hljs-number">1</span>);<br><br><span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; cnt; i ++ )     <span class="hljs-comment">// 用高精度乘法将所有质因子相乘</span><br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> j = <span class="hljs-number">0</span>; j &lt; sum[i]; j ++ )<br>        res = <span class="hljs-built_in">mul</span>(res, primes[i]);<br></code></pre></td></tr></table></figure>



<h3 id="卡特兰数"><a href="#卡特兰数" class="headerlink" title="卡特兰数"></a>卡特兰数</h3><p>给定n个0和n个1，它们按照某种顺序排成长度为2n的序列，满足任意前缀中0的个数都不少于1的个数的序列的数量为：$$ Cat(n) &#x3D; \frac{C_{2n}^n}{(n + 1)}$$</p>
<h3 id="简单博弈论"><a href="#简单博弈论" class="headerlink" title="简单博弈论"></a>简单博弈论</h3><h4 id="NIM游戏"><a href="#NIM游戏" class="headerlink" title="NIM游戏"></a>NIM游戏</h4><p>给定N堆物品，第i堆物品有$A_i$个。两名玩家轮流行动，每次可以任选一堆，取走任意多个物品，可把一堆取光，但不能不取。取走最后一件物品者获胜。两人都采取最优策略，问先手是否必胜。</p>
<p>我们把这种游戏称为NIM博弈。把游戏过程中面临的状态称为局面。整局游戏第一个行动的称为先手，第二个行动的称为后手。若在某一局面下无论采取何种行动，都会输掉游戏，则称该局面必败。<br>所谓采取最优策略是指，若在某一局面下存在某种行动，使得行动后对面面临必败局面，则优先采取该行动。同时，这样的局面被称为必胜。我们讨论的博弈问题一般都只考虑理想情况，即两人均无失误，都采取最优策略行动时游戏的结果。<br>NIM博弈不存在平局，只有先手必胜和先手必败两种情况。</p>
<p>定理： NIM博弈先手必胜，当且仅当 $A_1$ ^$A_2$ ^ … ^ $A_n$ !&#x3D; 0</p>
<!--符号^表示异或-->

<h4 id="公平组合游戏ICG"><a href="#公平组合游戏ICG" class="headerlink" title="公平组合游戏ICG"></a>公平组合游戏ICG</h4><p>若一个游戏满足：</p>
<p>1.由两名玩家交替行动；<br>2.在游戏进程的任意时刻，可以执行的合法行动与轮到哪名玩家无关；<br>3.不能行动的玩家判负；<br>则称该游戏为一个公平组合游戏。<br>NIM博弈属于公平组合游戏，但城建的棋类游戏，比如围棋，就不是公平组合游戏。因为围棋交战双方分别只能落黑子和白子，胜负判定也比较复杂，不满足条件2和条件3。</p>
<h4 id="有向图游戏"><a href="#有向图游戏" class="headerlink" title="有向图游戏"></a>有向图游戏</h4><p>给定一个有向无环图，图中有一个唯一的起点，在起点上放有一枚棋子。两名玩家交替地把这枚棋子沿有向边进行移动，每次可以移动一步，无法移动者判负。该游戏被称为有向图游戏。<br>任何一个公平组合游戏都可以转化为有向图游戏。具体方法是，把每个局面看成图中的一个节点，并且从每个局面向沿着合法行动能够到达的下一个局面连有向边。</p>
<h4 id="Mex运算"><a href="#Mex运算" class="headerlink" title="Mex运算"></a>Mex运算</h4><p>设S表示一个非负整数集合。定义$mex(S)$为求出不属于集合S的最小非负整数的运算，即：<br>$mex(S) &#x3D; min(x)$, x属于自然数，且x不属于S</p>
<h4 id="SG函数"><a href="#SG函数" class="headerlink" title="SG函数"></a>SG函数</h4><p>在有向图游戏中，对于每个节点x，设从x出发共有k条有向边，分别到达节点$y_1, y_2, …, y_k$，定义$$SG(x)$$为x的后继节点$y_1, y_2, …, y_k$ 的SG函数值构成的集合再执行$$mex(S)$$运算的结果，即：<br>$SG(x) &#x3D; mex({SG(y_1), SG(y_2), …, SG(y_k)})$<br>特别地，整个有向图游戏G的SG函数值被定义为有向图游戏起点s的SG函数值，即$SG(G) &#x3D; SG(s)$。</p>
<h4 id="有向图游戏的和"><a href="#有向图游戏的和" class="headerlink" title="有向图游戏的和"></a>有向图游戏的和</h4><p>设$$G_1, G_2, …, G_m $$是m个有向图游戏。定义有向图游戏G，它的行动规则是任选某个有向图游戏$G_i$，并在$G_i$上行动一步。G被称为有向图游戏$$G_1, G_2, …, G_m$$的和。<br>有向图游戏的和的SG函数值等于它包含的各个子游戏SG函数值的异或和，即：<br>$SG(G) &#x3D; SG(G_1)$ ^ $SG(G_2) $^ … ^ $SG(G_m)$</p>
<p>定理<br>有向图游戏的某个局面必胜，当且仅当该局面对应节点的SG函数值大于0。<br>有向图游戏的某个局面必败，当且仅当该局面对应节点的SG函数值等于0。</p>
<h2 id="图论"><a href="#图论" class="headerlink" title="图论"></a>图论</h2><p>图论的题核心在与如何将题目的图建出来。</p>
<h3 id="树与图的存储"><a href="#树与图的存储" class="headerlink" title="树与图的存储"></a>树与图的存储</h3><p>树是一种特殊的图，与图的存储方式相同</p>
<p>无向图是可以看成存了两个方向的有向图</p>
<p>对于无向图中的边ab,存储两条有向边a-&gt;b,b-&gt;a</p>
<p><strong>稠密图用邻接矩阵，稀疏图用邻接表</strong></p>
<h4 id="邻接矩阵"><a href="#邻接矩阵" class="headerlink" title="邻接矩阵"></a>邻接矩阵</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++">g[a][b] <span class="hljs-comment">//存储边a-&gt;b</span><br></code></pre></td></tr></table></figure>

<h4 id="邻接表"><a href="#邻接表" class="headerlink" title="邻接表"></a>邻接表</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-comment">//对于每个点k,开一个单链表，存储k所有可以走到的点。h[k]存储这个单链表的头节点</span><br><span class="hljs-type">int</span> h[N],e[N],ne[N],idx;<br><br><span class="hljs-comment">//添加一条边a-&gt;b</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">insert</span><span class="hljs-params">(<span class="hljs-type">int</span> a,<span class="hljs-type">int</span> b)</span></span><br><span class="hljs-function"></span>&#123;<br>    e[idx]=b,ne[idx]=h[a],h[a]=idx++;<br>&#125;<br><br><span class="hljs-comment">//初始化</span><br>idx=<span class="hljs-number">0</span>;<br><span class="hljs-built_in">memset</span>(h,<span class="hljs-number">-1</span>,<span class="hljs-keyword">sizeof</span> h);<br></code></pre></td></tr></table></figure>

<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330110648123.png"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330104035427.png"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330104125039.png" alt="image-20240330104125039"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330104301116.png" alt="image-20240330104301116"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330105525376.png" alt="image-20240330105525376"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330105714998.png" alt="image-20240330105714998"></p>
<h3 id="树与图的遍历"><a href="#树与图的遍历" class="headerlink" title="树与图的遍历"></a>树与图的遍历</h3><p>时间复杂度O(n+m),n表示点数，m表示边数</p>
<h4 id="深度优先搜索-DFS"><a href="#深度优先搜索-DFS" class="headerlink" title="深度优先搜索(DFS)"></a>深度优先搜索(DFS)</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">dfs</span><span class="hljs-params">(<span class="hljs-type">int</span> u)</span></span><br><span class="hljs-function"></span>&#123;<br>    vis[u]=<span class="hljs-literal">true</span>;<span class="hljs-comment">//vis[u]表示点u已经被遍历过</span><br>    <span class="hljs-comment">//点u就是被搜到的点</span><br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=h[u];i!=<span class="hljs-number">-1</span>;i=ne[i])<br>    &#123;&#123;<span class="hljs-comment">//遍历邻接表</span><br>        <span class="hljs-type">int</span> j=e[i];<br>        <span class="hljs-keyword">if</span>(!vis[j]) <span class="hljs-built_in">dfs</span>(j);<span class="hljs-comment">//没搜过的结点就继续dfs</span><br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>

<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330112444523.png" alt="image-20240330112444523"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330112707567.png" alt="image-20240330112707567"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330112927066.png" alt="image-20240330112927066"></p>
<p><strong>迷宫问题</strong></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330113258853.png" alt="image-20240330113258853"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330113407706.png" alt="image-20240330113407706"></p>
<p>走迷宫</p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330115306923.png" alt="image-20240330115306923"></p>
<p><strong>跳马问题</strong></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330113652507.png" alt="image-20240330113652507"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330113751250.png" alt="image-20240330113751250"></p>
<p><strong>八皇后问题</strong></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330114033809.png" alt="image-20240330114033809"></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330114118779.png" alt="image-20240330114118779"></p>
<p>思路1</p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330114832803.png" alt="image-20240330114832803"></p>
<p>思路2</p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330115026016.png" alt="image-20240330115026016"></p>
<h4 id="宽度优先搜索-BFS"><a href="#宽度优先搜索-BFS" class="headerlink" title="宽度优先搜索(BFS)"></a>宽度优先搜索(BFS)</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++">queue&lt;<span class="hljs-type">int</span>&gt; q;<br>vis[<span class="hljs-number">1</span>]=<span class="hljs-literal">true</span>;<span class="hljs-comment">//表示1号点已经被遍历过</span><br>q.<span class="hljs-built_in">push</span>(<span class="hljs-number">1</span>);<span class="hljs-comment">//标记该点被搜，并入队</span><br><br><span class="hljs-keyword">while</span>(q.<span class="hljs-built_in">size</span>())<br>&#123;<span class="hljs-comment">//队列不空就一直循环</span><br>    <span class="hljs-type">int</span> t=q.<span class="hljs-built_in">front</span>();<span class="hljs-comment">//队头就是搜的点</span><br>    q.<span class="hljs-built_in">pop</span>();<span class="hljs-comment">//取出顶点，处理下一个点</span><br>    <br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=h[t];i!=<span class="hljs-number">-1</span>;i=ne[i])<br>    &#123;<span class="hljs-comment">//遍历邻接表</span><br>        <span class="hljs-type">int</span> j=e[i];<br>        <span class="hljs-keyword">if</span>(!vis[j])<br>        &#123;<span class="hljs-comment">//没搜过的结点就入队</span><br>            vis[j]=<span class="hljs-literal">true</span>;<span class="hljs-comment">//表示点j已经被遍历过</span><br>            q.<span class="hljs-built_in">push</span>(j);<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>



<h3 id="拓扑排序"><a href="#拓扑排序" class="headerlink" title="拓扑排序"></a>拓扑排序</h3><p>首先要确定图是有向无环图才有拓扑序</p>
<p>时间复杂度O(n+m)</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">topsort</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> hh=<span class="hljs-number">0</span>,tt=<span class="hljs-number">-1</span>;<br>    <br>    <span class="hljs-comment">//d[i]存储点的入度</span><br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=n;i++)<br>    &#123;<br>        <span class="hljs-keyword">if</span>(!d[i]) q[++tt]=i;<br>    &#125;<br>    <br>    <span class="hljs-keyword">while</span>(hh&lt;=tt)<br>    &#123;<br>        <span class="hljs-type">int</span> t=q[hh++];<br>        <br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=h[t];i!=<span class="hljs-number">-1</span>;i=ne[i])<br>        &#123;<br>            <span class="hljs-type">int</span> j=ne[i];<br>            <span class="hljs-keyword">if</span>(--d[j]==<span class="hljs-number">0</span>) q[++tt]=j;<br>        &#125;<br>    &#125;<br>    <br>    <span class="hljs-comment">//如果所有点都入队了，说明存在拓扑序，否则不存在</span><br>    <span class="hljs-keyword">return</span> tt=n<span class="hljs-number">-1</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="朴素拓扑排序"><a href="#朴素拓扑排序" class="headerlink" title="朴素拓扑排序"></a>朴素拓扑排序</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-meta">#<span class="hljs-keyword">include</span><span class="hljs-string">&lt;iostream&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span><span class="hljs-string">&lt;cstring&gt;</span></span><br><span class="hljs-keyword">using</span> <span class="hljs-keyword">namespace</span> std;<br><span class="hljs-comment">//拓扑排序模板</span><br><span class="hljs-type">const</span> <span class="hljs-type">int</span> N = <span class="hljs-number">100010</span>;<br><span class="hljs-type">int</span> n, m;<br><span class="hljs-type">int</span> h[N], e[N], ne[N], idx = <span class="hljs-number">0</span>;<br><span class="hljs-type">int</span> q[N], d[N];<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">add</span><span class="hljs-params">(<span class="hljs-type">int</span> a,<span class="hljs-type">int</span> b)</span></span><br><span class="hljs-function"></span>&#123;<br>    e[idx] = b, ne[idx] = h[a], h[a] = idx++;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">init</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    idx = <span class="hljs-number">0</span>;<br>    <span class="hljs-built_in">memset</span>(h, <span class="hljs-number">-1</span>, <span class="hljs-keyword">sizeof</span> h);<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">topsort</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> hh=<span class="hljs-number">0</span>,tt=<span class="hljs-number">-1</span>;<br><br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= n; i++)<br>    &#123;<span class="hljs-comment">//看题目的结点是从0开始还是从1开始</span><br>        <span class="hljs-comment">//cout&lt;&lt;d[i]&lt;&lt;&quot; &quot;;</span><br>        <span class="hljs-keyword">if</span> (!d[i])<br>        &#123;<br>            q[++tt] = i;<br>            <span class="hljs-comment">//cout&lt;&lt;i&lt;&lt;&quot; &quot;;</span><br>        &#125;<br>        <br>    &#125;<br><br>    <span class="hljs-keyword">while</span> (hh&lt;=tt)<br>    &#123;<br>        <span class="hljs-type">int</span> t = q[hh++];<br>        <span class="hljs-comment">//cout&lt;&lt;t&lt;&lt;&quot; &quot;;</span><br><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = h[t]; i != <span class="hljs-number">-1</span>; i=ne[i])<br>        &#123;<br>            <span class="hljs-type">int</span> j = e[i];<br>            <span class="hljs-keyword">if</span> (--d[j]==<span class="hljs-number">0</span>)<br>            &#123;<br>                q[++tt] = j;<br>            &#125;<br>            <br>        &#125;<br>        <br>    &#125;<br>    <span class="hljs-comment">// return tt=n-1;</span><br>    <span class="hljs-comment">//如果需要判断有无拓扑序</span><br>&#125;<br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-built_in">init</span>();<br>    cin &gt;&gt; n &gt;&gt; m;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= m; i++)<br>    &#123;<br>        <span class="hljs-type">int</span> a, b;<br>        cin &gt;&gt; a &gt;&gt; b;<br>        <span class="hljs-built_in">add</span>(a, b);<br>        d[b]++;<br>    &#125;<br>    <span class="hljs-comment">/*</span><br><span class="hljs-comment">    for (auto i:d)</span><br><span class="hljs-comment">    &#123;</span><br><span class="hljs-comment">        cout&lt;&lt;i&lt;&lt;&quot; &quot;;</span><br><span class="hljs-comment">    &#125;</span><br><span class="hljs-comment">    */</span><br>    <span class="hljs-built_in">topsort</span>();<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; i++) <br>    &#123;<br>        cout &lt;&lt; q[i] &lt;&lt; <span class="hljs-string">&quot; &quot;</span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="字典序拓扑排序"><a href="#字典序拓扑排序" class="headerlink" title="字典序拓扑排序"></a>字典序拓扑排序</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;iostream&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;cstring&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;queue&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;vector&gt;</span></span><br><span class="hljs-meta">#<span class="hljs-keyword">include</span> <span class="hljs-string">&lt;algorithm&gt;</span></span><br><span class="hljs-keyword">using</span> <span class="hljs-keyword">namespace</span> std;<br><span class="hljs-comment">//字典序拓扑排序</span><br><span class="hljs-type">const</span> <span class="hljs-type">int</span> N = <span class="hljs-number">100010</span>;<br><span class="hljs-type">int</span> n, m;<br><span class="hljs-type">int</span> h[N], e[N], ne[N], idx = <span class="hljs-number">0</span>;<br><span class="hljs-type">int</span> d[N];<br>vector&lt;<span class="hljs-type">int</span>&gt; ans;<br>priority_queue&lt;<span class="hljs-type">int</span>, vector&lt;<span class="hljs-type">int</span>&gt;, greater&lt;<span class="hljs-type">int</span>&gt;&gt; pq;  <span class="hljs-comment">// 优先队列，按照从小到大的顺序出队</span><br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">add</span><span class="hljs-params">(<span class="hljs-type">int</span> a, <span class="hljs-type">int</span> b)</span></span><br><span class="hljs-function"></span>&#123;<br>    e[idx] = b, ne[idx] = h[a], h[a] = idx++;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">init</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    idx = <span class="hljs-number">0</span>;<br>    <span class="hljs-built_in">memset</span>(h, <span class="hljs-number">-1</span>, <span class="hljs-keyword">sizeof</span> h);<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">topsort</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">0</span>; i &lt; n; i++)<br>    &#123;<br>        <span class="hljs-keyword">if</span> (!d[i])<br>        &#123;<br>            pq.<span class="hljs-built_in">push</span>(i);<br>        &#125;<br>    &#125;<br><br>    <span class="hljs-keyword">while</span> (!pq.<span class="hljs-built_in">empty</span>())<br>    &#123;<br>        <span class="hljs-type">int</span> t = pq.<span class="hljs-built_in">top</span>();<br>        pq.<span class="hljs-built_in">pop</span>();<br>        <span class="hljs-comment">//t就是拓扑序列的数</span><br>        cout &lt;&lt; t &lt;&lt; <span class="hljs-string">&quot; &quot;</span>;<br>        <span class="hljs-comment">// ans.push_back(t);</span><br>        <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = h[t]; i != <span class="hljs-number">-1</span>; i = ne[i])<br>        &#123;<br>            <span class="hljs-type">int</span> j = e[i];<br>            <span class="hljs-keyword">if</span> (--d[j] == <span class="hljs-number">0</span>)<br>            &#123;<br>                pq.<span class="hljs-built_in">push</span>(j);<br>            &#125;<br>        &#125;<br>    &#125;<br>    <span class="hljs-comment">// return ans.size()==n;</span><br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-built_in">init</span>();<br>    cin &gt;&gt; n &gt;&gt; m;<br>    <span class="hljs-keyword">for</span> (<span class="hljs-type">int</span> i = <span class="hljs-number">1</span>; i &lt;= m; i++)<br>    &#123;<br>        <span class="hljs-type">int</span> a, b;<br>        cin &gt;&gt; a &gt;&gt; b;<br>        <span class="hljs-built_in">add</span>(a, b);<br>        d[b]++;<br>    &#125;<br><br>    <span class="hljs-built_in">topsort</span>();<br><br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure>





<h3 id="最短路"><a href="#最短路" class="headerlink" title="最短路"></a>最短路</h3><p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240325111736784.png" alt="image-20240325111736784"></p>
<h4 id="朴素dijkstra算法"><a href="#朴素dijkstra算法" class="headerlink" title="朴素dijkstra算法"></a>朴素dijkstra算法</h4><p>基于贪心</p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240325112518533.png" alt="image-20240325112518533"></p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> g[N][N];<span class="hljs-comment">//存储每条边</span><br><span class="hljs-type">int</span> dist[N];<span class="hljs-comment">//存储1号点到每个点的最短距离</span><br><span class="hljs-type">bool</span> vis[N];<span class="hljs-comment">//存储每个点的最短路是否已经确定</span><br><br><span class="hljs-comment">//求1号点到n号点的最短路，不存在则返回-1</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">dijkstra</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-built_in">memset</span>(dist,<span class="hljs-number">0x3f</span>,<span class="hljs-keyword">sizeof</span> dist);<br>    dist[<span class="hljs-number">1</span>]=<span class="hljs-number">0</span>;<br>    <br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;n<span class="hljs-number">-1</span>;i++)<br>    &#123;<br>        <span class="hljs-type">int</span> t=<span class="hljs-number">-1</span>;<span class="hljs-comment">//在还未确定最短路的点中寻找距离最小的点</span><br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">1</span>;j&lt;=n;j++)<br>        &#123;<br>            <span class="hljs-keyword">if</span>(!vis[j]&amp;&amp;t==<span class="hljs-number">-1</span>||dist[t]&gt;dist[j]) t=j;<br>        &#125;<br>        <br>        <span class="hljs-comment">//用t更新其他点的距离</span><br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">1</span>;j&lt;=n;j++)<br>        &#123;<br>            dist[j]=<span class="hljs-built_in">min</span>(dist[j],dist[t]+g[t][j]);<br>        &#125;<br>        <br>        vis[t]=<span class="hljs-literal">true</span>;<br>    &#125;<br>    <br>    <span class="hljs-keyword">if</span>(dist[n]==<span class="hljs-number">0x3f3f3f3f</span>) <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<br>    <span class="hljs-keyword">return</span> dist[n];<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="堆优化版dijkstra算法"><a href="#堆优化版dijkstra算法" class="headerlink" title="堆优化版dijkstra算法"></a>堆优化版dijkstra算法</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-keyword">typedef</span> pair&lt;<span class="hljs-type">int</span>,<span class="hljs-type">int</span>&gt; PII;<br><br><span class="hljs-type">int</span> n;<span class="hljs-comment">//点的数量</span><br><span class="hljs-type">int</span> h[N],w[N],e[N],ne[N],idx;<span class="hljs-comment">//邻接表存所有边</span><br><span class="hljs-type">int</span> dist[N];<span class="hljs-comment">//存储所有点到1号点的距离</span><br><span class="hljs-type">bool</span> vis[N];<span class="hljs-comment">//存储每个点的最短距离是否已确定</span><br><br><span class="hljs-comment">//求1号点到n号点的最短距离，如果不存在，返回-1</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">dijkstra</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-built_in">memset</span>(dist,<span class="hljs-number">0x3f</span>,<span class="hljs-keyword">sizeof</span> dist);<br>    dist[<span class="hljs-number">1</span>]=<span class="hljs-number">0</span>;<br>    priority_queue&lt;PII,vector&lt;PII&gt;,greater&lt;PII&gt;&gt; heap;<br>    heap.<span class="hljs-built_in">push</span>(&#123;<span class="hljs-number">0</span>,<span class="hljs-number">1</span>&#125;);<span class="hljs-comment">//first存储距离，second存储节点编号</span><br>    <br>    <span class="hljs-keyword">while</span>(heap.<span class="hljs-built_in">size</span>())<br>    &#123;<br>        <span class="hljs-keyword">auto</span> t=heap.<span class="hljs-built_in">top</span>();<br>        heap.<span class="hljs-built_in">pop</span>();<br>        <br>        <span class="hljs-type">int</span> ver=t.second,distance=t.first;<br>        <br>        <span class="hljs-keyword">if</span>(!vis[ver]) <span class="hljs-keyword">continue</span>;<br>        vis[ver]=<span class="hljs-literal">true</span>;<br>        <br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=h[ver];i!=<span class="hljs-number">-1</span>;i=ne[i])<br>        &#123;<br>            <span class="hljs-type">int</span> j=e[i];<br>            <span class="hljs-keyword">if</span>(dist[j]&gt;distance+w[i])<br>            &#123;<br>                dist[i]=distance+w[i];<br>                heap.<span class="hljs-built_in">push</span>(&#123;dist[j],j&#125;);<br>            &#125;<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">if</span>(dist[n]==<span class="hljs-number">0x3f3f3f3f</span>) <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<br>    <span class="hljs-keyword">return</span> dist[n];<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="Bellman-Ford算法"><a href="#Bellman-Ford算法" class="headerlink" title="Bellman-Ford算法"></a>Bellman-Ford算法</h4><p>基于动态规划</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> n,m;<span class="hljs-comment">//n表示点数，m表示边数</span><br><span class="hljs-type">int</span> dist[N];<span class="hljs-comment">//dist[x]存储1到x的最短路距离</span><br><br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Edge</span><span class="hljs-comment">//边，a表示出点，b表示入点，w表示边的权重</span><br>&#123;<br>    <span class="hljs-type">int</span> a,b,w;<br>&#125;edges[M];<br><br><span class="hljs-comment">//求1到n的最短路距离，如果无法走到，则返回-1</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">bellman_ford</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-built_in">memset</span>(dist,<span class="hljs-number">0x3f</span>,<span class="hljs-keyword">sizeof</span> dist);<br>    dist[<span class="hljs-number">1</span>]=<span class="hljs-number">0</span>;<br>    <br>    <span class="hljs-comment">//如果第n次迭代任然会松弛三角不等式，就说明存在一条长度是n+1的最短路径。</span><br>    <span class="hljs-comment">//由抽屉原理，路径中至少存在两个相同的点，说明图中存在负权回路</span><br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;n;i++)<br>    &#123;<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">0</span>;j&lt;m;j++)<br>        &#123;<br>            <span class="hljs-type">int</span> a=edges[j].a,b=edges[j].b,w=edges[j].w;<br>            <span class="hljs-keyword">if</span>(dist[b]&gt;dist[a]+w)<br>            &#123;<br>                dist[b]=dist[a]+w;<br>            &#125;<br>        &#125;<br>    &#125;<br>    <br>    <span class="hljs-keyword">if</span>(dist[n]&gt;<span class="hljs-number">0x3f3f3f3f</span>/<span class="hljs-number">2</span>) <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<br>    <span class="hljs-keyword">return</span> dist[n];<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="spfa算法-队列优化的Bellman-Ford"><a href="#spfa算法-队列优化的Bellman-Ford" class="headerlink" title="spfa算法(队列优化的Bellman-Ford)"></a>spfa算法(队列优化的Bellman-Ford)</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-type">int</span> n;<span class="hljs-comment">//总点数</span><br><span class="hljs-type">int</span> h[N],w[N],e[N],ne[N],idx;<span class="hljs-comment">//邻接表存储所有边</span><br><span class="hljs-type">int</span> dist[N];<span class="hljs-comment">//存储每个点到1号点的最短距离</span><br><span class="hljs-type">bool</span> vis[N];<span class="hljs-comment">//存储每个点是否在队列中</span><br><br><span class="hljs-comment">//求1号点到n号点的最短路距离，如果从1号点无法走到n号点，返回-1</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">spfa</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-built_in">memset</span>(dist,<span class="hljs-number">0x3f</span>,<span class="hljs-keyword">sizeof</span> dist);<br>    dist[<span class="hljs-number">1</span>]=<span class="hljs-number">0</span>;<br>    <br>    queue&lt;<span class="hljs-type">int</span>&gt; q;<br>    q.<span class="hljs-built_in">push</span>(<span class="hljs-number">1</span>);<br>    vis[<span class="hljs-number">1</span>]=<span class="hljs-literal">true</span>;<br>    <br>    <span class="hljs-keyword">while</span>(q.<span class="hljs-built_in">size</span>())<br>    &#123;<br>        <span class="hljs-keyword">auto</span> t=q.<span class="hljs-built_in">front</span>();<br>        q.<span class="hljs-built_in">pop</span>();<br>        <br>        vis[t]=<span class="hljs-literal">false</span>;<br>        <br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=h[t];i!=<span class="hljs-number">-1</span>;i=ne[i])<br>        &#123;<br>            <span class="hljs-type">int</span> j=e[i];<br>            <span class="hljs-keyword">if</span>(dist[j]&gt;dist[t]+w[i])<br>            &#123;<br>                dist[j]=dist[t]+w[i];<br>                <span class="hljs-keyword">if</span>(!vis[j])<span class="hljs-comment">//如果队列中已存在j,则不需要重复将j插入</span><br>                &#123;<br>                    q.<span class="hljs-built_in">push</span>(j);<br>                    vis[j]=<span class="hljs-literal">true</span>;<br>                &#125;<br>            &#125;<br>        &#125;<br>    &#125;<br>    <br>    <span class="hljs-keyword">if</span>(dist[n]==<span class="hljs-number">0x3f3f3f3f</span>) <span class="hljs-keyword">return</span> <span class="hljs-number">-1</span>;<br>    <span class="hljs-keyword">return</span> dist[n];<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="spfa判断图中是否存在负环"><a href="#spfa判断图中是否存在负环" class="headerlink" title="spfa判断图中是否存在负环"></a>spfa判断图中是否存在负环</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> n;<span class="hljs-comment">//总点数</span><br><span class="hljs-type">int</span> h[N],w[N],e[N],ne[N],idx;<span class="hljs-comment">//邻接表存所有边</span><br><span class="hljs-type">int</span> dist[N],cnt[N];<span class="hljs-comment">//dist[]存储1到x的最短路距离，cnt[]存储1到x的最短路中经过的点数</span><br><span class="hljs-type">bool</span> vis[N];<span class="hljs-comment">//存储每个点是否在队列中</span><br><br><span class="hljs-comment">//如果存在负环，返回true，否则返回false</span><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">spfa</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-comment">//不需要初始化dist数组</span><br>    <span class="hljs-comment">//原理：如果某条最短路上有n个点(除了自己)，那么加上自己一共有n+1点，由抽屉原理，一定有两个点相同，所有存在环。</span><br>    <br>    queue&lt;<span class="hljs-type">int</span>&gt; q;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=n;i++)<br>    &#123;<br>        q.<span class="hljs-built_in">push</span>(i);<br>        vis[i]=<span class="hljs-literal">true</span>;<br>    &#125;<br>    <br>    <span class="hljs-keyword">while</span>(q.<span class="hljs-built_in">size</span>())<br>    &#123;<br>        <span class="hljs-keyword">auto</span> t=q.<span class="hljs-built_in">front</span>();<br>        q.<span class="hljs-built_in">pop</span>();<br>        <br>        vis[t]=<span class="hljs-literal">false</span>;<br>        <br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=h[t];i!=<span class="hljs-number">-1</span>;i=ne[i])<br>        &#123;<br>            <span class="hljs-type">int</span> j=e[i];<br>            <span class="hljs-keyword">if</span>(dist[j]&gt;dist[t]+w[i])<br>            &#123;<br>                dist[j]=dist[t]+w[i];<br>                cnt[j]=cnt[t]+<span class="hljs-number">1</span>;<br>                <span class="hljs-keyword">if</span>(cnt[j]&gt;=n) <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<span class="hljs-comment">//如果从1到x的最短路中包含至少n个点(不包括自己)，则说明存在环</span><br>                <span class="hljs-keyword">if</span>(!vis[j])<br>                &#123;<br>                    q.<span class="hljs-built_in">push</span>(j);<br>                    vis[j]=<span class="hljs-literal">true</span>;<br>                &#125;<br>            &#125;<br>        &#125;<br>    &#125;<br>    <br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="Floyd算法"><a href="#Floyd算法" class="headerlink" title="Floyd算法"></a>Floyd算法</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//初始化</span><br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=n;i++)<br>&#123;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">1</span>;j&lt;=n;j++)<br>    &#123;<br>        <span class="hljs-keyword">if</span>(i==j) d[i][j]=<span class="hljs-number">0</span>;<br>        <span class="hljs-keyword">else</span> d[i][j]=INF;<span class="hljs-comment">//INF是一个极大的数</span><br>    &#125;<br>&#125;<br><br><span class="hljs-comment">//算法结束后，d[a][b]表示a到b的最短距离</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">floyd</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> k=<span class="hljs-number">1</span>;k&lt;=n;k++)<br>    &#123;<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=n;i++)<br>        &#123;<br>            <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">1</span>;j&lt;=n;j++)<br>            &#123;<br>                d[i][j]=<span class="hljs-built_in">min</span>(d[i][j],d[i][k]+d[k][j]);<br>            &#125;<br>        &#125;<br>    &#125;<br>&#125;<br></code></pre></td></tr></table></figure>



<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\ce9cfa74-2c40-47b1-b7e4-1f923b3d50ab-3350490.jpg" alt="image"></p>
<h3 id="最小生成树"><a href="#最小生成树" class="headerlink" title="最小生成树"></a>最小生成树</h3><h4 id="Prim算法"><a href="#Prim算法" class="headerlink" title="Prim算法"></a>Prim算法</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> n;<span class="hljs-comment">//n表示点数</span><br><span class="hljs-type">int</span> g[N][N];<span class="hljs-comment">//邻接矩阵，存储所有边</span><br><span class="hljs-type">int</span> dist[N];<span class="hljs-comment">//存储其他点到当前最小生成树的距离</span><br><span class="hljs-type">bool</span> vis[N];<span class="hljs-comment">//存储每个点是否已经在生成树中</span><br><br><span class="hljs-comment">//如果图不联通，返回INF(0x3f3f3f3f),否则返回最小生成树的树边权重之和</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">prim</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-built_in">memset</span>(dist,<span class="hljs-number">0x3f</span>,<span class="hljs-keyword">sizeof</span> dist);<br>    <br>    <span class="hljs-type">int</span> res=<span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;n;i++)<br>    &#123;<br>        <span class="hljs-type">int</span> t=<span class="hljs-number">-1</span>;<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">1</span>;j&lt;=n;j++)<br>        &#123;<br>            <span class="hljs-keyword">if</span>(!vis[j]&amp;&amp;(t==<span class="hljs-number">-1</span>||dist[t]&gt;dist[j])) t=j;<br>        &#125;<br>        <span class="hljs-keyword">if</span>(i&amp;&amp;dist[t]==INF) <span class="hljs-keyword">return</span> INF;<br>        <br>        <span class="hljs-keyword">if</span>(i) res+=dist[t];<br>        vis[t]=<span class="hljs-literal">true</span>;<br>        <br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">1</span>;j&lt;=n;j++) dist[j]=<span class="hljs-built_in">min</span>(dist[j],g[t][j]);<br>    &#125;<br>    <br>    <span class="hljs-keyword">return</span> res;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="Kruskal算法"><a href="#Kruskal算法" class="headerlink" title="Kruskal算法"></a>Kruskal算法</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> n,m;<span class="hljs-comment">//n是点数，m是边数</span><br><span class="hljs-type">int</span> p[N];<span class="hljs-comment">//并查集父节点数组</span><br><br><span class="hljs-keyword">struct</span> <span class="hljs-title class_">Edge</span><span class="hljs-comment">//存储边</span><br>&#123;<br>    <span class="hljs-type">int</span> a,b,w;<br>    <br>    <span class="hljs-type">bool</span> <span class="hljs-keyword">operator</span>&lt;(<span class="hljs-type">const</span> Edge &amp;W)<span class="hljs-type">const</span>&#123;<br>        <span class="hljs-keyword">return</span> w&lt;W.w;<br>    &#125;<br>&#125;edges[M];<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">find</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span><span class="hljs-comment">//并查集核心操作</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">if</span>(p[x]!=x) p[x]=<span class="hljs-built_in">find</span>(p[x]);<br>    <span class="hljs-keyword">return</span> p[x];<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">kruskal</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-built_in">sort</span>(edges,edges+m);<br>    <br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=n;i++) p[i]=i;<span class="hljs-comment">//并查集初始化</span><br>    <br>    <span class="hljs-type">int</span> res=<span class="hljs-number">0</span>,cnt=<span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;m;i++)<br>    &#123;<br>        <span class="hljs-type">int</span> a=edges[i].a,b=edges[i].b,w=edges[i].w;<br>        <br>        a=<span class="hljs-built_in">find</span>(a),b=<span class="hljs-built_in">find</span>(b);<br>        <span class="hljs-keyword">if</span>(a!=b)<span class="hljs-comment">//如果两个连通块不连通，就合并</span><br>        &#123;<br>            p[a]=b;<br>            res+=w;<br>            cnt++;<br>        &#125;<br>    &#125;<br>    <br>    <span class="hljs-keyword">if</span>(cnt&lt;n<span class="hljs-number">-1</span>) <span class="hljs-keyword">return</span> INF;<br>    <span class="hljs-keyword">return</span> res;<br>&#125;<br></code></pre></td></tr></table></figure>



<h3 id="二分图"><a href="#二分图" class="headerlink" title="二分图"></a>二分图</h3><h4 id="染色法判别二分图"><a href="#染色法判别二分图" class="headerlink" title="染色法判别二分图"></a>染色法判别二分图</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> n;<span class="hljs-comment">//n表示点数</span><br><span class="hljs-type">int</span> h[N],e[N],ne[N],idx;<span class="hljs-comment">//邻接表存储图</span><br><span class="hljs-type">int</span> color[N];<span class="hljs-comment">//表示每个点的颜色，-1表示未染色，0表示白色，1表示黑色</span><br><br><span class="hljs-comment">//u表示当前节点，c表示当前点的颜色</span><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">dfs</span><span class="hljs-params">(<span class="hljs-type">int</span> u,<span class="hljs-type">int</span> c)</span></span><br><span class="hljs-function"></span>&#123;<br>    color[u]=c;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=h[u];i!=<span class="hljs-number">-1</span>;i=ne[i])<br>    &#123;<br>        <span class="hljs-type">int</span> j=e[i];<br>        <span class="hljs-keyword">if</span>(color[j]==<span class="hljs-number">-1</span>)<br>        &#123;<br>            <span class="hljs-keyword">if</span>(!<span class="hljs-built_in">dfs</span>(j,!c)) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>        &#125;<br>        <span class="hljs-keyword">else</span> <span class="hljs-keyword">if</span>(color[j]==c) <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>    &#125;<br>    <br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>&#125;<br><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">check</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-built_in">memset</span>(color,<span class="hljs-number">-1</span>,<span class="hljs-keyword">sizeof</span> color);<br>    <span class="hljs-type">bool</span> flag=<span class="hljs-literal">true</span>;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=n;i++)<br>    &#123;<br>        <span class="hljs-keyword">if</span>(!<span class="hljs-built_in">dfs</span>(i,<span class="hljs-number">0</span>))<br>        &#123;<br>            flag=<span class="hljs-literal">false</span>;<br>            <span class="hljs-keyword">break</span>;<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">return</span> flag;<br>&#125;<br></code></pre></td></tr></table></figure>



<h4 id="匈牙利算法"><a href="#匈牙利算法" class="headerlink" title="匈牙利算法"></a>匈牙利算法</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-type">int</span> n1,n2;<span class="hljs-comment">//n1表示第一个集合中的点数，n2表示第二个集合中的点数</span><br><span class="hljs-type">int</span> h[N],e[N],ne[N],idx;<span class="hljs-comment">//邻接表 存储所有边，匈牙利算法只会用到第二个集合指向第一个集合的边，所以只用存一个方向的边</span><br><span class="hljs-type">int</span> match[N];<span class="hljs-comment">//存储第二个集合中每个点当前匹配的第一个集合中的点是哪个</span><br><span class="hljs-type">bool</span> vis[N];<span class="hljs-comment">//表示第二个集合中每个点是否已经被遍历过</span><br><br><span class="hljs-function"><span class="hljs-type">bool</span> <span class="hljs-title">find</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=h[x];i!=<span class="hljs-number">-1</span>;i=ne[i])<br>    &#123;<br>        <span class="hljs-type">int</span> j=e[i];<br>        <span class="hljs-keyword">if</span>(!vis[j])<br>        &#123;<br>            vis[j]=<span class="hljs-literal">true</span>;<br>            <span class="hljs-keyword">if</span>(match[j]==<span class="hljs-number">0</span>||<span class="hljs-built_in">find</span>(match[j]))<br>            &#123;<br>                match[j]=x;<br>                <span class="hljs-keyword">return</span> <span class="hljs-literal">true</span>;<br>            &#125;<br>        &#125;<br>    &#125;<br>    <br>    <span class="hljs-keyword">return</span> <span class="hljs-literal">false</span>;<br>&#125;<br><br><span class="hljs-comment">//求最大匹配数，依次枚举第一个集合中的每一个点能否匹配第二个集合中的点</span><br><span class="hljs-type">int</span> res=<span class="hljs-number">0</span>;<br><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">1</span>;i&lt;=n1;i++)<br>&#123;<br>    <span class="hljs-built_in">memset</span>(vis,<span class="hljs-literal">false</span>,sizeod vis);<br>    <span class="hljs-keyword">if</span>(<span class="hljs-built_in">find</span>(i)) res++;<br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="强连通分量"><a href="#强连通分量" class="headerlink" title="强连通分量"></a>强连通分量</h3><h2 id="搜索"><a href="#搜索" class="headerlink" title="搜索"></a>搜索</h2><h2 id="算法思想"><a href="#算法思想" class="headerlink" title="算法思想"></a>算法思想</h2><h3 id="动态规划"><a href="#动态规划" class="headerlink" title="动态规划"></a>动态规划</h3><p>如果某一问题有很多重叠子问题，使用动态规划是最有效的。</p>
<p>动态规划中每一个状态一定是由上一个状态推导出来的。</p>
<p><strong>对于动态规划问题，拆解为如下五步曲</strong></p>
<ol>
<li>确定dp数组（dp table）以及下标的含义</li>
<li>确定递推公式</li>
<li>dp数组如何初始化</li>
<li>确定遍历顺序</li>
<li>举例推导dp数组</li>
</ol>
<p><strong>找问题的最好方式就是把dp数组打印出来，看看究竟是不是按照自己思路推导的！</strong></p>
<p><strong>分析框架</strong></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240402104638583.png" alt="image-20240402104638583"></p>
<p><strong>记忆化搜索</strong></p>
<h4 id="背包问题"><a href="#背包问题" class="headerlink" title="背包问题"></a>背包问题</h4><p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330075950562.png" alt="image-20240330075950562"></p>
<h4 id="线性DP"><a href="#线性DP" class="headerlink" title="线性DP"></a>线性DP</h4><p><strong>数字三角形模型</strong></p>
<p>摘花生</p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330075925758.png" alt="image-20240330075925758"></p>
<p><strong>最长上升子序列模型(LIS)</strong></p>
<p><img src="/2049/04/01/%E7%AE%97%E6%B3%95%E7%AB%9E%E8%B5%9B%E7%9F%A5%E8%AF%86%E6%80%BB%E7%BB%93/project\Test_Hexo\blog\source_posts\算法竞赛知识总结\image-20240330090737129.png" alt="image-20240330090737129"></p>
<h4 id="区间DP"><a href="#区间DP" class="headerlink" title="区间DP"></a>区间DP</h4><h4 id="计数类DP"><a href="#计数类DP" class="headerlink" title="计数类DP"></a>计数类DP</h4><h4 id="数位统计DP"><a href="#数位统计DP" class="headerlink" title="数位统计DP"></a>数位统计DP</h4><h4 id="状态压缩DP"><a href="#状态压缩DP" class="headerlink" title="状态压缩DP"></a>状态压缩DP</h4><h4 id="树形DP"><a href="#树形DP" class="headerlink" title="树形DP"></a>树形DP</h4><h4 id><a href="#" class="headerlink" title></a></h4><h3 id="贪心"><a href="#贪心" class="headerlink" title="贪心"></a>贪心</h3><p>单峰的情况下，才有贪心的可能性</p>
<p><strong>总选择局部最优解，以获得全局最优解</strong></p>
<p>贪心算法一般分为如下四步：</p>
<ul>
<li>将问题分解为若干个子问题</li>
<li>找出适合的贪心策略</li>
<li>求解每一个子问题的最优解</li>
<li>将局部最优解堆叠成全局最优解</li>
</ul>
<p>这个四步其实过于理论化了，平时在做贪心类的题目，很难去按照这四步去思考。</p>
<p>做题的时候，只要想清楚 局部最优 是什么，如果推导出全局最优，其实就够了。</p>
<p><strong>如何验证可不可以用贪心算法呢？</strong></p>
<p><strong>最好用的策略就是举反例，如果想不到反例，那么就试一试贪心吧</strong>。</p>
<p>如果认为举例子得出的结论不靠谱，想要严格的数学证明。</p>
<p>一般数学证明有如下两种方法：</p>
<ul>
<li>数学归纳法</li>
<li>反证法</li>
</ul>
<h3 id="枚举"><a href="#枚举" class="headerlink" title="枚举"></a>枚举</h3><p>一一枚举结果。</p>
<h3 id="分治"><a href="#分治" class="headerlink" title="分治"></a>分治</h3><p>将问题分解，分而治之。</p>
<p>将一个复杂的问题，分成两个或多个相似的子问题，在把子问题分成更小的子问题，直到更小的子问题可以简单求解，求解子问题，则原问题的解则为阿子问题解的合并。</p>
<p>当出现满足以下条件的问题：</p>
<ul>
<li>原始问题可以分成多个相似的子问题</li>
<li>子问题可以很简单的求解</li>
<li>原始问题的解是子问题解的合并</li>
<li>各个子问题是相互独立的，不包含相同的子问题</li>
</ul>
<p><strong>分治的解题策略：</strong></p>
<ul>
<li>第一步：分解，将原问题分解为若干个规模较小，相互独立，与原问题形式相同的子问题</li>
<li>第二步：解决，解决各个子问题</li>
<li>第三步：合并，将各个子问题的解合并为原问题的解</li>
</ul>
<h2 id="其他"><a href="#其他" class="headerlink" title="其他"></a>其他</h2><h3 id="高精度"><a href="#高精度" class="headerlink" title="高精度"></a>高精度</h3><h4 id="高精度加法"><a href="#高精度加法" class="headerlink" title="高精度加法"></a>高精度加法</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-function">vector&lt;<span class="hljs-type">int</span>&gt; <span class="hljs-title">add</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt; &amp;A,vector&lt;<span class="hljs-type">int</span>&gt; &amp;B)</span></span><br><span class="hljs-function"></span>&#123;<span class="hljs-comment">//C=A+B,满足A&gt;=0,B&gt;=0</span><br>    <span class="hljs-keyword">if</span>(A.<span class="hljs-built_in">size</span>()&lt;B.<span class="hljs-built_in">size</span>()) <span class="hljs-keyword">return</span> <span class="hljs-built_in">add</span>(B,A);<br>    <br>    vector&lt;<span class="hljs-type">int</span>&gt; C;<br>    <span class="hljs-type">int</span> t=<span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;A.<span class="hljs-built_in">size</span>();i++)<br>    &#123;<br>        t+=A[i];<br>        <span class="hljs-keyword">if</span>(i&lt;B.<span class="hljs-built_in">size</span>()) t+=B[i];<br>        C.<span class="hljs-built_in">push_back</span>(t%<span class="hljs-number">10</span>);<br>        t/=<span class="hljs-number">10</span>;<br>    &#125;<br>    <br>    <span class="hljs-keyword">if</span>(t) C.<span class="hljs-built_in">push_back</span>(t);<br>    <span class="hljs-keyword">return</span> C;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="高精度减法"><a href="#高精度减法" class="headerlink" title="高精度减法"></a>高精度减法</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-function">vector&lt;<span class="hljs-type">int</span>&gt; <span class="hljs-title">div</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt; &amp;A,vector&lt;<span class="hljs-type">int</span>&gt; &amp;B)</span></span><br><span class="hljs-function"></span>&#123;<span class="hljs-comment">//C=A-B,满足A&gt;=B,A&gt;=0,B&gt;=0</span><br>    vector&lt;<span class="hljs-type">int</span>&gt; C;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>,t=<span class="hljs-number">0</span>;i&lt;A.<span class="hljs-built_in">size</span>();i++)<br>    &#123;<br>        t=A[i]-t;<br>        <span class="hljs-keyword">if</span>(i&lt;B.<span class="hljs-built_in">size</span>()) t-=B[i];<br>        C.<span class="hljs-built_in">push_back</span>((t+<span class="hljs-number">10</span>)%<span class="hljs-number">10</span>);<br>        <span class="hljs-keyword">if</span>(t&lt;<span class="hljs-number">0</span>) t=<span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">else</span> t=<span class="hljs-number">0</span>;<br>    &#125;<br>    <br>    <span class="hljs-keyword">while</span>(C.<span class="hljs-built_in">size</span>()&gt;<span class="hljs-number">1</span>&amp;&amp;C.<span class="hljs-built_in">back</span>()==<span class="hljs-number">0</span>) C.<span class="hljs-built_in">pop_back</span>();<br>    <span class="hljs-keyword">return</span> C;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="高精度乘低精度"><a href="#高精度乘低精度" class="headerlink" title="高精度乘低精度"></a>高精度乘低精度</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-function">vector&lt;<span class="hljs-type">int</span>&gt; <span class="hljs-title">mul</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt; &amp;A,<span class="hljs-type">int</span> b)</span></span><br><span class="hljs-function"></span>&#123;<span class="hljs-comment">//C=A*b,满足A&gt;=0,b&gt;0</span><br>    vector&lt;<span class="hljs-type">int</span>&gt; C;<br>    <span class="hljs-type">int</span> t=<span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;A.<span class="hljs-built_in">size</span>()||t;i++)<br>    &#123;<br>        <span class="hljs-keyword">if</span>(i&lt;A.<span class="hljs-built_in">size</span>()) t+=A[i]*b;<br>        C.<span class="hljs-built_in">push_back</span>(t%<span class="hljs-number">10</span>);<br>        t/=<span class="hljs-number">10</span>;<br>    &#125;<br>    <br>    <span class="hljs-keyword">return</span> C;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="高精度除以低精度"><a href="#高精度除以低精度" class="headerlink" title="高精度除以低精度"></a>高精度除以低精度</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-function">vector&lt;<span class="hljs-type">int</span>&gt; <span class="hljs-title">div</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt; &amp;A,<span class="hljs-type">int</span> b,<span class="hljs-type">int</span> &amp;r)</span></span><br><span class="hljs-function"></span>&#123;<span class="hljs-comment">//A/b=C...r,满足A&gt;=0,b&gt;0</span><br>    vector&lt;<span class="hljs-type">int</span>&gt; C;<br>    r=<span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=A.<span class="hljs-built_in">size</span>()<span class="hljs-number">-1</span>;i&gt;=<span class="hljs-number">0</span>;i--)<br>    &#123;<br>        r=r*<span class="hljs-number">10</span>+A[i];<br>        C.<span class="hljs-built_in">push_back</span>(r/b);<br>        r%=b;<br>    &#125;<br>    <span class="hljs-built_in">reverse</span>(C.<span class="hljs-built_in">begin</span>(),C.<span class="hljs-built_in">end</span>());<br>    <span class="hljs-keyword">while</span>(C.<span class="hljs-built_in">size</span>()&gt;<span class="hljs-number">1</span>&amp;&amp;C.<span class="hljs-built_in">back</span>()==<span class="hljs-number">0</span>) C.<span class="hljs-built_in">pop_back</span>();<br>    <span class="hljs-keyword">return</span> C;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="高精度乘高精度"><a href="#高精度乘高精度" class="headerlink" title="高精度乘高精度"></a>高精度乘高精度</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-function">vector&lt;<span class="hljs-type">int</span>&gt; <span class="hljs-title">mul</span><span class="hljs-params">(vector&lt;<span class="hljs-type">int</span>&gt; &amp;A,vector&lt;<span class="hljs-type">int</span>&gt; &amp;B)</span></span><br><span class="hljs-function"></span>&#123;<span class="hljs-comment">//C=A*B,满足A&gt;=0,B&gt;=0</span><br>    <span class="hljs-function">vector&lt;<span class="hljs-type">int</span>&gt; <span class="hljs-title">C</span><span class="hljs-params">(A.size()+B.size(),<span class="hljs-number">0</span>)</span></span>;<br>    <br>    <span class="hljs-type">int</span> t=<span class="hljs-number">0</span>;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>;i&lt;A.<span class="hljs-built_in">size</span>();i++)<br>    &#123;<br>        <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">0</span>;j&lt;B.<span class="hljs-built_in">size</span>();j++)<br>        &#123;<br>            t+=A[i]*B[i]+C[i+j];<br>            C[i+j]=t%<span class="hljs-number">10</span>;<br>            t/=<span class="hljs-number">10</span>;<br>        &#125;<br>        <span class="hljs-type">int</span> k=B.<span class="hljs-built_in">size</span>();<br>        <span class="hljs-keyword">if</span>(t)<br>        &#123;<br>            C[i+k]=t%<span class="hljs-number">10</span>;<br>            t/=<span class="hljs-number">10</span>;<br>            k++;<br>        &#125;<br>    &#125;<br>    <span class="hljs-keyword">while</span>(C[C.<span class="hljs-built_in">size</span>()<span class="hljs-number">-1</span>]==<span class="hljs-number">0</span>&amp;&amp;C.<span class="hljs-built_in">size</span>()&gt;<span class="hljs-number">1</span>) C.<span class="hljs-built_in">pop_back</span>();<br>    <br>    <span class="hljs-keyword">return</span> C;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="高精度如何输入输出数据？"><a href="#高精度如何输入输出数据？" class="headerlink" title="高精度如何输入输出数据？"></a>高精度如何输入输出数据？</h4><p>以字符串输入，逆序存入数组，逆序输出数组</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">using</span> <span class="hljs-keyword">namespace</span> std;<br><span class="hljs-comment">//以高精度乘高精度举例</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">main</span><span class="hljs-params">()</span></span><br><span class="hljs-function"></span>&#123;<br>    string a,b;<br>    cin&gt;&gt;a&gt;&gt;b;<br>    vector&lt;<span class="hljs-type">int</span>&gt; A,B;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=a.<span class="hljs-built_in">size</span>()<span class="hljs-number">-1</span>;i&gt;=<span class="hljs-number">0</span>;i--) A.<span class="hljs-built_in">push_back</span>(a[i]-<span class="hljs-string">&#x27;0&#x27;</span>);<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=b.<span class="hljs-built_in">size</span>()<span class="hljs-number">-1</span>;i&gt;=<span class="hljs-number">0</span>;i--) B.<span class="hljs-built_in">push_back</span>(b[i]-<span class="hljs-string">&#x27;0&#x27;</span>);<br>    <br>    vector&lt;<span class="hljs-type">int</span>&gt; C=<span class="hljs-built_in">mul</span>(A,B);<br>    <span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=C.<span class="hljs-built_in">size</span>()<span class="hljs-number">-1</span>;i&gt;=<span class="hljs-number">0</span>;i--) <span class="hljs-built_in">printf</span>(<span class="hljs-string">&quot;%d&quot;</span>,C[i]);<br>    <br>    <span class="hljs-keyword">return</span> <span class="hljs-number">0</span>;<br>&#125;<br></code></pre></td></tr></table></figure>

<h4 id="阶乘之和"><a href="#阶乘之和" class="headerlink" title="阶乘之和"></a>阶乘之和</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><code class="hljs cpp"><span class="hljs-comment">//length为所求的阶乘（答案）的位数，n为求1到n的阶乘之和</span><br><span class="hljs-function">string <span class="hljs-title">Bigfact</span><span class="hljs-params">(<span class="hljs-type">int</span> length,<span class="hljs-type">int</span> n)</span></span><br><span class="hljs-function"></span>&#123;<br>	<span class="hljs-type">int</span> a[length]=&#123;<span class="hljs-number">0</span>&#125;,sum[length]=&#123;<span class="hljs-number">0</span>&#125;;<span class="hljs-comment">//a数组放阶乘，sum数组放阶乘之和，这两个数组一定要初始化；</span><br>	a[<span class="hljs-number">0</span>]=<span class="hljs-number">1</span>;<span class="hljs-comment">//1的阶乘为1</span><br>	sum[<span class="hljs-number">0</span>]=<span class="hljs-number">1</span>;<span class="hljs-comment">//1的阶乘没有可加了，所以阶乘之和为1</span><br>	<span class="hljs-keyword">if</span>(n==<span class="hljs-number">1</span>) <span class="hljs-keyword">return</span> <span class="hljs-string">&quot;1&quot;</span>;<br>	<span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">2</span>;i&lt;=n;i++)<br>    &#123;<br>		<span class="hljs-comment">//计算从1到i的阶乘之和</span><br>		<span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">0</span>;j&lt;length;j++)&#123;a[j]*=i;&#125;<span class="hljs-comment">//计算i的阶乘，每一位上都要乘以i，因为没有去除0的操作，所以数位为零的就不用管，依旧是0</span><br>		<span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> j=<span class="hljs-number">0</span>;j&lt;length;j++)<br>        &#123;<span class="hljs-comment">//length其实代表的是那一个数最多有多少位；</span><br>			<span class="hljs-keyword">if</span>(a[j]&gt;=<span class="hljs-number">10</span>)<br>            &#123;<span class="hljs-comment">//当一位数大于10或者等于10的时候就不是一位数了，是两位，所以要往前进一</span><br>			    a[j+<span class="hljs-number">1</span>]+=a[j]/<span class="hljs-number">10</span>;<span class="hljs-comment">//进位运算把十位往前进一位</span><br>			    a[j]%=<span class="hljs-number">10</span>;<span class="hljs-comment">//留下个位</span><br>            &#125;<br>		    sum[j]+=a[j];<span class="hljs-comment">//相当于每一个位对应相加</span><br>		    <span class="hljs-keyword">if</span>(sum[j]&gt;=<span class="hljs-number">10</span>)<br>            &#123;<br>			  sum[j+<span class="hljs-number">1</span>]++;<span class="hljs-comment">//进位运算：和乘法不一样，两个一位数相加只能进一，例如6+8=12，进1留2，乘法则可以进更多，例如：4*6=24，进2留4</span><br>			  sum[j]-=<span class="hljs-number">10</span>;<span class="hljs-comment">//加法留个位就可以用减10</span><br>            &#125;<br>		&#125;<br>	&#125;<br>	<span class="hljs-type">int</span> p=length<span class="hljs-number">-1</span>;<span class="hljs-comment">//数组下标是用长度减一；</span><br>	<span class="hljs-keyword">while</span>(sum[p]==<span class="hljs-number">0</span>)p--;<span class="hljs-comment">//删除默认值为0的，意思就是00001111变成1111从右往左是第一位</span><br>	string ans=<span class="hljs-string">&quot;&quot;</span>;<br>	<span class="hljs-keyword">while</span>(p&gt;=<span class="hljs-number">0</span>)&#123;ans+=sum[p]+<span class="hljs-string">&#x27;0&#x27;</span>;p--;&#125;<span class="hljs-comment">//从最高位开始输出：p--来控制往后输出；意思就是从最高位输出到最后一位，例如：1435，先输出千位1然后百位4十位3依次类推</span><br>	<span class="hljs-keyword">return</span> ans;<br>&#125;<br></code></pre></td></tr></table></figure>



<h3 id="快速幂"><a href="#快速幂" class="headerlink" title="快速幂"></a>快速幂</h3><p>常用小技巧，快速求幂</p>
<h4 id="朴素快速幂"><a href="#朴素快速幂" class="headerlink" title="朴素快速幂"></a>朴素快速幂</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-comment">//求m^k mod p 时间复杂度O(logk)</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">qmi</span><span class="hljs-params">(<span class="hljs-type">int</span> m,<span class="hljs-type">int</span> k,<span class="hljs-type">int</span> p)</span></span><br><span class="hljs-function"></span>&#123;<br>    <span class="hljs-type">int</span> res=<span class="hljs-number">1</span>%p,t=m;<br>    <span class="hljs-keyword">while</span>(k)<br>    &#123;<br>        <span class="hljs-keyword">if</span>(k&amp;<span class="hljs-number">1</span>) res=res*t%p;<br>        t=t*t%p;<br>        k&gt;&gt;=<span class="hljs-number">1</span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> res;<br>&#125;<br></code></pre></td></tr></table></figure>







<h4 id="矩阵快速幂"><a href="#矩阵快速幂" class="headerlink" title="矩阵快速幂"></a>矩阵快速幂</h4><h3 id="前缀和与差分"><a href="#前缀和与差分" class="headerlink" title="前缀和与差分"></a>前缀和与差分</h3><p>前缀和与差分互为逆运算</p>
<h4 id="一维前缀和"><a href="#一维前缀和" class="headerlink" title="一维前缀和"></a>一维前缀和</h4><p>s[i]&#x3D;a[1]+a[2]+…+a[i];<br>a[l]+…+a[r]&#x3D;s[r]-s[l-1];</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++">cin&gt;&gt;a[i];<br>a[i]+=a[i];<span class="hljs-comment">//不保留原数组</span><br><br>s[i]=s[i<span class="hljs-number">-1</span>]+a[i];<span class="hljs-comment">//保留原数组，开新数组存前缀和</span><br></code></pre></td></tr></table></figure>

<h4 id="二维前缀和"><a href="#二维前缀和" class="headerlink" title="二维前缀和"></a>二维前缀和</h4><p>s[i,j]&#x3D;第i行第j列格子左上部分所有元素之和<br>&#x2F;&#x2F;以(x1,y1)为左上角，（x2,y2）为右下角的子矩阵和<br>s[x2,y2]-s[x1-1,y2]-s[x2,y1-1]+s[x1-1,y1-1]</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++">cin&gt;&gt;a[i][j];    <br>a[i][j]+=a[i<span class="hljs-number">-1</span>][j]+a[i][j<span class="hljs-number">-1</span>]-a[i<span class="hljs-number">-1</span>][j<span class="hljs-number">-1</span>];<span class="hljs-comment">//不保留原数组</span><br><br>s[i][j]=s[i<span class="hljs-number">-1</span>][j]+s[i][j<span class="hljs-number">-1</span>]-s[i<span class="hljs-number">-1</span>][j<span class="hljs-number">-1</span>]+a[i][j];<span class="hljs-comment">//保留原数组，开新数组存前缀和</span><br></code></pre></td></tr></table></figure>

<h4 id="一维差分"><a href="#一维差分" class="headerlink" title="一维差分"></a>一维差分</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-comment">//给区间[l,r]中每个数加上c</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">insert</span><span class="hljs-params">(<span class="hljs-type">int</span> l,<span class="hljs-type">int</span> r,<span class="hljs-type">int</span> c)</span></span><br><span class="hljs-function"></span>&#123;<br>    b[l]+=c,b[r+<span class="hljs-number">1</span>]-=c;<br>&#125;<br><br></code></pre></td></tr></table></figure>

<h4 id="二维差分"><a href="#二维差分" class="headerlink" title="二维差分"></a>二维差分</h4><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-comment">//以(x1,y1)为左上角，（x2,y2）为右下角的子矩阵所有元素加上c</span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">insert</span><span class="hljs-params">(<span class="hljs-type">int</span> x1,<span class="hljs-type">int</span> y1,<span class="hljs-type">int</span> x2,<span class="hljs-type">int</span> y2,<span class="hljs-type">int</span> c)</span></span><br><span class="hljs-function"></span>&#123;<br>    b[x1,y1]+=c;<br>    b[x2+<span class="hljs-number">1</span>,y1]-=c;<br>    b[x1,y2+<span class="hljs-number">1</span>]-=c;<br>    b[x2+<span class="hljs-number">1</span>,y2+<span class="hljs-number">1</span>]+=c;<br>&#125;<br><br></code></pre></td></tr></table></figure>



<h3 id="离散化"><a href="#离散化" class="headerlink" title="离散化"></a>离散化</h3><p>将离散的数据映射到一个小区间内</p>
<p>对数据直接mod一个数也相当于将数据映射在这个区间内</p>
<p><strong>1.排序</strong></p>
<p><strong>2.去重</strong></p>
<p><strong>3.查找</strong></p>
<figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++">vector&lt;<span class="hljs-type">int</span>&gt; alls;<span class="hljs-comment">//存储所有待离散化的值</span><br><span class="hljs-built_in">sort</span>(alls.<span class="hljs-built_in">begin</span>(),alls.<span class="hljs-built_in">end</span>());<span class="hljs-comment">//排序</span><br>alls.<span class="hljs-built_in">erase</span>(<span class="hljs-built_in">unique</span>(alls.<span class="hljs-built_in">begin</span>(),alls.<span class="hljs-built_in">end</span>()),alls.<span class="hljs-built_in">end</span>());<span class="hljs-comment">//去重</span><br><span class="hljs-comment">//二分求出对应离散化的值</span><br><span class="hljs-function"><span class="hljs-type">int</span> <span class="hljs-title">find</span><span class="hljs-params">(<span class="hljs-type">int</span> x)</span></span><br><span class="hljs-function"></span>&#123;<span class="hljs-comment">//找到第一个大于等于x的位置</span><br>    <span class="hljs-type">int</span> l=<span class="hljs-number">0</span>,r=alls.<span class="hljs-built_in">size</span>()<span class="hljs-number">-1</span>;<br>    <span class="hljs-keyword">while</span>(l&lt;r)<br>    &#123;<br>        <span class="hljs-type">int</span> mid=l+r&gt;&gt;<span class="hljs-number">1</span>;<br>        <span class="hljs-keyword">if</span>(alls[mid]&gt;=x) r=mid;<br>        <span class="hljs-keyword">else</span> l=mid+<span class="hljs-number">1</span>;<br>    &#125;<br>    <span class="hljs-keyword">return</span> r+<span class="hljs-number">1</span>;<span class="hljs-comment">//映射到1,2,3...n</span><br>&#125;<br></code></pre></td></tr></table></figure>

<h3 id="双指针"><a href="#双指针" class="headerlink" title="双指针"></a>双指针</h3><p>先暴力模拟，然后看i和j有什么单调关系，再套模板将时间复杂度降低</p>
<p>常见问题分类：</p>
<p>(1)对于一个序列，用两个指针维护一段区间</p>
<p>(2)对于两个序列，维护某种次序，比如归并排序中合并两个有序序列的操作</p>
<figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-keyword">for</span>(<span class="hljs-type">int</span> i=<span class="hljs-number">0</span>,j=<span class="hljs-number">0</span>;i&lt;n;i++)<br>&#123;<br>    <span class="hljs-keyword">while</span>(j&lt;i&amp;&amp;<span class="hljs-built_in">check</span>(i,j)) j++;<br>    <br>    <span class="hljs-comment">//具体问题的逻辑</span><br>&#125;<br></code></pre></td></tr></table></figure>



<h3 id="区间合并"><a href="#区间合并" class="headerlink" title="区间合并"></a>区间合并</h3><figure class="highlight c++"><table><tr><td class="code"><pre><code class="hljs c++"><span class="hljs-comment">//将所有存在交集的区间合并</span><br><span class="hljs-meta">#<span class="hljs-keyword">define</span> Pll pair<span class="hljs-string">&lt;long long,long long&gt;</span></span><br><span class="hljs-function"><span class="hljs-type">void</span> <span class="hljs-title">merge</span><span class="hljs-params">(vector&lt;Pll&gt; &amp;segs)</span></span><br><span class="hljs-function"></span>&#123;<br>    vector&lt;Pll&gt; res;<br>    <br>    <span class="hljs-built_in">sort</span>(segs.<span class="hljs-built_in">begin</span>(),segs.<span class="hljs-built_in">end</span>());<br>    <br>    <span class="hljs-type">int</span> st=<span class="hljs-number">-2e9</span>,ed=<span class="hljs-number">-2e9</span>;<br>    <span class="hljs-keyword">for</span>(<span class="hljs-keyword">auto</span> seg:segs)<br>    &#123;<br>        <span class="hljs-keyword">if</span>(ed&lt;seg.first)<br>        &#123;<br>            <span class="hljs-keyword">if</span>(st!=<span class="hljs-number">-2e9</span>) res.<span class="hljs-built_in">push_back</span>(&#123;st,ed&#125;);<br>            st=seg.first,ed=seg.second;<br>        &#125;<br>        <span class="hljs-keyword">else</span><br>        &#123;<br>            ed=<span class="hljs-built_in">max</span>(ed,seg.second);<br>        &#125;<br>    &#125;<br>    <br>    <span class="hljs-keyword">if</span>(st!=<span class="hljs-number">-2e9</span>) res.<span class="hljs-built_in">push_back</span>(&#123;st,ed&#125;);<br>    <br>    segs=res;<br>&#125;<br></code></pre></td></tr></table></figure>

]]></content>
  </entry>
  <entry>
    <title>自我介绍</title>
    <url>/2050/03/14/%E8%87%AA%E6%88%91%E4%BB%8B%E7%BB%8D/</url>
    <content><![CDATA[<p><del>因为喜欢，所以我为之痴迷。</del></p>
<p>其实还没想好写些什么。</p>
]]></content>
      <tags>
        <tag>公告</tag>
      </tags>
  </entry>
  <entry>
    <title>Unity如何生成WebGl发布到网站上分享你的游戏给小伙伴游玩</title>
    <url>/2024/01/18/Unity%E5%A6%82%E4%BD%95%E7%94%9F%E6%88%90WebGl%E5%8F%91%E5%B8%83%E5%88%B0%E7%BD%91%E7%AB%99%E4%B8%8A%E5%88%86%E4%BA%AB%E4%BD%A0%E7%9A%84%E6%B8%B8%E6%88%8F%E7%BB%99%E5%B0%8F%E4%BC%99%E4%BC%B4%E6%B8%B8%E7%8E%A9/</url>
    <content><![CDATA[<h3 id="前言"><a href="#前言" class="headerlink" title="前言"></a>前言</h3><p>本文的内容需要首先掌握unity的基本操作，能够制作小型的游戏demo即可。<del>(毕竟连游戏本体都没有，怎么打包出来？)</del></p>
<p>首先，要明确一点的是，<code>(仅个人观点)</code>unity打包到WebGl这个功能主要用于将小型游戏demo发布试玩，本文是将游戏发布到unity官方提供的空间上，如果想要部署到自己的服务器的话，<del>我暂时不会</del>。</p>
<p>网页快速试玩永远是让大家最容易接受的方法，快来看看如何快速分享你的游戏给小伙伴吧。</p>
<h3 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h3><p>首先，准备好一个你想要分享的简单的游戏demo。好了，第一步就结束捏<del>(bushi)</del></p>
<p>其次，安装<strong>WebGL Build Support</strong>到你的unity游戏项目的编辑器版本。这是非常简单的操作，直接去到<strong>unity hub</strong>中安装的编辑器版本中<strong>添加模块</strong>，勾选WebGl选项。</p>
<p>然后，打开你的unity游戏项目。</p>
<h3 id="打包上传"><a href="#打包上传" class="headerlink" title="打包上传"></a>打包上传</h3><p>将你的unity游戏项目在<strong>Build Settings</strong>中切换到 WebGL 平台。</p>
<p><code>注意！！！</code>切换好平台可能会有一些问题，最主要集中的就是在<strong>Color Space</strong> 上。很可能你的项目设置的是 <strong>Liner</strong>，但是为了生成 WebGL 你需要切换到 <strong>Gamma</strong> 。Gamma和Linear颜色空间，两者有色差，Gamma有个2.25左右的修正值。<del>其实具体的我也不懂</del></p>
<p>接下来，需要安装一个 package 插件包 ：<strong>WebGL Publisher</strong></p>
<p>在你的unity游戏项目中菜单栏找到<strong>window</strong>，打开<strong>Package Manager</strong>，勾选<strong>Unity Registry</strong>，然后搜索该插件包(WebGl Publisher)进行安装。</p>
<p>安装好了之后，你的整个菜单当中会多出来一个 <strong>Publish</strong>的菜单，里面可以打开一个窗口叫做：<strong>WebGL Project</strong> </p>
<p>然后还是用正常的<strong>Build Settings</strong> 的窗口来 Build 保存你的整个项目。当你打包成功了，就可以在 WebGL Project 的窗口里看到你最新打包的 WebGL 项目。 然后点击 <strong>Publish</strong> 就会帮你上传到你的 connect 网站(unity官方提供的网站)上。</p>
<p>上传完成后，会自动在浏览器跳转到网站上，没有的话，就在 WebGL Project 的窗口里应该可以看到一个click相关的字样，点击就能跳转。在网站上，<strong>add</strong>可以为游戏选择一个封面，<strong>play</strong>选项就可以直接试玩游戏。然后填写相关信息即可发布。</p>
<p><strong>至此你就成功上传好了 WebGL 平台的游戏了！可以把网址分享给你的朋友试玩啦！</strong></p>
<h3 id="注意事项"><a href="#注意事项" class="headerlink" title="注意事项"></a>注意事项</h3><p>1.网站仅支持电脑端打开游玩，<del>(我也不知道做其他平台的操作适配后可不可以)</del></p>
<p>2.游戏项目不能太大，不然容易卡加载界面</p>
<p>3.默认的字体打包后不显示中文，需要中文的话需要连相对应的字体文件一起打包<del>(但是字体文件本身也挺大，需要优化)</del></p>
<p>4.默认游玩的时候，音频是关闭的，需要手动打开</p>
<h3 id="写在最后"><a href="#写在最后" class="headerlink" title="写在最后"></a>写在最后</h3><p>好耶，奇怪的知识增加了、</p>
<p>测试的游戏项目试玩网址</p>
<p>Running：<a href="https://play.unity.com/mg/other/runningwebgl-3">Running_Webgl_Test - Unity Play</a></p>
<p>这是我首次接触unity时制作的小游戏<del>(初学者学习项目)</del>，欢迎试玩！！！</p>
]]></content>
      <tags>
        <tag>unity WebGl</tag>
      </tags>
  </entry>
</search>
